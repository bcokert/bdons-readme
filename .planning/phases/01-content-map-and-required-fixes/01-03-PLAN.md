---
phase: 01-content-map-and-required-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02-PLAN
files_modified:
  - readme.md
autonomous: false
requirements:
  - FIX-02
must_haves:
  truths:
    - "The current bullet at line 55 (`Communication and Slack - I depend on frequent signal...`) no longer reads as ambiguous preference — the stakes are stated explicitly OR the bullet is explicitly framed as genuine preference where the report's style wins"
    - "A reader can answer the question 'what happens if I go silent?' from the revised bullet alone, without inferring"
    - "The fix is committed as a single atomic commit on `readme.md` (per D-10)"
    - "Bdon's voice is intact — terse, no HR-coded language, distinctive markers preserved (D-06, Pitfall 8)"
  artifacts:
    - path: "readme.md"
      provides: "Revised communication bullet with explicit stakes OR explicit preference framing"
      contains: "frequent signal"
  key_links:
    - from: "readme.md (Style > Communication and Influence section)"
      to: "the revised bullet"
      via: "explicit stakes phrasing OR explicit-preference phrasing"
      pattern: "frequent signal"
---

<objective>
Apply FIX-02: revise the "I depend on frequent signal / hate silence" bullet at line 55 of `readme.md` so the stakes are unambiguous — either explicit evaluation input or explicit genuine preference.

Purpose: PITFALLS.md Pitfall 3 — preference language from someone who controls the review carries near-mandatory weight regardless of intent. The current bullet reads as preference but functions as a requirement; reports are left to guess. The two valid resolutions are stated-stakes or stated-preference. Bdon picks which signal he wants to send.

Output: One commit modifying `readme.md` to revise line 55. Per D-11, this plan proposes a recommended approach and surfaces the alternative; Bdon approves the final wording at the human-verify checkpoint before the commit lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md
@.planning/research/PITFALLS.md
@readme.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply FIX-02 to readme.md — disambiguate "frequent signal / hate silence"</name>
  <files>readme.md</files>
  <read_first>
    - readme.md (the full file — the bullet under fix is currently at line 55 OR the equivalent line if FIX-01 from plan 01-02 shifted line numbers; locate by content: the bullet starting `Communication and Slack - I depend on frequent signal`)
    - .planning/research/PITFALLS.md (Pitfall 3 — source-of-truth; assessment block at the end of the Pitfall 3 section names both resolution paths verbatim)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md (D-11 documents the two paths; D-06 is the voice rule)
    - CLAUDE.md (voice preservation; markdown only)
  </read_first>
  <action>
**The two documented options (per PITFALLS.md Pitfall 3 and CONTEXT.md D-11):**

**Option A — Explicit evaluation stakes (the bullet is a job requirement; say so):**

Rewrite to make explicit that frequent visible signal is an input to how Bdon evaluates judgment/trust. The reader stops guessing whether to mirror Bdon's preference; they know it affects their review.

Proposed wording:

```markdown
* Communication and Slack - I rely on visible signal (positive, negative, "got it") to calibrate trust and scope. Silence and inactivity make it hard for me to extend autonomy — that's the cost, name it if you need to operate differently. Emoji-acks or short replies showing things are read/understood do the job; explicit "need clarity here" pings are equally welcome.
```

Voice check: that draft is ~50 words vs. the current ~35; the lengthening is justified by Pitfall 3 (the fix is the entire point). Distinctive markers (terse, no warmup, no transition words) preserved. Cuts the word "hate" (an aspirational/preference word — see QUAL-02 trust-claim list).

**Option B — Explicit genuine preference (the bullet is style, not requirement; say that too):**

Rewrite to explicitly mark this as a preference where the report's style wins if it conflicts. Useful if Bdon does NOT want to make visible-signal an evaluation input.

Proposed wording:

```markdown
* Communication and Slack - I work best with frequent visible signal (positive, negative, "got it"). This is how I operate, not a requirement on you. If you communicate differently and it's working, tell me your rhythm and I'll adapt.
```

Voice check: ~45 words, similar lengthening to Option A. Removes "hate silence" (unanchored emotional claim). Removes the implicit demand by stating the constraint is on Bdon, not the reader.

**Recommendation (per D-11): Option A — explicit evaluation stakes.**

Rationale: (a) PITFALLS.md Pitfall 3 frames the "preferences" framing as the problem — Bdon already structurally treats silence as a trust signal, so naming it directly is more honest than reframing it as optional; (b) FIX-01 is also surfacing evaluation criteria, and a coherent Phase 1 message is "evaluation inputs are named, not implied"; (c) Option B requires Bdon to actually adapt to a different report rhythm — that's a behavioral commitment that needs verifying separately. Option A is the safer claim given Pitfall 1 (don't make claims you can't verify behaviorally). BUT — this is Bdon's call. He may genuinely want Option B if his observed behavior already supports it.

**Execution steps:**

1. Open `readme.md`. Locate the bullet by content match: `Communication and Slack - I depend on frequent signal`. Do NOT rely on line 55 verbatim — plan 01-02 may have shifted line numbers depending on which FIX-01 option was applied.
2. Present the two options to Bdon (the checkpoint task below).
3. Apply Bdon's chosen option (default Option A if Bdon does not specify).
4. The bullet structure stays as a `*`-prefixed list item under `### Communication and Influence`. Do NOT promote it to its own subsection (the heading already covers it).
5. Voice check: re-read the modified bullet aloud. No transition words ("furthermore", "additionally", "however"). No HR-coded phrasing ("I value", "psychological safety", "open dialogue"). The phrase "emoji-acks or short replies" or equivalent — preserving the existing voice's specificity about ack patterns — must survive.

**Hard constraints (must not violate):**

- Markdown only. No HTML.
- Do not touch lines 1–46 (Values, Priorities, Career, Work — those are owned by plans 01-01 and 01-02 respectively).
- Do not touch L56 (`Flexibility - I love data...`) — that's FIX-03, plan 01-04.
- Do not touch the Feedback and Performance bullets (L49–L52).
- Do not touch the Learning and Memory bullets (L59–L61).
- Do not remove the substantive content (the request for visible signal is real and should survive in some form — this is not a content cut, it's a stakes disambiguation).
- Do not introduce new aspirational claims with "always", "never", "love", "hate", "I will". Specifically: the existing "hate silence" phrasing should be REMOVED or reframed — it is on the Phase 4 QUAL-02 trust-claim audit list.
  </action>
  <verify>
    <automated>! grep -q 'hate silence' readme.md && grep -q 'frequent signal\|visible signal' readme.md</automated>
  </verify>
  <acceptance_criteria>
    - The existing problematic phrasing `hate silence/inactivity` is gone: `grep -c 'hate silence' readme.md` returns 0.
    - The substantive content (request for visible signal) survives: `grep -ciE 'frequent signal|visible signal' readme.md` returns ≥ 1.
    - The bullet still lives in the Communication and Influence subsection: locate the modified bullet; confirm the nearest `^### ` heading above it is `### Communication and Influence`.
    - Stakes-or-preference disambiguation is present. Verify ONE of these patterns is matched (Option A or Option B language): `grep -ciE 'calibrate trust|extend autonomy|affects? .{0,30}evaluation|input to|how i operate|not a requirement|your rhythm' readme.md` returns ≥ 1.
    - No new banned trust-claim markers introduced: `grep -cE '\bI always\b|\bI never\b|\bI love\b' readme.md` returns the same count as before the edit OR lower (FIX-03 lowers it further in plan 01-04; this plan must not increase it).
    - No HTML: `grep -cE '<[a-zA-Z]' readme.md` returns 0.
    - The bullet remains a list item (starts with `*` or `-`), not promoted to a heading: the modified line begins with `* ` or `- `.
    - Other Communication and Influence bullets are intact: `grep -c 'Flexibility - I love data\|love teaching' readme.md` returns 2 (FIX-03 will modify the first of these in plan 01-04, but not in this plan).
  </acceptance_criteria>
  <done>
    The Communication and Slack bullet is revised so its stakes are explicit (Option A) or it is explicitly framed as genuine preference (Option B). The "hate silence" phrasing is gone. The bullet is still in the Communication and Influence subsection. Voice is preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon approves wording before commit</name>
  <files>readme.md</files>
  <action>Pause execution. Surface the diff from Task 1 to Bdon. Wait for his approval signal before proceeding to Task 3 (the commit). If he requests a different option or wording changes, return to Task 1 and re-apply with the new direction; do not commit until he says "approved".</action>
  <what-built>
    FIX-02 has been applied to `readme.md` using one of the two documented options (default: Option A — explicit evaluation stakes). The edit is in the working tree, not yet committed.
  </what-built>
  <how-to-verify>
    1. Run `git diff readme.md` to see the proposed change.
    2. Read the revised bullet aloud. Does it still sound like you?
    3. Is the disambiguation honest? If Option A (explicit stakes) — does that match how you actually operate? If Option B (genuine preference) — are you actually willing to adapt to a different rhythm?
    4. If you want Option B instead of Option A (or vice versa), say which and the executor rewrites. If you want wording adjusted, say what to change.
    5. If approved, the next task commits.
  </how-to-verify>
  <resume-signal>Type "approved" to commit as-is, "option B" / "option A" to swap, or specify wording changes.</resume-signal>
  <verify>
    <automated>git diff --name-only readme.md | grep -qx readme.md</automated>
  </verify>
  <done>Bdon has reviewed the diff and given an approval signal ("approved", or a swap-to-other-option directive, or specific wording changes that have been re-applied and re-approved).</done>
</task>

<task type="auto">
  <name>Task 3: Commit FIX-02 as a single atomic commit</name>
  <files>readme.md</files>
  <read_first>
    - readme.md (the edited state — confirm the diff matches what Bdon approved at the checkpoint)
  </read_first>
  <action>
Per D-10, FIX-02 lands as a single atomic commit on `readme.md`.

Commit message (use HEREDOC):

```
fix(01): disambiguate communication stakes (FIX-02)

PITFALLS.md Pitfall 3: "I depend on frequent signal / hate silence/inactivity"
read as preference but functioned as a requirement. Revised to {explicit
evaluation stakes / explicit genuine preference — fill in the chosen path}.

Removes "hate silence" from the doc (trust-claim audit per Phase 4 QUAL-02).

Phase 1 of the readme restructure; lands the fix in source before Phase 2
extracts content to style.md.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Replace the bracketed `{...}` text with the actual option applied. Stage only `readme.md`.

```
git add readme.md
git commit -m "$(cat <<'EOF'
... message above ...
EOF
)"
```
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q 'FIX-02' && git diff HEAD~1 HEAD --name-only | grep -qx 'readme.md'</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject contains `FIX-02`: `git log -1 --format='%s' | grep -c 'FIX-02'` returns 1.
    - Latest commit modifies exactly `readme.md` and nothing else: `git diff HEAD~1 HEAD --name-only` returns a single line matching `^readme\.md$`.
    - The previous commit (HEAD~1) is the FIX-01 commit: `git log -2 --format='%s' | grep -c 'FIX-01\|FIX-02'` returns 2 (D-10 atomic-commits ordering).
    - Working tree clean: `git status --porcelain readme.md` is empty.
  </acceptance_criteria>
  <done>
    FIX-02 is a single atomic commit on `readme.md`. The diff is exactly the approved edit. The commit follows the FIX-01 commit from plan 01-02.
  </done>
</task>

</tasks>

<verification>
- `git log --oneline -3` shows FIX-02 most recent, FIX-01 before it.
- `readme.md` no longer contains the phrase "hate silence".
- The substantive request for visible signal still exists, with explicit stakes or explicit preference framing.
</verification>

<success_criteria>
Phase 1 Success Criterion 3 ("The 'I depend on frequent signal / hate silence' passage is revised to state explicit stakes (evaluation input, not mere preference)") is satisfied — either as explicit stakes (the success-criterion default reading) or as explicit-preference if Bdon chooses Option B (PITFALLS.md Pitfall 3 names both as valid resolutions; the success criterion's "stakes" language allows the explicit-preference framing because the stakes are then explicitly *none*). Requirement FIX-02 is implemented in source.
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-map-and-required-fixes/01-03-SUMMARY.md` per the standard summary template. Include which of the two options (A/B) Bdon selected and the exact revised bullet text.
</output>
