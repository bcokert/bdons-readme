---
phase: 01-content-map-and-required-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every numbered bullet, paragraph, sub-heading, and section in readme.md is listed in 01-CONTENT-MAP.md with an explicit destination file"
    - "No row in the destination column reads 'TBD', 'ambiguous', or 'either'"
    - "The six boundary cases flagged in 01-CONTEXT.md (Test when Reversible, Work Out Loud, Flexibility/love data, Learning and Memory, Career summed up, Values/Priorities chains) each have a row with a Notes-column rationale"
    - "Destination values are restricted to the canonical set: work.md | style.md | playbooks.md | hub-inline"
    - "Phase 2 can read 01-CONTENT-MAP.md and port every item without re-deriving placement"
    - "The content map ships as its own atomic commit (per D-10: 'Content map ships in a fourth commit')"
  artifacts:
    - path: ".planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md"
      provides: "Bullet-level destination map for every item in readme.md"
      contains: "| Source location | Content snippet | Destination | Notes |"
  key_links:
    - from: ".planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md"
      to: "readme.md"
      via: "line-number source-location column"
      pattern: "L\\d+"
    - from: ".planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md"
      to: ".planning/research/ARCHITECTURE.md (File Map table)"
      via: "destination categories match File Map rows"
      pattern: "work\\.md|style\\.md|playbooks\\.md|hub-inline"
---

<objective>
Produce 01-CONTENT-MAP.md — a bullet-level destination map for every item in the current `readme.md`. This is the artifact Phase 2 reads when porting content to spoke files. Then commit it as its own atomic commit per D-10.

Purpose: Forces every ambiguous category seam to surface and be resolved before any content is moved. Prevents Pitfall 7 (category bleed) by closing the question "does this go in work.md or style.md?" once, in writing, with rationale.

Output: One new file at `.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md`, committed as a single atomic commit. No edits to `readme.md` in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@readme.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 01-CONTENT-MAP.md with bullet-level destination assignments</name>
  <files>.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md</files>
  <read_first>
    - readme.md (the full 62-line source — every bullet, every heading, every paragraph must appear as a row)
    - .planning/research/ARCHITECTURE.md (File Map table around line 104–118 — canonical destinations are work.md, style.md, playbooks.md, plus hub-inline for values/priorities/career equations)
    - .planning/research/PITFALLS.md (Pitfall 7 "Category Bleed in Per-File Split" — boundary cases live here)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md (the six pre-flagged boundary cases in `<specifics>` MUST appear as rows with Notes-column rationale)
    - CLAUDE.md (voice rule reminder — even map Notes prose stays terse)
  </read_first>
  <action>
Create the file at `.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` with this exact structure (per D-09):

```markdown
# Phase 1 Content Map

**Phase:** 01-content-map-and-required-fixes
**Source:** `readme.md` (62 lines, pre-rename, pre-restructure)
**Granularity:** bullet-level (per D-08) — every bullet, paragraph, sub-heading appears as a row
**Destinations:** one of `work.md` | `style.md` | `playbooks.md` | `hub-inline`
**Created:** {today's date}

## How to read this map

- **Source location** column: line number (e.g., `L23`) or line range (e.g., `L1–L9`) in current `readme.md`.
- **Content snippet** column: first 5–8 words of the source line, enough to disambiguate from other bullets. Use backticks for verbatim snippets.
- **Destination** column: exactly one of `work.md`, `style.md`, `playbooks.md`, or `hub-inline`. No `TBD`, no `either/or`.
- **Notes** column: empty for unambiguous items; one short sentence for boundary calls explaining the rationale. Reference the relevant Pitfall when applicable (e.g., "Pitfall 7: style-side because it's a communication claim, not a work principle").

## Map

| Source location | Content snippet | Destination | Notes |
|---|---|---|---|
| L1 | `# Bdon's Manager Readme` (title) | hub-inline | Hub title stays. |
| L3–L9 | `## Values and Priorities` heading + `### My Values summed up` + chain `Learning -> Freedom -> Adaptability -> Calm` + 4 sub-bullets | hub-inline | Per ARCHITECTURE.md: values equations and chains stay inline on hub (`values.md` is explicitly out of scope, D-03). |
| ... | ... | ... | ... |
```

(That preamble is fixed. Now produce a complete table covering ALL 62 lines of `readme.md`.)

**Coverage rules — every row of the source must be accounted for:**

1. Walk `readme.md` top to bottom.
2. For each markdown unit (heading, bullet, paragraph, blank-separated chunk), emit one row.
3. Group consecutive bullets that share an obvious unit (e.g., the 4 bullets under `### My Values summed up`) into a single row spanning the line range — but only if their destination is identical. If two adjacent bullets split categories, give each its own row.
4. Where the source has decorative whitespace (blank lines, repeated `*` separators), skip those — they are not content.

**Destination assignments — apply these rules:**

| readme.md content | Destination | Rationale source |
|---|---|---|
| Title (L1) | hub-inline | trivial |
| `## Values and Priorities`, both chains (Values, Priorities), Career paragraph + equation (L3–L23) | hub-inline | ARCHITECTURE.md File Map; D-03 (no values.md) |
| `## Work` > `### General Expectations` 4 bullets (L27–L35) — Test when Reversible, Work Out Loud, Prioritize Proactivity, Treat AI as Powertool | work.md | ARCHITECTURE.md File Map row for work.md; Pitfall 7 explicitly assigns Test-when-Reversible and Work-Out-Loud to work.md |
| `## Work` > `### Dev Expectations` paragraph + D1–D5 link list (L37–L45) | work.md | ARCHITECTURE.md hub spec (Dev Expectations block links from work.md via the spoke; D-files themselves are separate) — Note: hub will ALSO carry a Dev Expectations link block per HUB-05 (Phase 3), but the source content here ports to work.md per ARCHITECTURE.md File Map |
| `## Bdons Style` > `### Feedback and Performance` — 3 bullets (L49–L52) | style.md | ARCHITECTURE.md File Map |
| `## Bdons Style` > `### Communication and Influence` > `Communication and Slack` bullet (L55) | style.md | ARCHITECTURE.md File Map |
| `## Bdons Style` > `### Communication and Influence` > `Flexibility - I love data` bullet (L56) | style.md | **Boundary case** — flagged in 01-CONTEXT.md `<specifics>`: it's a communication/influence claim, not a work principle. Pitfall 7 cites this exact line. Note: this is also the FIX-03 subject. |
| `## Bdons Style` > `### Communication and Influence` > `I love teaching, analogies and metaphors` bullet (L57) | style.md | Same section, same destination |
| `## Bdons Style` > `### Learning and Memory` — 2 bullets (L59–L61) | style.md | **Boundary case** — flagged in 01-CONTEXT.md `<specifics>`. ARCHITECTURE.md File Map row for style.md lists "learning/memory quirks" explicitly. |

**Specifics column — for these six boundary cases (per 01-CONTEXT.md `<specifics>` block), the Notes column MUST be populated with a one-sentence rationale, not left blank:**

1. `Test when Reversible` (L29) → `work.md` — Notes: "Pitfall 7: operating principle despite living next to values; work.md per ARCHITECTURE.md File Map."
2. `Work Out Loud with Intent` (L31) → `work.md` — Notes: "Pitfall 7 boundary; Phase 2 question: does playbooks.md need a cross-reference when 1:1s or Decisions invoke it? Mark with a TODO for Phase 2 planner, not a destination split."
3. `Flexibility - I love data` (L56) → `style.md` — Notes: "Pitfall 7: communication/influence claim, not a work principle. Subject of FIX-03 in plan 01.4 — the corrected text is what ports."
4. `Learning and Memory` (L59–L61) → `style.md` — Notes: "ARCHITECTURE.md File Map row for style.md lists 'learning/memory quirks'."
5. `My Career summed up` paragraph + equation (L18–L23) → `hub-inline` — Notes: "ARCHITECTURE.md hub spec section 1 'Who I Am' uses this paragraph + equation as raw material. Equation render-test is a separate Phase 3 blocker (STATE.md)."
6. `My Values summed up` + `My Priorities summed up` chains (L3–L16) → `hub-inline` — Notes: "ARCHITECTURE.md hub spec section 2 'What I Optimize For'; D-03 (no values.md)."

**Phase 2 TODO row at the end of the table:**

Add a final row (or short subsection after the table) listing Phase-2-only questions the map surfaces but does not answer:

```markdown
## Phase 2 follow-ups (not Phase 1 work)

- Should `playbooks.md` cross-reference `work.md` for Work Out Loud when 1:1s or Decisions playbooks invoke it? (Pitfall 7 boundary; resolve when drafting playbooks.md.)
- Power-asymmetry acknowledgment in `style.md` Feedback section (PITFALLS.md Pitfall 2, LOW severity, PARTIAL FLAG) — not a Phase 1 FIX, but Phase 2 should consider it when porting feedback content.
```

**Voice rule (D-06):** Map Notes prose stays terse — one sentence per cell, no warmup, no inflation. The map is a working document, not a memo.

**Format compliance (CLAUDE.md):** GFM markdown only. No inline HTML. Use the standard `|` table pipe syntax. Lowercase-with-hyphens filenames in the Destination column (`work.md`, `style.md`, `playbooks.md`).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md && grep -c '^| ' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns 0.
    - Table has at least 12 content rows (header row + alignment row + at least 12 data rows): `awk -F'|' '/^\|/ && !/^\|---/{n++} END{print n}'` on the file returns ≥ 13.
    - The four canonical destinations are the only ones used: `grep -oE 'hub-inline|work\.md|style\.md|playbooks\.md' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md | sort -u | wc -l` returns exactly 4.
    - No banned ambiguity values: `grep -E '\bTBD\b|\beither\b|\bambiguous\b|\?' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns no rows in the table body (matches inside the `## How to read this map` preamble that explain the rule are allowed; matches inside data rows are not).
    - All six boundary cases referenced by line: `grep -cE 'L29|L31|L56|L59|L18|L3' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns ≥ 6.
    - Pitfall 7 cited at least once in Notes column: `grep -c 'Pitfall 7' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns ≥ 1.
    - Phase 2 follow-ups section exists: `grep -c '## Phase 2 follow-ups' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns exactly 1.
    - No inline HTML introduced: `grep -cE '<[a-zA-Z]' .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns 0.
  </acceptance_criteria>
  <done>
    01-CONTENT-MAP.md exists, table covers every content unit in readme.md, all six pre-flagged boundary cases carry rationale, the four-destination vocabulary is the only one in use, and the Phase 2 follow-ups section captures items the map surfaces but does not resolve.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit 01-CONTENT-MAP.md as its own atomic commit (D-10 fourth commit)</name>
  <files>.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md</files>
  <read_first>
    - .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md (confirm the file exists and the diff matches Task 1's output before staging)
  </read_first>
  <action>
Per D-10 ("Content map ships in a fourth commit"), the content map lands as its own atomic commit — separate from FIX-01/FIX-02/FIX-03. This task stages and commits ONLY the content map; no other paths should be in the diff.

Note on ordering: this plan is in Wave 1 alongside plan 01-02 (FIX-01). In execution order, this commit is created when plan 01-01 finishes — which may be before or after the FIX commits depending on wave parallelism. D-10's "fourth commit" phrasing is about atomicity (one commit per artifact), not strict chronological position relative to the fixes. The acceptance criterion below verifies atomicity, not order.

Commit message (use HEREDOC):

```
docs(01): add bullet-level content map for readme.md

01-CONTENT-MAP.md is the Phase 1 artifact Phase 2 reads when porting
readme.md content to spoke files (work.md, style.md, playbooks.md) or
keeping it hub-inline. Bullet-level granularity per D-08; four-destination
vocabulary per ARCHITECTURE.md File Map; six boundary cases carry rationale
per 01-CONTEXT.md.

Per D-10: ships as its own atomic commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Execution:

```
git add .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md
git commit -m "$(cat <<'EOF'
... message above ...
EOF
)"
```

Stage ONLY the content-map file. If `git status` shows other modified or untracked paths in the working tree, do NOT include them in this commit. Other plans (01-02, 01-03, 01-04) handle their own commits.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -qiE 'content map|01-CONTENT-MAP' && git diff HEAD~1 HEAD --name-only | grep -qx '.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md' && [ "$(git diff HEAD~1 HEAD --name-only | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject references the content map: `git log -1 --format='%s' | grep -ciE 'content map|01-CONTENT-MAP'` returns ≥ 1.
    - Latest commit modifies exactly one path: `git diff HEAD~1 HEAD --name-only | wc -l` returns 1.
    - That one path is the content map: `git diff HEAD~1 HEAD --name-only` returns exactly `.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md`.
    - Working tree is clean for the content-map file post-commit: `git status --porcelain .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md` returns empty.
  </acceptance_criteria>
  <done>
    01-CONTENT-MAP.md is committed as a single atomic commit containing only that file. D-10's "fourth commit" promise — content map ships separately from the three FIX commits — is satisfied.
  </done>
</task>

</tasks>

<verification>
- Walk `readme.md` line by line and confirm every non-trivial line is referenced in the map.
- Confirm no row has destination "TBD" or "either" or a question mark in the destination column.
- Confirm the Notes column is populated for at least the six pre-flagged boundary cases.
- Confirm `git log --oneline` shows a new commit for the content map containing only that file in its diff.
</verification>

<success_criteria>
Phase 1 Success Criterion 1 ("A written content map exists listing each item in `readme.md` and its target file with no ambiguous assignments") is satisfied by the existence of `01-CONTENT-MAP.md` meeting the acceptance criteria above. D-10's atomic-commit rule for the content map is satisfied by Task 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-map-and-required-fixes/01-01-SUMMARY.md` per the standard summary template.
</output>
