---
phase: 01-content-map-and-required-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - readme.md
autonomous: false
requirements:
  - FIX-01
must_haves:
  truths:
    - "A new reader can locate 'My primary evaluation criteria' in under 30 seconds without scanning bullet bodies (PITFALLS.md Pitfall 4 acceptance bar)"
    - "The evaluation-criteria content is no longer in the third-bullet-in-a-list position it currently occupies on line 33"
    - "The fix is committed as a single atomic commit on `readme.md` (per D-10)"
    - "Bdon's voice is intact — the edit does not lengthen the surrounding bullet bodies and does not introduce HR-coded language (D-06, Pitfall 8)"
    - "The new evaluation-criteria section body stays terse — ≤ 35 words (Pitfall 8 enforcement: bloat caught at the section level, not just the file level)"
  artifacts:
    - path: "readme.md"
      provides: "Promoted evaluation-criteria signal — own subsection OR lead-position bullet OR typographic prominence (one of three documented paths)"
      contains: "Proactivity"
  key_links:
    - from: "readme.md (Work section)"
      to: "the evaluation-criteria line"
      via: "visual prominence — heading level, lead position, or typographic differentiation"
      pattern: "evaluation criteria"
---

<objective>
Apply FIX-01: surface "My primary evaluation criteria" from its current buried third-bullet-in-a-list position (line 33) to an unmissable position in `readme.md`.

Purpose: PITFALLS.md Pitfall 4 — buried evaluation criteria are the highest-severity content issue in the current readme. A new report onboarding will not clock the most important line in the doc if it lives mid-list under "General Expectations." This fix lands in Phase 1 so the corrected source is what Phase 2 ports to `work.md`, not the broken version.

Output: One commit modifying `readme.md` to promote the evaluation-criteria signal. Per D-11, this plan proposes a recommended approach and surfaces the alternatives; Bdon approves the final wording at the human-verify checkpoint before the commit lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md
@.planning/research/PITFALLS.md
@readme.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply FIX-01 to readme.md — promote evaluation-criteria signal</name>
  <files>readme.md</files>
  <read_first>
    - readme.md (the full file — see current state at L27–L35 for `### General Expectations` and the buried L33 line)
    - .planning/research/PITFALLS.md (Pitfall 4 — source-of-truth for this fix; assessment block names the exact problem)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTEXT.md (D-11 documents the three options; D-06 is the voice rule)
    - CLAUDE.md (voice preservation; markdown only; no HTML)
  </read_first>
  <action>
**The three documented options (per PITFALLS.md Pitfall 4 and CONTEXT.md D-11):**

**Option A — Own subsection ("How I Evaluate You" or equivalent):**

Promote the evaluation criteria to its own `### How I Evaluate You` (or `### How You're Evaluated`) subsection inside `## Work`, above `### General Expectations`. The current line 33 bullet becomes the body of that subsection. Voice cost: adds one heading line. Visibility cost: zero — heading shows up in the GitHub TOC.

Proposed structure:

```markdown
## Work

### How I Evaluate You

**Proactivity & Ownership.** This determines how much you're trusted to take on and thus how much impact you can create in your role. It's the primary input to performance.

### General Expectations

**Test when Reversible, Analyze Otherwise**: ...
**Work Out Loud with Intent, not Permission**: ...
**Treat AI as a Powertool**: ...
```

Note that under Option A, the proactivity bullet is REMOVED from the General Expectations list (now a 3-item list) and its content moves up into the new subsection.

**Option B — Lead position in the list:**

Move the existing bullet to be the first item under `### General Expectations` rather than the third. Optionally bold-flag it with a leading marker. Voice cost: minimal. Visibility cost: still inside a list, but at the position a scanning reader's eye lands first.

Proposed structure:

```markdown
### General Expectations

**Prioritize Proactivity & Ownership** _(my primary evaluation criteria)_: This determines how much you're trusted to take on and thus how much impact you can create in your role.

**Test when Reversible, Analyze Otherwise**: ...
**Work Out Loud with Intent, not Permission**: ...
**Treat AI as a Powertool**: ...
```

**Option C — Typographic prominence (blockquote / callout):**

Lift the bullet out of the list entirely into a blockquote that sits before `### General Expectations`, while keeping the four-bullet list intact for the other three principles. Voice cost: low; blockquote is GFM-native. Visibility cost: zero — blockquote breaks the visual flow and forces attention.

Proposed structure:

```markdown
## Work

> **My primary evaluation criteria: Proactivity & Ownership.** This determines how much you're trusted to take on and thus how much impact you can create in your role.

### General Expectations

**Test when Reversible, Analyze Otherwise**: ...
**Work Out Loud with Intent, not Permission**: ...
**Treat AI as a Powertool**: ...
```

**Recommendation (per D-11): Option A — own subsection.**

Rationale: (a) it survives the Phase 2 port to `work.md` cleanly — Phase 3 hub also benefits because a named subsection is link-target-able; (b) the heading shows up in GitHub's native TOC, raising visibility for refresher reads; (c) the voice cost is one heading line, which is well within the "any edit that lengthens requires justification" rule (D-06) — the justification is the entire point of Pitfall 4; (d) it removes the buried bullet from the list rather than just reordering, which is the strongest fix.

**Execution steps:**

1. Open `readme.md`.
2. Present the three options above to Bdon (the checkpoint task below).
3. Apply Bdon's chosen option. If Bdon does not specify, apply Option A.
4. Verify line counts: under Option A, the original L33 line is removed and a new `### How I Evaluate You` subsection (with 2–3 lines of body) is inserted before `### General Expectations`. The four-bullet list becomes a three-bullet list.
5. Voice check: re-read the modified Work section. Confirm no transition words ("furthermore", "additionally", "it's important to note that") were introduced. Confirm "My memory is shit" style — terse, declarative — is preserved in the new subsection body.
6. **Post-checkpoint, before the commit task runs:** if Bdon picked Option A and approved a heading whose wording differs from `### How I Evaluate You` (e.g., `### How You're Evaluated`, or `My primary evaluation criteria` promoted to its own subsection heading), update the awk pattern in Task 3's section-body-word-cap acceptance criterion to match the approved heading text exactly. If Bdon picked Option B (lead-position bullet) or Option C (blockquote), no new `### ` subsection is introduced — in that case, swap the section-body-word-cap check for an equivalent bullet-body / blockquote-body word count using the same ≤ 35-word ceiling, anchored to the approved text.
7. Do not commit yet — the next task is the checkpoint, then the commit task.

**Hard constraints:**

- Markdown only. No HTML, no `<details>` collapsible, no inline `<a>` anchors (CLAUDE.md).
- Do not change the wording of the OTHER three bullets (Test when Reversible, Work Out Loud, Treat AI as a Powertool). This plan touches only the evaluation-criteria content.
- Do not move the line into `## Bdons Style` or any other section. It stays in `## Work`.
- Do not delete the FIX-01 content — it is being promoted, not removed. The phrase "primary evaluation criteria" or equivalent claim must remain (PITFALLS.md is explicit that this signal needs to survive, just at higher prominence).
- Do not touch lines 1–26 (Values, Priorities, Career), lines 47–62 (Bdons Style), or `### Dev Expectations` (L37–L45). The fix is local to the General Expectations block.
- **Section-body word cap (Pitfall 8 enforcement):** The body of the new evaluation-criteria container (subsection / lead bullet / blockquote) is ≤ 35 words. This cap is enforced as an acceptance criterion in Task 3 (the commit task). Draft accordingly — the recommended Option A body above is ~30 words, well under the cap.
  </action>
  <verify>
    <automated>diff <(grep -c 'primary evaluation\|How I Evaluate\|evaluation criteria' readme.md) <(echo 1) || diff <(grep -c 'primary evaluation\|How I Evaluate\|evaluation criteria' readme.md) <(echo 2)</automated>
  </verify>
  <acceptance_criteria>
    - The original buried-in-list pattern is gone: the line `**Prioritize Proactivity & Ownership**: My primary evaluation criteria.` no longer exists as a mid-list bullet inside `### General Expectations`. Verify with: `grep -B1 -A1 'Prioritize Proactivity' readme.md` — the surrounding context must NOT be "**Work Out Loud...**" immediately above AND "**Treat AI...**" immediately below (which is the current buried-bullet sandwich).
    - The evaluation signal still exists in the doc: `grep -ciE 'evaluation criteria|how i evaluate|primary.*evaluation|evaluation.*primary' readme.md` returns ≥ 1.
    - The signal lives in `## Work`: locate the line; confirm the nearest `^## ` heading above it is `## Work`, not `## Bdons Style` or `## Values and Priorities`.
    - Voice preserved: the modified Work section adds ≤ 3 lines net (heading + body OR repositioned bullet). Compute: `wc -l readme.md` should be within `[original_count, original_count + 3]`.
    - No HTML introduced: `grep -cE '<[a-zA-Z]' readme.md` returns 0.
    - No banned aspirational-claim markers introduced: `grep -cE '\bI always\b|\bI never\b' readme.md` returns the same count as before the edit (FIX-03 will reduce this in plan 01.4; this plan must not increase it).
    - The other three General Expectations principles are intact: `grep -c 'Test when Reversible\|Work Out Loud with Intent\|Treat AI as a Powertool' readme.md` returns 3.
  </acceptance_criteria>
  <done>
    The buried-in-list pattern at line 33 is gone; the evaluation-criteria signal survives in a more visible position (own subsection, lead bullet, or blockquote); the other three principles and the rest of the doc are untouched; voice is preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon approves wording before commit</name>
  <files>readme.md</files>
  <action>Pause execution. Surface the diff from Task 1 to Bdon. Wait for his approval signal before proceeding to Task 3 (the commit). If he requests a different option or wording changes, return to Task 1 and re-apply with the new direction; do not commit until he says "approved".</action>
  <what-built>
    FIX-01 has been applied to `readme.md` using one of the three documented options (default: Option A — own subsection). The edit is in the working tree, not yet committed.
  </what-built>
  <how-to-verify>
    1. Run `git diff readme.md` to see the proposed change.
    2. Read the modified Work section aloud. Does it still sound like you? Terse, deadpan, no warmup, no inflation.
    3. Confirm the evaluation signal is visible — would a new report clock this in under 30 seconds on a first scroll?
    4. If you want a different option (B or C) instead, say which one and the executor will rewrite. If you want the wording adjusted, say what to change.
    5. If approved, the next task commits.
  </how-to-verify>
  <resume-signal>Type "approved" to commit as-is, "option B", "option C", or specify wording changes.</resume-signal>
  <verify>
    <automated>git diff --name-only readme.md | grep -qx readme.md</automated>
  </verify>
  <done>Bdon has reviewed the diff and given an approval signal ("approved", or a swap-to-other-option directive, or specific wording changes that have been re-applied and re-approved).</done>
</task>

<task type="auto">
  <name>Task 3: Commit FIX-01 as a single atomic commit</name>
  <files>readme.md</files>
  <read_first>
    - readme.md (the edited state — confirm the diff matches what Bdon approved at the checkpoint)
  </read_first>
  <action>
Per D-10, FIX-01 lands as a single atomic commit on `readme.md`.

**Before staging:** confirm the section-body word-cap acceptance criterion below matches the heading/container wording Bdon approved at the Task 2 checkpoint. If the executor adjusted the awk pattern per Task 1 step 6 (e.g., to `### How You're Evaluated`, or to a bullet/blockquote container under Option B/C), the adjusted pattern is the one that runs.

Commit message (use this format via HEREDOC):

```
fix(01): promote evaluation criteria to unmissable position (FIX-01)

PITFALLS.md Pitfall 4: "My primary evaluation criteria" was buried as the third
bullet in a four-item General Expectations list. Promoted to {own subsection /
lead position / blockquote — fill in the chosen path}.

Phase 1 of the readme restructure; lands the fix in source before Phase 2
extracts content to work.md.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

Replace the bracketed `{...}` text with the actual option that was applied. Stage only `readme.md` (no other files should be in the diff for this plan).

```
git add readme.md
git commit -m "$(cat <<'EOF'
... message above ...
EOF
)"
```
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -q 'FIX-01' && git diff HEAD~1 HEAD --name-only | grep -qx 'readme.md'</automated>
  </verify>
  <acceptance_criteria>
    - Latest commit subject contains `FIX-01`: `git log -1 --format='%s' | grep -c 'FIX-01'` returns 1.
    - Latest commit modifies exactly `readme.md` and nothing else: `git diff HEAD~1 HEAD --name-only` returns a single line matching `^readme\.md$`.
    - Working tree is clean post-commit (other files from other plans not staged): `git status --porcelain readme.md` is empty.
    - **Section-body word cap ≤ 35 (Pitfall 8 enforcement):** the body of the new evaluation-criteria container is ≤ 35 words. Default check (Option A, default wording): `awk '/^### How I Evaluate You/{flag=1; next} /^### /{flag=0} flag' readme.md | wc -w` returns a value ≤ 35. If Bdon approved a different Option A heading (e.g., `### How You're Evaluated`), the executor has already adjusted the awk pattern per Task 1 step 6 — run the adjusted pattern. If Bdon picked Option B (lead-position bullet), substitute: `awk '/^\*\*Prioritize Proactivity/,/^\*\*/' readme.md | head -n -1 | wc -w` returns ≤ 35 (counting only the lead bullet's body, not the following bullets — adjust the awk pattern to whatever lead-marker wording was approved). If Bdon picked Option C (blockquote), substitute: `awk '/^> /' readme.md | wc -w` returns ≤ 35 (counting the blockquote lines that contain the promoted evaluation signal — verify only one blockquote exists in the Work section, or scope the awk range to the Work section if multiple blockquotes appear elsewhere).
  </acceptance_criteria>
  <done>
    FIX-01 is a single atomic commit on `readme.md`. The diff is exactly the approved edit. No other files are in the commit. The new evaluation-criteria container body is ≤ 35 words.
  </done>
</task>

</tasks>

<verification>
- A `git log --oneline` shows one new commit referencing FIX-01.
- `readme.md` no longer has the buried-in-list pattern at the old line 33.
- The evaluation-criteria signal is locatable in under 30 seconds on a cold read of the Work section.
- The new evaluation-criteria container body is ≤ 35 words (Pitfall 8 enforcement at the section level, not just the file level).
</verification>

<success_criteria>
Phase 1 Success Criterion 2 ("My primary evaluation criteria is marked for promotion to an unmissable position — not buried in a list") is satisfied. Requirement FIX-01 is implemented in source.
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-map-and-required-fixes/01-02-SUMMARY.md` per the standard summary template. Include in the summary which of the three options (A/B/C) Bdon selected and the exact promoted text.
</output>
