---
phase: 03-hub-restructure-and-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - readme.md
autonomous: true
requirements:
  - HUB-02
  - HUB-03
  - HUB-04
  - HUB-05
  - HUB-06
tags:
  - markdown
  - hub
  - restructure

must_haves:
  truths:
    - "Hub fits in one GitHub viewport at default zoom (≤380 words / ≤55 rendered lines)"
    - "Reader sees Bdon's identity statement + Autonomy × ∫(Capability × Confidence) equation without clicking"
    - "Reader can scent which spoke answers their question from the link block teases (no title-only links)"
    - "All 3 spoke files (work.md, style.md, playbooks.md) are reachable from the hub"
    - "All 5 D-level files (coop-d1, junior-d2, intermediate-d3, senior-d4, staff-d5) are reachable from the hub with level teases"
    - "Reader can see when the hub was last reviewed"
  artifacts:
    - path: "readme.md"
      provides: "Restructured one-page hub content"
      contains: "Autonomy"
    - path: "readme.md"
      provides: "Four bold-arrow link blocks (3 spokes + 1 D-level block)"
      contains: "→](./"
    - path: "readme.md"
      provides: "Last-reviewed date line"
      contains: "last reviewed: 2026-05-14"
  key_links:
    - from: "readme.md"
      to: "./work.md"
      via: "bold-arrow link block + italic tease"
      pattern: '\*\*\[.*→\]\(\./work\.md\)\*\*'
    - from: "readme.md"
      to: "./style.md"
      via: "bold-arrow link block + italic tease"
      pattern: '\*\*\[.*→\]\(\./style\.md\)\*\*'
    - from: "readme.md"
      to: "./playbooks.md"
      via: "bold-arrow link block + italic tease"
      pattern: '\*\*\[.*→\]\(\./playbooks\.md\)\*\*'
    - from: "readme.md"
      to: "./coop-d1.md … ./staff-d5.md"
      via: "Dev Expectations link block with five level links"
      pattern: 'coop-d1|junior-d2|intermediate-d3|senior-d4|staff-d5'
---

<objective>
Restructure the existing `readme.md` content in-place into the one-page hub layout: identity content + inline equation, four information-scent link blocks (3 spokes + 1 Dev Expectations block), and a "last reviewed: 2026-05-14" line. File rename to `README.md` happens in plan 03-02 — this plan keeps the filename lowercase so spoke back-links continue to resolve during the restructure.

Purpose: Compress the current 637-word / 63-line flat-content hub into the one-screen orientation layer that satisfies HUB-02 through HUB-06. The hub stops being a content document and becomes a router with enough identity context that a reader knows who Bdon is and where to drill in.

Output: `readme.md` rewritten in place to ≤380 words / ≤55 rendered lines, four bold-arrow link blocks, five D-level links with level teases, last-reviewed date.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@./readme.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@.planning/phases/02-spoke-files/02-04-SUMMARY.md
@./work.md
@./style.md
@./playbooks.md

<interfaces>
<!-- Spoke file shapes the hub links into. Extracted from spoke heads/tails. -->
<!-- Executor uses these to write accurate teases — no spoke re-exploration needed. -->

From ./work.md (top + heading structure):
```
_How I evaluate peeps, myt four general principles, and specific role expectations._
## Work
### How I Evaluate You          # Proactivity & Ownership (FIX-01 promoted)
### General Expectations         # Test-when-Reversible / Work-Out-Loud / AI-as-Powertool
### Dev Expectations             # contains D1–D5 link list
```

From ./style.md (top + heading structure):
```
_How I give and receive feedback, communicate on Slack, and prefer to learn._
## Bdons Style
### Feedback and Performance     # giving + receiving feedback, ideal perf review
### Communication and Influence  # Slack signal, Flexibility, teaching analogies
### Learning and Memory          # test-to-learn, "my memory is shit"
```

From ./playbooks.md (top + heading structure):
```
_How I run evaluations, delegate, structure 1:1s, and make decisions._
## Playbooks
### Evaluate
### Delegate
### 1:1s
### Decisions
```

From current ./readme.md (content to preserve / port / cut):
- L1–L23: `## Values and Priorities` (Values + Priorities + Career + equation) — KEEP on hub per D-03 (values stay inline)
- L25–L47: `## Work` + `### How I Evaluate You` + `### General Expectations` + `### Dev Expectations` + D1–D5 list — REMOVE (lives in work.md now), replace with link block
- L49–L63: `## Bdons Style` and sub-sections — REMOVE (lives in style.md now), replace with link block
- The Autonomy × ∫(Capability × Confidence) equation at L23 is currently in `$$...$$` LaTeX form — STACK.md flags this as unreliable on plain README. Convert to inline-code form: `Team Net Impact = Autonomy × ∫(Capability × Confidence)` (rendered in a fenced code block or backticks) so it renders reliably on GitHub.

Spoke tease content (use these to inform information-scent link blocks — keep verbatim or paraphrase tightly):
- work.md tease shape: "How I evaluate peeps, my four general principles, and specific role expectations."
- style.md tease shape: "How I give and receive feedback, communicate on Slack, and prefer to learn."
- playbooks.md tease shape: "How I run evaluations, delegate, structure 1:1s, and make decisions."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft and write the restructured hub content to readme.md</name>
  <files>readme.md</files>
  <read_first>
    - ./readme.md (current 637-word hub — voice + content to preserve from `## Values and Priorities` section, lines 1–23)
    - ./CLAUDE.md (Markdown flavor section: GFM target, no inline HTML, no LaTeX math blocks — convert equation to prose / inline-code; anchor conventions; lowercase-with-hyphens for spokes)
    - ./work.md (line 1 orientation tease — informs the work.md link-block tease wording)
    - ./style.md (line 1 orientation tease — informs the style.md link-block tease wording)
    - ./playbooks.md (line 1 orientation tease — informs the playbooks.md link-block tease wording)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-03: values stay inline on hub, no values.md spoke; D-06: voice rule — terse, deadpan, structured, no warmup, no inflation; protected markers include the equation)
  </read_first>
  <action>
Open `./readme.md` and replace its entire contents with the restructured one-page hub. Do NOT rename the file in this task — the filename stays `readme.md` (lowercase) until plan 03-02 handles the `git mv`. Use exactly this structure (substituting today's date 2026-05-14 in the last-reviewed line):

```markdown
# Bdon's Manager Readme

_Working philosophy, values, and management mechanics — written so you can get the gist in one page and drill in when you need to._

## Values and Priorities

### My Values summed up
Learning -> Freedom -> Adaptability -> Calm
* Learning and Teaching: my primary source of fun and mental health, whether at work or play
* Freedom: the purpose of money and health is the freedom to do what you want when you want
* Adaptability: I prefer apapting over resisting, and all my opinions are loosely held
* Calm, No Drama: I hate pointless stress, and filter processes and behaviours to maintain it

### My Priorities summed up
Family -> Health -> Security
* Family: my family can always interrupt my work
* Health: physical via daily habits, mental via learning and flexibility
* Security: The money and safety and predictability needed for everything

Work falls in health (achievement & learning) + security.

### My Career summed up
I make people more capable and confident, and teams more autonomous. Everything I do at work is through this lens. I'm more of a how guy than an ideas guy.

`Team Net Impact = Autonomy × ∫(Capability × Confidence)`

## Drill in

**[Work →](./work.md)**
_How I evaluate peeps, my four general principles, and specific role expectations._

**[Style →](./style.md)**
_How I give and receive feedback, communicate on Slack, and prefer to learn._

**[Playbooks →](./playbooks.md)**
_How I run evaluations, delegate, structure 1:1s, and make decisions._

**[Dev Expectations →](./work.md#dev-expectations)**
_What I expect at each level — Vidyard's ladder in my own language._
* [Coop - D1](./coop-d1.md) — first job, learning the trade
* [Junior - D2](./junior-d2.md) — owns tasks, asks good questions
* [Intermediate - D3](./intermediate-d3.md) — owns features, unblocks self
* [Senior - D4](./senior-d4.md) — owns systems, unblocks others
* [Staff - D5](./staff-d5.md) — owns outcomes, multiplies the team

---

_last reviewed: 2026-05-14_
```

Notes on the structure (do not include these as comments in the file — they are guidance for the executor):
- The Values / Priorities / Career content (lines 1–22 of the source, plus the Career paragraph at L18–L21) ports verbatim, preserving voice per D-06 and the "my values summed up / my priorities summed up / my career summed up" headings. This is per D-03 (values stay inline, no values.md spoke).
- The decorative LaTeX equation `$$\text{Team Net Impact} = \text{Autonomy} \cdot \int (\text{Capability} \cdot \text{Confidence})$$` from L23 converts to single-line inline-code form: `` `Team Net Impact = Autonomy × ∫(Capability × Confidence)` `` — uses Unicode `×` and `∫` which render reliably in GFM, sidesteps LaTeX unreliability flagged in CLAUDE.md and STATE.md blockers.
- The four information-scent link blocks all use the pattern: a single line `**[Title →](./file.md)**`, followed on the next line by `_one-sentence italic tease_`. The four blocks: Work, Style, Playbooks, Dev Expectations. Dev Expectations is the fourth block per the phase-specifics reconciliation (3 spokes + 1 D-level block = 4 total info-scent blocks).
- The D-level link list is nested under the Dev Expectations block as five bullets, each with a level tease (one short clause). Format: `* [Coop - D1](./coop-d1.md) — first job, learning the trade`.
- Last-reviewed line: a thematic break (`---`) followed by `_last reviewed: 2026-05-14_` in italics. The date is today's session date per Notes for the executor — 2026-05-14.
- All voice ports verbatim from the source. The hub orientation line under the H1 is a new sentence drafted to match Bdon's voice (terse, deadpan, no warmup). If the executor judges the orientation sentence too long or off-voice, it may be trimmed but must stay ≤25 words and must not introduce HR-coded language.
- Do not add an inline TOC (Out of Scope per REQUIREMENTS.md). GitHub renders a native TOC button.
- Do not add a `<details>`, `<div>`, `<table>`, or any HTML. CLAUDE.md prohibits inline HTML.
- The Dev Expectations link block points to `./work.md#dev-expectations` (anchor to the `### Dev Expectations` heading inside work.md, GitHub auto-generates the slug `dev-expectations`). This is the closest match to "the D-level block lives logically inside work.md" without duplicating the D1–D5 list across two files. The D1–D5 list also remains inline on the hub because HUB-05 requires "all five D1–D5 links are present" on the hub itself.

After writing the file, sanity-check by hand: count words (`wc -w < readme.md` must be ≤ 380), count lines (`wc -l < readme.md` must be ≤ 55), grep for the four bold-arrow link blocks (must be exactly 4), grep for all five D-level filenames (must each appear ≥ 1), grep for the last-reviewed line.
  </action>
  <verify>
    <automated>test "$(wc -w < readme.md | tr -d ' ')" -le 380 && test "$(wc -l < readme.md | tr -d ' ')" -le 55 && grep -q "Autonomy" readme.md && [ "$(grep -cE '^\*\*\[.*→\]\(\./.*\.md.*\)\*\*$' readme.md)" -eq 4 ] && grep -q "work.md" readme.md && grep -q "style.md" readme.md && grep -q "playbooks.md" readme.md && [ "$(grep -cE 'coop-d1|junior-d2|intermediate-d3|senior-d4|staff-d5' readme.md)" -ge 5 ] && grep -qE 'last reviewed: 20[0-9]{2}-[0-9]{2}-[0-9]{2}' readme.md && ! grep -qE '<(div|details|table|svg|a name)' readme.md && ! grep -qE '\$\$' readme.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f readme.md` — file still exists (filename unchanged, lowercase)
    - `wc -w < readme.md` returns a value ≤ 380 (HUB-02 word budget)
    - `wc -l < readme.md` returns a value ≤ 55 (HUB-02 line budget)
    - `grep -c "Autonomy" readme.md` returns ≥ 1 (HUB-03 equation present inline)
    - `grep -c "Capability" readme.md` returns ≥ 1 (HUB-03 equation present inline)
    - `grep -c "Confidence" readme.md` returns ≥ 1 (HUB-03 equation present inline)
    - `grep -c "make people more capable and confident" readme.md` returns ≥ 1 (HUB-03 identity statement inline; verbatim port from current L19)
    - `grep -cE '^\*\*\[.*→\]\(\./.*\.md.*\)\*\*$' readme.md` returns exactly 4 (HUB-04 — four bold-arrow link block headers, title + arrow + relative md link)
    - The line immediately following each `**[... →](./...)**` line begins with `_` and ends with `_` (HUB-04 italic-tease pattern; verify with: `awk '/^\*\*\[.*→\].*\*\*$/{getline next_line; if (next_line !~ /^_.*_$/) { print "FAIL: tease missing for: " $0; exit 1 }} END{print "OK"}' readme.md`)
    - `grep -c "./work.md" readme.md` returns ≥ 1 (HUB-05 — work.md link present)
    - `grep -c "./style.md" readme.md` returns ≥ 1 (HUB-05 — style.md link present)
    - `grep -c "./playbooks.md" readme.md` returns ≥ 1 (HUB-05 — playbooks.md link present)
    - `grep -c "./coop-d1.md" readme.md` returns ≥ 1 (HUB-05 — D1 link present)
    - `grep -c "./junior-d2.md" readme.md` returns ≥ 1 (HUB-05 — D2 link present)
    - `grep -c "./intermediate-d3.md" readme.md` returns ≥ 1 (HUB-05 — D3 link present)
    - `grep -c "./senior-d4.md" readme.md` returns ≥ 1 (HUB-05 — D4 link present)
    - `grep -c "./staff-d5.md" readme.md` returns ≥ 1 (HUB-05 — D5 link present)
    - `grep -E 'last reviewed: 20[0-9]{2}-[0-9]{2}-[0-9]{2}' readme.md` returns a match (HUB-06 — last-reviewed line present with ISO date)
    - `grep -c "last reviewed: 2026-05-14" readme.md` returns 1 (HUB-06 — date is today's session date)
    - `grep -cE '<(div|details|table|svg|a name)' readme.md` returns 0 (CLAUDE.md — no inline HTML)
    - `grep -cE '\$\$' readme.md` returns 0 (CLAUDE.md / STATE.md blocker — LaTeX math block converted to prose/inline-code form)
    - `grep -c "values.md" readme.md` returns 0 (D-03 — no values.md spoke; values content lives inline)
    - `grep -c "## Work" readme.md` returns 0 (Work content lives in work.md now; hub only has link block)
    - `grep -c "## Bdons Style" readme.md` returns 0 (Style content lives in style.md now; hub only has link block)
  </acceptance_criteria>
  <done>
    `readme.md` is fully restructured to the new hub layout: identity + values + priorities + career + equation inline, four bold-arrow link blocks with italic teases (Work / Style / Playbooks / Dev Expectations), five D1–D5 links with level teases nested under the Dev Expectations block, and a `_last reviewed: 2026-05-14_` line at the bottom. Word count ≤ 380, line count ≤ 55. No LaTeX math blocks, no inline HTML, no values.md references. Filename remains `readme.md` (lowercase) — plan 03-02 handles the rename.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → GitHub renderer | Markdown source becomes HTML rendered by GitHub. Sanitizer scope limits attack surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | readme.md content | accept | Markdown-only repo, no runtime, no secrets. Bdon-owned voice content; commit history is the audit trail. |
| T-03-02 | Information Disclosure | Relative link paths in link blocks | mitigate | All links use `./filename.md` form, no absolute file:// or path-traversal patterns. Acceptance criteria pin exact relative-path patterns; verify command rejects HTML that could obscure path content. |
| T-03-03 | Elevation of Privilege | Inline HTML / SVG / JS injection via markdown | mitigate | Acceptance criteria explicitly grep -c for `<div|details|table|svg|a name` returning 0. CLAUDE.md no-inline-HTML rule enforced. GFM sanitizer is the platform-level second line. |
| T-03-04 | Repudiation | Last-reviewed date provenance | accept | Date is a single italic line; staleness is the signal, not authenticity. Commit history provides actual provenance. N/A for a markdown-only artifact. |
</threat_model>

<verification>
After the task completes, the full automated check from `<verify>` should pass. Manual eyeball check (Bdon's call, not enforced by this plan): open the rendered `readme.md` on GitHub in a 1080p browser at default zoom and confirm the entire hub fits without scrolling. If it overflows, the line/word budget got too generous and the executor should re-trim — but the automated 380w/55L check is the binding gate.
</verification>

<success_criteria>
- `readme.md` is restructured to the one-page hub layout per the action's specified structure.
- `wc -w < readme.md` ≤ 380 (HUB-02).
- `wc -l < readme.md` ≤ 55 (HUB-02).
- Identity sentence (`make people more capable and confident...`) and equation (`Autonomy × ∫(Capability × Confidence)`) appear inline (HUB-03).
- Exactly four bold-arrow link blocks, each followed by an italic-tease line (HUB-04).
- All three spoke files and all five D-level files are linked from the hub (HUB-05).
- A `_last reviewed: 2026-05-14_` line appears (HUB-06).
- File is committed (single atomic commit `docs(03-01): restructure readme.md into one-page hub`).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hub-restructure-and-rename/03-01-SUMMARY.md` capturing:
- Final word and line counts.
- The four link block teases as written.
- The exact equation form used (the inline-code path vs prose path).
- Note for plan 03-02: filename is still `readme.md`; spoke back-links still target `./readme.md` and will be updated in 03-02's rename commit.
</output>
</content>
</invoke>