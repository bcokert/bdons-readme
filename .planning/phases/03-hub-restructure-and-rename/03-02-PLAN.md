---
phase: 03-hub-restructure-and-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - README.md
  - work.md
  - style.md
  - playbooks.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - HUB-01
tags:
  - markdown
  - rename
  - requirements

must_haves:
  truths:
    - "GitHub repo landing page renders the hub automatically (file is named README.md uppercase at the root)"
    - "Every spoke file's back-link resolves to the renamed README.md (no broken back-links)"
    - "REQUIREMENTS.md reflects HUB-01..HUB-06 as implemented (consistent with Phase 02 plan 02-01's check-off pattern)"
    - "Git history for the hub file is preserved through the rename (git mv, not delete + create)"
  artifacts:
    - path: "README.md"
      provides: "Uppercase hub file at repo root — GitHub landing page"
      contains: "Autonomy"
    - path: "work.md"
      provides: "Spoke back-link updated to ./README.md"
      contains: "[Back to readme](./README.md)"
    - path: "style.md"
      provides: "Spoke back-link updated to ./README.md"
      contains: "[Back to readme](./README.md)"
    - path: "playbooks.md"
      provides: "Spoke back-link updated to ./README.md"
      contains: "[Back to readme](./README.md)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HUB-01..HUB-06 checklist items marked complete; Traceability table updated"
      contains: "- [x] **HUB-01**"
  key_links:
    - from: "GitHub repo root"
      to: "README.md"
      via: "GitHub auto-renders uppercase README.md on the landing page"
      pattern: '^README\.md$'
    - from: "work.md / style.md / playbooks.md (back-link footer)"
      to: "./README.md"
      via: "← [Back to readme](./README.md) line at file bottom"
      pattern: '\[Back to readme\]\(\./README\.md\)'
---

<objective>
Rename `readme.md` → `README.md` via `git mv` (history-preserving), update the three spoke files' back-links to point to the new uppercase filename, and check off HUB-01..HUB-06 in `.planning/REQUIREMENTS.md`. This is the final structural move that lands the Phase 3 deliverable: the hub renders as the GitHub repo landing page.

Purpose: Close HUB-01 (the rename itself) and ensure no broken back-links result from the rename. The spoke files currently say `./readme.md` (lowercase) per D-05/D-36 from Phase 2 — they need updating now that the hub exists at the uppercase path. Also brings REQUIREMENTS.md in sync with the actual state of HUB-01..HUB-06 so Phase 4 verifiers see correct status.

Output: `README.md` at the repo root (history preserved); three spoke files with updated back-links; REQUIREMENTS.md with HUB-01..HUB-06 marked `[x]` and the Traceability table updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@.planning/phases/03-hub-restructure-and-rename/03-01-PLAN.md

<interfaces>
<!-- Files affected by the rename. Extracted so executor knows exact back-link strings. -->

Current back-link line in each spoke (from Phase 2 D-36 — verbatim, lowercase):
```
← [Back to readme](./readme.md)
```

Target back-link line after this plan runs:
```
← [Back to readme](./README.md)
```

REQUIREMENTS.md checklist items to flip from `[ ]` to `[x]` (from REQUIREMENTS.md L10–L15):
```
- [ ] **HUB-01**: `readme.md` renamed to `README.md` (GitHub platform convention)
- [ ] **HUB-02**: Hub fits on one screen — ≤380 words / ≤55 rendered lines on default GitHub render
- [ ] **HUB-03**: Hub opens with identity content + the Autonomy × ∫(Capability × Confidence) equation inline (orientation data, not a link)
- [ ] **HUB-04**: Hub uses information-scent link blocks for each spoke: bold title + one-line tease naming the specific questions that spoke answers (no title-only links)
- [ ] **HUB-05**: Hub links to all 3 spokes (`work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block
- [ ] **HUB-06**: Hub has a "last reviewed" date so staleness is visible
```

REQUIREMENTS.md Traceability table rows to flip from `Pending` to `Complete` (REQUIREMENTS.md L73–L78):
```
| HUB-01 | Phase 3 | Pending |
| HUB-02 | Phase 3 | Pending |
| HUB-03 | Phase 3 | Pending |
| HUB-04 | Phase 3 | Pending |
| HUB-05 | Phase 3 | Pending |
| HUB-06 | Phase 3 | Pending |
```

Phase 2 plan 02-01 set this pattern (REQUIREMENTS.md patch in same phase): consult `.planning/phases/02-spoke-files/02-01-SUMMARY.md` if a precedent example is helpful, but the action below specifies the exact edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv readme.md → README.md and update spoke back-links</name>
  <files>README.md, work.md, style.md, playbooks.md</files>
  <read_first>
    - ./readme.md (the file being renamed — confirm it still exists at lowercase path before the rename; this is the post-03-01 restructured hub)
    - ./CLAUDE.md (File Naming section: `README.md` uppercase is the GitHub convention for the hub; lowercase-with-hyphens for spokes — back-link target case must match the new uppercase filename exactly because case matters on Linux/macOS hosts)
    - ./work.md (current back-link line at bottom: `← [Back to readme](./readme.md)`)
    - ./style.md (current back-link line at bottom: `← [Back to readme](./readme.md)`)
    - ./playbooks.md (current back-link line at bottom: `← [Back to readme](./readme.md)`)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-05: lowercase during Phase 2, rename in Phase 3; D-36: back-link target pattern `./readme.md` updates to `./README.md` in Phase 3's rename commit)
  </read_first>
  <action>
Execute three changes in this exact order:

1. **Rename via `git mv` (preserves history):**
   ```
   git mv readme.md README.md
   ```
   Use `git mv` (not `mv` + `git add`) so git records the rename as a single rename operation and history is preserved. After the rename, verify with `git status` that the change is a rename, not a delete + add. Do NOT modify the file contents during this step — `README.md` must contain exactly what `readme.md` contained at the end of plan 03-01.

2. **Update back-link in each spoke file** — use the Edit tool to change the back-link line in `work.md`, `style.md`, and `playbooks.md`. Exact substitution (one line per file, at or near the file's last non-empty line):

   From:
   ```
   ← [Back to readme](./readme.md)
   ```

   To:
   ```
   ← [Back to readme](./README.md)
   ```

   The visible text "Back to readme" stays lowercase (it's prose, not a filename); only the link target `./readme.md` becomes `./README.md`. Capitalization matters — case must match the renamed file exactly.

3. **Do NOT modify REQUIREMENTS.md in this task** — that happens in Task 2. Keep this commit focused on the file-system rename + back-link updates so it's independently revertable.

After all three substitutions, the working tree should show:
- `R readme.md -> README.md` (rename)
- `M work.md`
- `M style.md`
- `M playbooks.md`

Commit this task's changes atomically with message:
```
docs(03-02): rename readme.md to README.md and update spoke back-links
```
  </action>
  <verify>
    <automated>[ "$(git ls-files | grep -cx 'README.md')" -eq 1 ] && [ "$(git ls-files | grep -cx 'readme.md')" -eq 0 ] && grep -q '\[Back to readme\](\./README\.md)' work.md && grep -q '\[Back to readme\](\./README\.md)' style.md && grep -q '\[Back to readme\](\./README\.md)' playbooks.md && ! grep -q '\[Back to readme\](\./readme\.md)' work.md && ! grep -q '\[Back to readme\](\./readme\.md)' style.md && ! grep -q '\[Back to readme\](\./readme\.md)' playbooks.md && grep -q "Autonomy" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | grep -x README.md` returns exactly 1 line (HUB-01 — uppercase README.md is tracked)
    - `git ls-files | grep -x readme.md` returns exactly 0 lines (HUB-01 — lowercase is gone from the index)
    - `git log --diff-filter=R --name-status -1 -- README.md` shows `R` (rename) for the most recent commit on `README.md` (HUB-01 — history preserved via `git mv`, not delete+add)
    - `grep -c '\[Back to readme\](\./README\.md)' work.md` returns ≥ 1 (work.md back-link updated)
    - `grep -c '\[Back to readme\](\./README\.md)' style.md` returns ≥ 1 (style.md back-link updated)
    - `grep -c '\[Back to readme\](\./README\.md)' playbooks.md` returns ≥ 1 (playbooks.md back-link updated)
    - `grep -c '\[Back to readme\](\./readme\.md)' work.md` returns 0 (old lowercase back-link is gone)
    - `grep -c '\[Back to readme\](\./readme\.md)' style.md` returns 0 (old lowercase back-link is gone)
    - `grep -c '\[Back to readme\](\./readme\.md)' playbooks.md` returns 0 (old lowercase back-link is gone)
    - `grep -c "Autonomy" README.md` returns ≥ 1 (renamed file content is intact — equation still present from plan 03-01)
    - `wc -w < README.md` returns ≤ 380 (content carried over unchanged from 03-01)
    - `wc -l < README.md` returns ≤ 55 (content carried over unchanged from 03-01)
    - `test ! -e readme.md` (working tree no longer has the lowercase file)
  </acceptance_criteria>
  <done>
    `readme.md` is renamed to `README.md` via `git mv` with history preserved. All three spoke files have their back-link target updated from `./readme.md` to `./README.md` (the visible link text "Back to readme" stays lowercase — it's prose). A single atomic commit `docs(03-02): rename readme.md to README.md and update spoke back-links` records the rename + back-link updates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Check off HUB-01..HUB-06 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state of the Hub checklist lines 10–15 and the Traceability table rows for HUB-01..HUB-06 at lines 73–78; also the "Last updated:" line at the very bottom)
    - .planning/phases/02-spoke-files/02-01-SUMMARY.md (precedent for REQUIREMENTS.md doc-only patching as a separate atomic commit within a phase; Phase 02 plan 02-01 used the same pattern for SPOKE-01 and HUB-05)
  </read_first>
  <action>
Edit `.planning/REQUIREMENTS.md` to mark HUB-01..HUB-06 as implemented. Two edits in this file:

1. **Hub checklist (lines 10–15):** Change each `- [ ]` to `- [x]` for HUB-01 through HUB-06. The rest of each bullet's text stays exactly as written.

   Before:
   ```
   - [ ] **HUB-01**: `readme.md` renamed to `README.md` (GitHub platform convention)
   - [ ] **HUB-02**: Hub fits on one screen — ≤380 words / ≤55 rendered lines on default GitHub render
   - [ ] **HUB-03**: Hub opens with identity content + the Autonomy × ∫(Capability × Confidence) equation inline (orientation data, not a link)
   - [ ] **HUB-04**: Hub uses information-scent link blocks for each spoke: bold title + one-line tease naming the specific questions that spoke answers (no title-only links)
   - [ ] **HUB-05**: Hub links to all 3 spokes (`work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block
   - [ ] **HUB-06**: Hub has a "last reviewed" date so staleness is visible
   ```

   After:
   ```
   - [x] **HUB-01**: `readme.md` renamed to `README.md` (GitHub platform convention)
   - [x] **HUB-02**: Hub fits on one screen — ≤380 words / ≤55 rendered lines on default GitHub render
   - [x] **HUB-03**: Hub opens with identity content + the Autonomy × ∫(Capability × Confidence) equation inline (orientation data, not a link)
   - [x] **HUB-04**: Hub uses information-scent link blocks for each spoke: bold title + one-line tease naming the specific questions that spoke answers (no title-only links)
   - [x] **HUB-05**: Hub links to all 3 spokes (`work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block
   - [x] **HUB-06**: Hub has a "last reviewed" date so staleness is visible
   ```

2. **Traceability table (lines 73–78):** Change the Status column for HUB-01..HUB-06 from `Pending` to `Complete`.

   Before:
   ```
   | HUB-01 | Phase 3 | Pending |
   | HUB-02 | Phase 3 | Pending |
   | HUB-03 | Phase 3 | Pending |
   | HUB-04 | Phase 3 | Pending |
   | HUB-05 | Phase 3 | Pending |
   | HUB-06 | Phase 3 | Pending |
   ```

   After:
   ```
   | HUB-01 | Phase 3 | Complete |
   | HUB-02 | Phase 3 | Complete |
   | HUB-03 | Phase 3 | Complete |
   | HUB-04 | Phase 3 | Complete |
   | HUB-05 | Phase 3 | Complete |
   | HUB-06 | Phase 3 | Complete |
   ```

3. **Update the "Last updated:" trailing line at the bottom of the file** (currently `*Last updated: 2026-05-11 after roadmap creation (traceability populated)*`):

   Replace with:
   ```
   *Last updated: 2026-05-14 — HUB-01..HUB-06 marked complete after Phase 3 ship*
   ```

Do NOT touch any other content in REQUIREMENTS.md. Do NOT change SPOKE-* or QUAL-* lines. Do NOT alter the Coverage section or Out-of-Scope table.

Commit this task's changes atomically with message:
```
docs(03-02): mark HUB-01..HUB-06 complete in REQUIREMENTS.md
```
  </action>
  <verify>
    <automated>[ "$(grep -c '^- \[x\] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md)" -eq 6 ] && [ "$(grep -c '^- \[ \] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md)" -eq 0 ] && [ "$(grep -cE '^\| HUB-0[1-6] \| Phase 3 \| Complete \|' .planning/REQUIREMENTS.md)" -eq 6 ] && [ "$(grep -cE '^\| HUB-0[1-6] \| Phase 3 \| Pending \|' .planning/REQUIREMENTS.md)" -eq 0 ] && grep -q 'Last updated: 2026-05-14' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md` returns exactly 6 (all six HUB checklist items now `[x]`)
    - `grep -c '^- \[ \] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md` returns exactly 0 (no HUB checklist items still unchecked)
    - `grep -cE '^\| HUB-0[1-6] \| Phase 3 \| Complete \|' .planning/REQUIREMENTS.md` returns exactly 6 (all six HUB Traceability rows say Complete)
    - `grep -cE '^\| HUB-0[1-6] \| Phase 3 \| Pending \|' .planning/REQUIREMENTS.md` returns exactly 0 (no HUB Traceability rows still Pending)
    - `grep -c 'Last updated: 2026-05-14' .planning/REQUIREMENTS.md` returns ≥ 1 (Last-updated line bumped to today)
    - `grep -c '^- \[ \] \*\*SPOKE-' .planning/REQUIREMENTS.md` returns 5 (SPOKE-01..SPOKE-05 unchanged — Phase 2 already shipped, those will be marked elsewhere; this plan must not touch them)
    - `grep -c '^- \[ \] \*\*QUAL-' .planning/REQUIREMENTS.md` returns 3 (QUAL-01..QUAL-03 still unchecked — Phase 4 territory; this plan must not touch them)
    - `grep -c '^- \[ \] \*\*FIX-0[12]\*\*' .planning/REQUIREMENTS.md` returns 2 (FIX-01 and FIX-02 still as-is — Phase 1 territory, separate concern; this plan must not touch them)
    - `git diff --name-only HEAD~1 HEAD -- .planning/REQUIREMENTS.md` (after commit) shows the file as modified (committed cleanly)
  </acceptance_criteria>
  <done>
    `.planning/REQUIREMENTS.md` has HUB-01..HUB-06 marked `[x]` in the Hub checklist and `Complete` in the Traceability table. The "Last updated:" footer line is bumped to 2026-05-14. No other requirements (SPOKE-*, FIX-*, QUAL-*) are touched. Atomic commit `docs(03-02): mark HUB-01..HUB-06 complete in REQUIREMENTS.md` records the change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → git index → GitHub | Renamed file lands in the repo and GitHub auto-renders the uppercase README.md on the landing page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Hub content during rename | mitigate | Use `git mv` (atomic rename, no content edit) so plan 03-01's restructured content carries over byte-for-byte. Acceptance criteria verify content invariants (`grep -q "Autonomy" README.md`, word/line counts ≤ 380/55) post-rename. |
| T-03-06 | Information Disclosure | Spoke back-link paths | mitigate | All updated back-links use `./README.md` form (relative, no absolute paths). Acceptance criteria pin the exact relative-path string. |
| T-03-07 | Repudiation | Git history through the rename | mitigate | `git mv` preserves rename detection; acceptance criterion explicitly checks `git log --diff-filter=R` finds the rename in the latest commit. Without this, history would show delete+add and blame would break. |
| T-03-08 | Denial of Service | Broken back-links on the rendered repo | mitigate | All three spoke back-links are updated in the same plan as the rename, so there is never a window where a spoke's back-link points at a missing file. Phase 4 QUAL-01 will re-verify link integrity. |
</threat_model>

<verification>
After both tasks complete, the repo state should be:
- `README.md` exists at the repo root, content unchanged from end-of-03-01 (verifiable: word and line budgets still satisfied).
- `readme.md` (lowercase) does not exist (verifiable: `test ! -e readme.md`).
- `git log --diff-filter=R --name-status` shows the rename in the most recent commit history.
- Three spoke files have updated back-links (verifiable: `grep -q '\[Back to readme\](\./README\.md)'` succeeds on each).
- REQUIREMENTS.md has six `[x]` HUB items and six `Complete` HUB Traceability rows.
- Two atomic commits land in this plan, in order: rename commit, then REQUIREMENTS.md commit.
</verification>

<success_criteria>
- HUB-01 satisfied: file is `README.md` (uppercase) at the repo root, history preserved via `git mv`.
- Spoke back-links resolve to the new uppercase filename (no broken back-links — Phase 4 QUAL-01 will catch any drift).
- REQUIREMENTS.md HUB-01..HUB-06 are all `[x]` and Traceability shows `Complete` for each.
- Two atomic commits: `docs(03-02): rename readme.md to README.md and update spoke back-links` and `docs(03-02): mark HUB-01..HUB-06 complete in REQUIREMENTS.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hub-restructure-and-rename/03-02-SUMMARY.md` capturing:
- The two commit SHAs (rename + requirements check-off).
- Confirmation that `git log --diff-filter=R` records the rename (history preserved).
- Note for Phase 4 QUAL-01: spoke back-links now point to `./README.md` — verify these resolve.
- Note for Phase 4 entry: HUB-01..HUB-06 are marked Complete in REQUIREMENTS.md; SPOKE-01..SPOKE-05 remain Pending pending a separate doc-only pass (or Phase 4 entry); QUAL-01..QUAL-03 remain Pending awaiting Phase 4 work.
</output>
</content>
</invoke>