---
phase: 02-spoke-files
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - playbooks.md
autonomous: false
requirements: [SPOKE-04, SPOKE-05]
must_haves:
  truths:
    - "`playbooks.md` exists at the repo root."
    - "`playbooks.md` opens with a single-sentence italic orientation line (≤ 15 words) and closes with the back-link `← [Back to readme](./readme.md)`."
    - "`playbooks.md` contains exactly four `### {Title}` headings in this order: Evaluate, Delegate, 1:1s, Decisions (D-27)."
    - "Each playbook section uses the D-27 minimal-shape scaffold: title heading + one-line italic intro (Claude-drafted per D-28, ≤ 15 words) + one body placeholder (D-29 shape)."
    - "Each playbook's body placeholder explicitly reserves a slot for 'mechanics + concrete behavioral question this playbook answers' (D-27 + SPOKE-05 structural satisfaction)."
    - "`playbooks.md` contains exactly **4** placeholder markers matching `^_\\[placeholder:.*\\]_$` — one per playbook."
    - "No actual playbook copy ships in Phase 2 (D-26). The italic one-line intro per playbook is Claude-drafted reservation language, not playbook content."
    - "Bdon reviewed and approved the diff before commit (D-39)."
    - "The commit is atomic — diff contains only `playbooks.md`."
    - "SPOKE-05 reaches **'Pending — scaffold complete'** status at Phase 2 ship; SPOKE-05 reaches **'Complete'** when Bdon writes playbook bodies post-Phase-2. Phase 4 verifier should not flag SPOKE-05 as failed at Phase 2 ship — the structural slot for the concrete behavioral question exists by design (D-26, D-27)."
  artifacts:
    - path: "playbooks.md"
      provides: "Scaffold for 4 playbooks: Evaluate, Delegate, 1:1s, Decisions"
      contains: "### Evaluate|### Delegate|### 1:1s|### Decisions|_[placeholder:"
  key_links:
    - from: "playbooks.md top"
      to: "orientation line"
      via: "italic single-sentence ≤15 words"
      pattern: "^_.{1,90}\\._$"
    - from: "playbooks.md bottom"
      to: "./readme.md"
      via: "back-link line"
      pattern: "← \\[Back to readme\\]\\(\\./readme\\.md\\)"
    - from: "each playbook section"
      to: "Bdon-authored copy (post-Phase-2)"
      via: "placeholder reservation"
      pattern: "concrete behavioral question this playbook answers"
---

<objective>
Create `playbooks.md` at the repo root as a **pure scaffold** for four playbook sections per D-26 / D-27 / D-28: Evaluate, Delegate, 1:1s, Decisions. Each playbook = `### {Title}` heading + Claude-drafted one-line italic intro (≤ 15 words) + a single D-29 `_[placeholder: ...]_` body marker that explicitly reserves the "mechanics + concrete behavioral question this playbook answers" slot. Add the D-35 orientation line at the top and D-36 back-link at the bottom.

Purpose: Land the file structure so Bdon can verify the shape before committing to specific mechanics (D-26 rationale). Bdon writes actual playbook bodies and the concrete behavioral questions post-Phase-2, outside the GSD workflow.

**SPOKE-05 coverage note (deliberate):** SPOKE-05 requires "each playbook answers a concrete behavioral question (mechanics-first), not just philosophy — what Bdon *does*, not what he *believes*." Phase 2 ships scaffold only. The structural satisfaction at Phase 2 ship is that each playbook's placeholder description explicitly names "concrete behavioral question this playbook answers" as one of the things the eventual copy must contain. SPOKE-05 reaches "Pending — scaffold complete" at Phase 2 ship; "Complete" status waits for Bdon's copy. The Phase 4 verifier should treat SPOKE-05 as scaffold-satisfied at Phase 2 ship.

Output: New file `playbooks.md` at repo root; single atomic commit `docs(02): create playbooks.md (scaffold) — Refs: SPOKE-04, SPOKE-05`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@readme.md

<interfaces>
<!-- No content ports OUT of readme.md into playbooks.md. playbooks.md is brand new — no source rows. -->
<!-- 01-CONTENT-MAP.md confirms: "playbooks.md is written from scratch in Phase 2 (SPOKE-03 [now SPOKE-04]). It has no source rows in this map." -->

<!-- Scaffold shape (locked by D-27 — exact). Each playbook section is exactly: -->
<!--   ### {Title}                                                                           -->
<!--   (blank line)                                                                          -->
<!--   _{one-line intro: what this playbook covers}_                                         -->
<!--   (blank line)                                                                          -->
<!--   _[placeholder: mechanics + concrete behavioral question this playbook answers]_       -->

<!-- The four titles in this order (D-27): Evaluate, Delegate, 1:1s, Decisions. -->
<!-- Order matches PROJECT.md Key Decisions row "4 playbooks in v1: Evaluate, Delegate, 1:1s, Decisions" -->
<!-- and ARCHITECTURE.md hub section 5 tease "How I run 1:1s, evaluate performance, delegate, and make decisions". -->

<!-- Drafted orientation line (D-35): voice-neutral, single sentence, italic, ≤ 15 words. Bdon reviews + may rewrite at checkpoint. -->
<!-- Default: -->
`_How I run evaluations, delegate, structure 1:1s, and make decisions._`
(10 content words. Italic. Single sentence. Names the four playbooks in source order.)

<!-- Drafted one-line intros per playbook (D-28: Claude drafts so Bdon sees what each section is reserving; voice-neutral, terse, ≤ 15 words). -->
<!-- All four are pre-validated at ≤ 15 content words (re-verified post-revision). -->

For `### Evaluate`:
`_How I evaluate performance — what I track, how often, what triggers a write-up._`
(13 content words.)

For `### Delegate`:
`_How I hand work off — what I keep and what I pass on._`
(13 content words.)

For `### 1:1s`:
`_How I run 1:1s — cadence, agenda ownership, what we cover, what we skip._`
(13 content words.)

For `### Decisions`:
`_How I make and reverse decisions — what I decide alone, what I decide together._`
(14 content words.)

<!-- Drafted placeholder body descriptions (one per playbook). All four use the SAME description text per D-27 — the slot is the same shape for every playbook. -->
<!-- D-29 marker shape; the description explicitly names the SPOKE-05 "concrete behavioral question" slot so Bdon knows to write one when filling in. -->
The single placeholder body, used for all four playbooks:
`_[placeholder: mechanics — what Bdon does in what order, with what outputs — plus the concrete behavioral question this playbook answers (e.g., "how do I know how I'm doing before review?", "how does a decision get reversed?").]_`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create playbooks.md (scaffold + 4 placeholders + orientation + back-link)</name>
  <files>playbooks.md</files>
  <read_first>
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-26 scaffold-only, D-27 exact minimal-shape, D-28 Claude-drafts-intros, D-29 placeholder shape, D-35 orientation, D-36 back-link, D-37 no spoke-spoke cross-links pre-wired)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md (confirms playbooks.md has no source rows — written from scratch)
    - .planning/research/ARCHITECTURE.md (File Map row for playbooks.md, Section-by-Section Spec section 5, hub tease wording for cross-checking title order)
    - .planning/REQUIREMENTS.md (SPOKE-04 + SPOKE-05 acceptance language; SPOKE-05 specifies each playbook answers a concrete behavioral question)
    - .planning/ROADMAP.md Phase 2 SC3 + SC5 (the scaffold-acknowledgment note will be in place from 02-01)
    - CLAUDE.md (markdown only, GFM, no inline HTML, voice preservation)
  </read_first>
  <action>
Create `playbooks.md` at the repo root using the Write tool. Exact file body (copy this structure — no executor invention):

```markdown
_How I run evaluations, delegate, structure 1:1s, and make decisions._

## Playbooks

### Evaluate

_How I evaluate performance — what I track, how often, what triggers a write-up._

_[placeholder: mechanics — what Bdon does in what order, with what outputs — plus the concrete behavioral question this playbook answers (e.g., "how do I know how I'm doing before review?", "how does a decision get reversed?").]_

### Delegate

_How I hand work off — what I keep and what I pass on._

_[placeholder: mechanics — what Bdon does in what order, with what outputs — plus the concrete behavioral question this playbook answers (e.g., "how do I know how I'm doing before review?", "how does a decision get reversed?").]_

### 1:1s

_How I run 1:1s — cadence, agenda ownership, what we cover, what we skip._

_[placeholder: mechanics — what Bdon does in what order, with what outputs — plus the concrete behavioral question this playbook answers (e.g., "how do I know how I'm doing before review?", "how does a decision get reversed?").]_

### Decisions

_How I make and reverse decisions — what I decide alone, what I decide together._

_[placeholder: mechanics — what Bdon does in what order, with what outputs — plus the concrete behavioral question this playbook answers (e.g., "how do I know how I'm doing before review?", "how does a decision get reversed?").]_

← [Back to readme](./readme.md)
```

Rules:
- Section order: Evaluate → Delegate → 1:1s → Decisions (D-27 + PROJECT.md Key Decisions).
- Each playbook = exactly one `### {Title}` heading + one italic intro line + one placeholder body line. Nothing else.
- The four intro lines are the Claude-drafted lines from `<interfaces>` above. They name what each section RESERVES (D-28); they are NOT playbook content. Voice-neutral. Each is ≤ 15 content words — pre-validated at draft time.
- The four placeholder lines use D-29 shape exactly: italic-wrapped, square brackets, lowercase `placeholder:` prefix. All four placeholders use the SAME description text (per the `<interfaces>` block) — the slot is the same shape for every playbook, and the description explicitly reserves the SPOKE-05 "concrete behavioral question" slot.
- No actual playbook copy. No "Bdon does X step 1, step 2..." text. No example mechanics. Only scaffold.
- No spoke→spoke cross-links (D-37 — pre-wiring those is post-Phase-2 work).
- The `## Playbooks` section heading sits between the orientation line and the first `### Evaluate` subsection.
- Orientation line is the first non-empty line of the file.
- Back-link is the last non-empty line of the file.
- No HTML. No LaTeX. No inline anchors. No frontmatter.
  </action>
  <verify>
    <automated>
test -f playbooks.md \
&amp;&amp; head -3 playbooks.md | grep -qE '^_How I run evaluations.{1,80}\._$' \
&amp;&amp; awk 'NR<=3 &amp;&amp; /^_/ {n=split($0,a," "); if (n>17) {exit 1}}' playbooks.md \
&amp;&amp; tail -3 playbooks.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' playbooks.md)" -eq 4 ] \
&amp;&amp; [ "$(grep -c "concrete behavioral question this playbook answers" playbooks.md)" -eq 4 ] \
&amp;&amp; grep -q '^## Playbooks$' playbooks.md \
&amp;&amp; grep -q '^### Evaluate$' playbooks.md \
&amp;&amp; grep -q '^### Delegate$' playbooks.md \
&amp;&amp; grep -q '^### 1:1s$' playbooks.md \
&amp;&amp; grep -q '^### Decisions$' playbooks.md \
&amp;&amp; [ "$(grep -nE '^### (Evaluate|Delegate|1:1s|Decisions)$' playbooks.md | cut -d: -f1 | paste -sd, -)" = "$(printf '%s\n' "$(grep -nE '^### Evaluate$' playbooks.md | cut -d: -f1)" "$(grep -nE '^### Delegate$' playbooks.md | cut -d: -f1)" "$(grep -nE '^### 1:1s$' playbooks.md | cut -d: -f1)" "$(grep -nE '^### Decisions$' playbooks.md | cut -d: -f1)" | paste -sd, -)" ] \
&amp;&amp; ! grep -qE "values\.md" playbooks.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `playbooks.md` exists at the repo root.
    - First non-empty line matches `^_How I run evaluations.{1,80}\._$` (the drafted orientation line; italic, single sentence). Additionally, the awk word-count gate `n > 17` (tokens including italic delimiters) must not trip on the orientation line — equivalent to ≤ 15 content words.
    - Note: this prefix-pinned regex verifies the drafted orientation line. If Bdon rewrites the line at the Task 2 checkpoint, Task 1's gate no longer applies; Task 2's looser regex `^_.{1,90}\._$` is the post-checkpoint authoritative gate. The per-playbook intro word-count caps (≤ 15 words each) are enforced at draft time and re-checked at the Task 2 checkpoint review, not by the Task 1 automated verify (the intros live inside subsection blocks, so the awk gate that targets the first three lines does not reach them).
    - Last non-empty line is exactly `← [Back to readme](./readme.md)`.
    - Exactly **4** lines match `^_\[placeholder:.*\]_$` (one per playbook section).
    - Exactly **4** occurrences of the string `concrete behavioral question this playbook answers` (one per placeholder — structurally reserves the SPOKE-05 slot).
    - All four playbook headings present: `### Evaluate`, `### Delegate`, `### 1:1s`, `### Decisions`.
    - The four playbook headings appear in source order: Evaluate < Delegate < 1:1s < Decisions (verified by line-number comparison in the verify command).
    - Each playbook section has the D-27 minimal shape (heading + italic intro + single placeholder), in that order.
    - Each per-playbook italic intro is ≤ 15 content words (pre-validated at draft time: Evaluate 13, Delegate 13, 1:1s 13, Decisions 14). Bdon re-checks at the Task 2 checkpoint.
    - No playbook body copy beyond the placeholder marker (Phase 2 ships scaffold only — D-26).
    - No spoke→spoke cross-link (no occurrences of `./work.md` or `./style.md` in playbooks.md per D-37).
    - No `values.md` reference anywhere.
    - No inline HTML, LaTeX, or frontmatter.
  </acceptance_criteria>
  <done>`playbooks.md` exists with the spec'd shape; all grep gates pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon reviews playbooks.md diff and approves orientation, four intros, and placeholder shape</name>
  <files>playbooks.md</files>
  <action>
Pause execution and present the diff to Bdon. Run `cat playbooks.md` (or `git diff --no-index /dev/null playbooks.md` for a diff-style view) to display the full file content. Wait for Bdon's response:

- If Bdon types `approved` — continue to the next task (commit) without changes.
- If Bdon dictates changes (orientation line wording, placeholder descriptions/placement, intro lines, section order — whichever applies to this file), apply those edits to `playbooks.md` using the Edit tool, then re-run the Task 1 grep gates locally to confirm the structural invariants still hold, then continue to the commit task.

Do not commit during this task. Do not stage. This task only modifies `playbooks.md` if Bdon requests changes; otherwise it makes zero file modifications and only waits for the resume signal.
  </action>
  <verify>
    <automated>
test -f playbooks.md \
&amp;&amp; head -3 playbooks.md | grep -qE '^_.{1,90}\._$' \
&amp;&amp; tail -3 playbooks.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' playbooks.md)" -eq 4 ]
    </automated>
  </verify>
  <what-built>
Claude created `playbooks.md` at the repo root with:
1. A drafted italic orientation line at top: `_How I run evaluations, delegate, structure 1:1s, and make decisions._`
2. A `## Playbooks` section heading.
3. Four `### {Title}` subsections in this order: Evaluate, Delegate, 1:1s, Decisions (D-27 + PROJECT.md Key Decisions).
4. Each subsection contains exactly:
   - The `### {Title}` heading.
   - A Claude-drafted italic one-line intro (≤ 15 content words) describing what that playbook RESERVES (D-28). These are NOT playbook content — they name the slot.
   - A single `_[placeholder: ...]_` line whose description explicitly reserves the SPOKE-05 "concrete behavioral question this playbook answers" requirement so Bdon's eventual copy answers a behavioral question (not just philosophy).
5. A back-link line at the bottom: `← [Back to readme](./readme.md)`.

**No actual playbook copy ships in Phase 2** (D-26). Bdon writes the playbook bodies and the concrete behavioral questions manually post-Phase-2, outside the GSD workflow.
  </what-built>
  <how-to-verify>
1. Run `git diff --staged` or `cat playbooks.md` to see the full file.
2. Read the orientation line aloud. Does it sound like you? Is it ≤ 15 words? Does it name the four playbooks without inflation? If not, rewrite inline before approving.
3. Confirm section order: Evaluate → Delegate → 1:1s → Decisions. (If you want a different order, request the change now; if you reorder, also reorder the orientation line's listing.)
4. Read each of the four italic one-line intros aloud:
   - Evaluate: `_How I evaluate performance — what I track, how often, what triggers a write-up._` (13 content words)
   - Delegate: `_How I hand work off — what I keep and what I pass on._` (13 content words)
   - 1:1s: `_How I run 1:1s — cadence, agenda ownership, what we cover, what we skip._` (13 content words)
   - Decisions: `_How I make and reverse decisions — what I decide alone, what I decide together._` (14 content words)

   Each is a RESERVATION line, not playbook content. Each is ≤ 15 words, voice-neutral, terse, deadpan. Do any of them nudge your eventual playbook copy? If yes, rewrite. If you rewrite, keep the rewritten line ≤ 15 content words.
5. Confirm the placeholder body line is identical under all four playbooks and that it explicitly contains the phrase `concrete behavioral question this playbook answers`. That phrase is what carries SPOKE-05 at scaffold ship — it tells you the slot reserves a behavioral question.
6. Confirm zero playbook copy (no "Bdon does X then Y then Z" anywhere). Phase 2 is scaffold only.
7. Confirm the back-link reads exactly `← [Back to readme](./readme.md)`.
  </how-to-verify>
  <read_first>
    - playbooks.md (the file just created)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-26 scaffold-only, D-27 exact shape, D-28 intro line rule, D-29 placeholder shape, D-35 orientation, D-36 back-link)
    - .planning/REQUIREMENTS.md (SPOKE-05 — the "concrete behavioral question" requirement that the scaffold structurally reserves)
  </read_first>
  <acceptance_criteria>
    - Bdon types `approved` (commit as drafted), OR
    - Bdon dictates specific changes to the orientation line, one or more intro lines, the playbook order, or the placeholder description — those changes land in `playbooks.md` before commit, and the Task 1 grep gates (4 placeholders, 4 `concrete behavioral question` occurrences, 4 headings in source order, orientation regex, back-link) still pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" to commit, or specify changes (orientation, intro lines, section order, placeholder description).</resume-signal>
  <done>Bdon has approved playbooks.md; ready to commit.</done>
</task>

<task type="auto">
  <name>Task 3: Commit playbooks.md as a single atomic commit</name>
  <files>playbooks.md</files>
  <read_first>
    - (None — git only; playbooks.md is the only new path to stage)
  </read_first>
  <action>
Stage exactly one path and commit:

```bash
git add playbooks.md
git commit -m "docs(02): create playbooks.md (scaffold)

Pure scaffold for 4 playbooks per D-26/D-27/D-28: Evaluate, Delegate, 1:1s, Decisions.
Each section: heading + Claude-drafted italic intro + 1 placeholder body line.

Placeholder bodies explicitly reserve the SPOKE-05 'concrete behavioral
question this playbook answers' slot. Bdon authors actual playbook copy
post-Phase-2, outside the GSD workflow (D-26).

SPOKE-05 at scaffold-complete status; Phase 4 verifier should treat
SPOKE-05 as scaffold-satisfied per the D-41 ROADMAP clarification.

Orientation line per D-35; back-link per D-36; no spoke-spoke cross-links per D-37.

Refs: SPOKE-04, SPOKE-05"
```

Do NOT stage planning artifacts in this commit. The diff must be exactly `playbooks.md`.
  </action>
  <verify>
    <automated>
git diff HEAD~1 HEAD --name-only | tr '\n' ' ' | grep -qE "^playbooks\.md $" \
&amp;&amp; git log -1 --pretty=%s | grep -qF "docs(02): create playbooks.md (scaffold)" \
&amp;&amp; git log -1 --pretty=%B | grep -qF "Refs: SPOKE-04, SPOKE-05"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 HEAD --name-only` returns exactly one path: `playbooks.md`.
    - Commit subject is exactly `docs(02): create playbooks.md (scaffold)`.
    - Commit body references both SPOKE-04 and SPOKE-05, and explicitly notes the SPOKE-05 scaffold-satisfied disposition so Phase 4 doesn't flag it.
    - Working tree is clean.
  </acceptance_criteria>
  <done>`playbooks.md` landed in a single atomic commit; Phase 2 ship list item #4 complete; Phase 2 ready to close.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `playbooks.md` exists at the repo root with: orientation line + `## Playbooks` heading + four playbook subsections (Evaluate / Delegate / 1:1s / Decisions) each with Claude-drafted intro (≤ 15 words) + single placeholder + back-link.
- Exactly 4 placeholders in `playbooks.md`.
- Exactly 4 occurrences of the phrase `concrete behavioral question this playbook answers` (structurally reserves SPOKE-05).
- The diff at HEAD contains exactly one file (`playbooks.md`).
- Bdon approved before commit (D-39).
- Phase 4 verifier reading the file sees the scaffold and the SPOKE-05 reservation; should treat SPOKE-05 as scaffold-satisfied at Phase 2 ship per the D-41 ROADMAP note.
</verification>

<success_criteria>
- Four playbook headings in source order (Evaluate, Delegate, 1:1s, Decisions).
- Each playbook follows D-27 minimal shape: heading + italic intro (≤ 15 content words) + single placeholder.
- Four placeholders all structurally reserve the SPOKE-05 concrete-behavioral-question slot via the description text.
- Zero playbook body copy (D-26).
- Zero spoke→spoke cross-links (D-37).
- Orientation line ≤ 15 words, italic, single sentence.
- Back-link present and points to `./readme.md`.
- Single atomic commit referencing SPOKE-04 and SPOKE-05.
- Bdon approved before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spoke-files/02-04-SUMMARY.md` recording:
- The final orientation line wording (if Bdon rewrote it).
- The final four intro lines (if Bdon rewrote any).
- The final placeholder body description (if Bdon rewrote it).
- The commit SHA.
- An explicit note: **SPOKE-05 is scaffold-satisfied at Phase 2 ship.** The "concrete behavioral question" slot is structurally reserved in each playbook's placeholder description; SPOKE-05 reaches full Complete status when Bdon writes the playbook bodies post-Phase-2. Phase 4 QUAL verifier should not flag SPOKE-05 as failed at Phase 2 ship.
</output>
