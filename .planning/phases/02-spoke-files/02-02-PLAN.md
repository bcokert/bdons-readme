---
phase: 02-spoke-files
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - work.md
autonomous: false
requirements: [SPOKE-02]
must_haves:
  truths:
    - "`work.md` exists at the repo root."
    - "`work.md` opens with a single-sentence italic orientation line (≤ 15 words) and closes with the back-link `← [Back to readme](./readme.md)`."
    - "`work.md` contains the ported `### How I Evaluate You` subsection (FIX-01 post-state) above `### General Expectations`."
    - "`work.md` contains all four operating-principle bullets ported verbatim from `readme.md` per 01-CONTENT-MAP.md: Test when Reversible, Work Out Loud with Intent, Prioritize Proactivity & Ownership, Treat AI as a Powertool."
    - "`work.md` contains the `### Dev Expectations` subsection with the Engineering Ladder paragraph, the lead-in line, and the D1–D5 link list ported verbatim from `readme.md` L37–L47."
    - "`work.md` contains exactly four placeholder body markers using the D-29 shape `_[placeholder: ...]_` — one per operating principle (D-31)."
    - "Each placeholder body description names the slot purpose in voice-neutral, terse language (≤ 2 sentences inside the brackets, per D-29 + D-06)."
    - "Bdon reviewed and approved the diff before commit (D-39)."
    - "The commit is atomic — diff contains only `work.md`."
  artifacts:
    - path: "work.md"
      provides: "Operating principles + Dev Expectations spoke file with port + scaffold for behavior expansion"
      contains: "### How I Evaluate You|### General Expectations|### Dev Expectations|_[placeholder:"
  key_links:
    - from: "work.md top"
      to: "orientation line"
      via: "italic single-sentence ≤15 words"
      pattern: "^_.{1,90}\\._$"
    - from: "work.md bottom"
      to: "./readme.md"
      via: "back-link line"
      pattern: "← \\[Back to readme\\]\\(\\./readme\\.md\\)"
    - from: "work.md Dev Expectations subsection"
      to: "coop-d1.md, junior-d2.md, intermediate-d3.md, senior-d4.md, staff-d5.md"
      via: "5 D-file links ported from readme.md L41-L45"
      pattern: "\\[Coop - D1\\]\\(\\./coop-d1\\.md\\)"
---

<objective>
Create `work.md` at the repo root: port the `## Work` section content from `readme.md` per 01-CONTENT-MAP.md (rows L25, L27, L29, L31, L33, L35, L37, L38, L40, L41-L45), add the D-35 orientation line at the top, the D-36 back-link at the bottom, and four D-29-shaped placeholder markers — one under each of the four operating principles per D-31.

Purpose: Land the first spoke file as port + scaffold. The "2–3 concrete behaviors" expansion under each principle is Bdon-authored post-Phase-2 (out of scope per D-30/D-31).

Output: New file `work.md` at repo root; single atomic commit `docs(02): create work.md (port + scaffold) — Refs: SPOKE-02`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md
@.planning/research/ARCHITECTURE.md
@CLAUDE.md
@readme.md

<interfaces>
<!-- Exact content to port — extracted from readme.md post-Phase-1 state. -->
<!-- Executor copies this verbatim; no rewording. -->

From readme.md L25-L47 (the `## Work` section):

```markdown
## Work

### How I Evaluate You

**Proactivity & Ownership.** This determines how much you're trusted to take on and thus how much impact you can create in your role.

### General Expectations

**Test when Reversible, Analyze Otherwise**: Don't analyze what can be more quickly learned by testing - if you can easily undo/retry a decision, decide asap.

**Work Out Loud with Intent, not Permission**: Create the best plan you can with what you know, share it out loud to enable feedback, and set a time limit for said feedback before before you take action.

**Treat AI as a Powertool**: AI replaces the hand saw, not the carpenter. Powertools build houses faster than hand tools if used right, but can't build good homes by themselves - going faster at sufficient quality is the goal, and learning how to balance these is critical.

### Dev Expectations
When I need a full framework, I lean on the [Engineering Ladder](https://www.engineeringladders.com/Developer.html#d4---developer-4) plus the [orgs specific definitions](https://docs.google.com/spreadsheets/d/1qLPYohreoSMk6EBaQdTvrlVihYU0dr9TwUKHdC-jVjc/edit?gid=1278440934#gid=1278440934).

I've integrated by own expectations with Vidyard's in my own language here:
* [Coop - D1](./coop-d1.md)
* [Junior - D2](./junior-d2.md)
* [Intermediate - D3](./intermediate-d3.md)
* [Senior - D4](./senior-d4.md)
* [Staff - D5](./staff-d5.md)
```

<!-- Placeholder marker shape (locked by D-29). Each placeholder lives on its own line, italic-wrapped, square brackets, lowercase "placeholder:" prefix. -->
<!-- Default placement: BELOW each principle bullet (D-Discretion default; Bdon may flip at checkpoint). -->

<!-- Drafted orientation line (D-35): voice-neutral, single sentence, italic, ≤ 15 words. Bdon reviews + may rewrite at checkpoint. -->
<!-- Default: -->
`_How I evaluate you, the four principles I expect you to operate by, and the dev ladder._`
(15 words. Italic. Single sentence. Names the three reservations of the file.)

<!-- Drafted placeholder body descriptions (one per principle). Voice-neutral, terse, ≤ 2 sentences inside brackets. Bdon reviews + may rewrite at checkpoint. -->
For Proactivity & Ownership (under "**Proactivity & Ownership.**" bullet inside `### How I Evaluate You`):
`_[placeholder: 2–3 concrete behaviors that show proactivity & ownership day-to-day. What failure looks like.]_`

For Test when Reversible (under the bullet):
`_[placeholder: 2–3 concrete behaviors — what test-when-reversible looks like in practice. What failure looks like.]_`

For Work Out Loud with Intent (under the bullet):
`_[placeholder: 2–3 concrete behaviors — what working out loud with intent looks like. What failure looks like.]_`

For Treat AI as a Powertool (under the bullet):
`_[placeholder: 2–3 concrete behaviors — what treating AI as a powertool looks like. What failure looks like.]_`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create work.md (port + 4 expansion placeholders + orientation + back-link)</name>
  <files>work.md</files>
  <read_first>
    - readme.md (current source — confirm post-Phase-1 state of `## Work` section L25-L47)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md (row-by-row destinations for L25, L27, L29, L31, L33, L35, L37, L38, L40, L41-L45)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-29 placeholder shape, D-30 work.md scope, D-31 four placeholder slots, D-32 evaluate-you ports as-is, D-35 orientation, D-36 back-link, D-06 voice rule)
    - .planning/research/ARCHITECTURE.md (Self-Contained Spoke pattern, File Map row for work.md, Length Budgets, Cross-Linking Strategy)
    - CLAUDE.md (markdown only, GFM, no inline HTML, voice preservation)
  </read_first>
  <action>
Create `work.md` at the repo root using the Write tool. Exact file body (copy this structure — do not invent wording):

```markdown
_How I evaluate you, the four principles I expect you to operate by, and the dev ladder._

## Work

### How I Evaluate You

**Proactivity & Ownership.** This determines how much you're trusted to take on and thus how much impact you can create in your role.

_[placeholder: 2–3 concrete behaviors that show proactivity & ownership day-to-day. What failure looks like.]_

### General Expectations

**Test when Reversible, Analyze Otherwise**: Don't analyze what can be more quickly learned by testing - if you can easily undo/retry a decision, decide asap.

_[placeholder: 2–3 concrete behaviors — what test-when-reversible looks like in practice. What failure looks like.]_

**Work Out Loud with Intent, not Permission**: Create the best plan you can with what you know, share it out loud to enable feedback, and set a time limit for said feedback before before you take action.

_[placeholder: 2–3 concrete behaviors — what working out loud with intent looks like. What failure looks like.]_

**Treat AI as a Powertool**: AI replaces the hand saw, not the carpenter. Powertools build houses faster than hand tools if used right, but can't build good homes by themselves - going faster at sufficient quality is the goal, and learning how to balance these is critical.

_[placeholder: 2–3 concrete behaviors — what treating AI as a powertool looks like. What failure looks like.]_

### Dev Expectations
When I need a full framework, I lean on the [Engineering Ladder](https://www.engineeringladders.com/Developer.html#d4---developer-4) plus the [orgs specific definitions](https://docs.google.com/spreadsheets/d/1qLPYohreoSMk6EBaQdTvrlVihYU0dr9TwUKHdC-jVjc/edit?gid=1278440934#gid=1278440934).

I've integrated by own expectations with Vidyard's in my own language here:
* [Coop - D1](./coop-d1.md)
* [Junior - D2](./junior-d2.md)
* [Intermediate - D3](./intermediate-d3.md)
* [Senior - D4](./senior-d4.md)
* [Staff - D5](./staff-d5.md)

← [Back to readme](./readme.md)
```

Rules:
- Verbatim port: every body bullet text, every link href, every typo (e.g., "before before you take action", "by own expectations") is preserved. The voice rule (D-06) protects Bdon's wording — do not silently fix typos. Phase 4 QUAL-02 may surface them later if needed.
- Section headings (`## Work`, `### How I Evaluate You`, `### General Expectations`, `### Dev Expectations`) port unchanged.
- Position of `### How I Evaluate You` is above `### General Expectations` (FIX-01 post-state).
- The four placeholder lines use D-29 shape exactly: italic-wrapped, square brackets, lowercase `placeholder:` prefix. Each on its own line.
- The four placeholders sit BELOW their respective principle bullets (D-Discretion default — Bdon may request a flip at checkpoint).
- Orientation line is the first non-empty line of the file (above `## Work`).
- Back-link is the last non-empty line of the file.
- No HTML. No LaTeX. No inline anchors. No frontmatter.
- The Engineering Ladder external URL and Vidyard Google Sheet URL port unchanged (D-Discretion default: keep as-is in the paragraph).
  </action>
  <verify>
    <automated>
test -f work.md \
&amp;&amp; head -3 work.md | grep -qE '^_How I evaluate you.{1,80}\._$' \
&amp;&amp; tail -3 work.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' work.md)" -eq 4 ] \
&amp;&amp; grep -q '^### How I Evaluate You$' work.md \
&amp;&amp; grep -q '^### General Expectations$' work.md \
&amp;&amp; grep -q '^### Dev Expectations$' work.md \
&amp;&amp; grep -q 'Test when Reversible, Analyze Otherwise' work.md \
&amp;&amp; grep -q 'Work Out Loud with Intent, not Permission' work.md \
&amp;&amp; grep -q 'Proactivity &amp; Ownership' work.md \
&amp;&amp; grep -q 'Treat AI as a Powertool' work.md \
&amp;&amp; grep -q 'Engineering Ladder' work.md \
&amp;&amp; grep -q '\[Coop - D1\](\./coop-d1\.md)' work.md \
&amp;&amp; grep -q '\[Staff - D5\](\./staff-d5\.md)' work.md \
&amp;&amp; awk -F '\\.' '/^_\[placeholder:/ {if (NF-1 > 2) {print "too long"; exit 1}}' work.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `work.md` exists at the repo root.
    - First non-empty line matches `^_How I evaluate you.{1,80}\._$` (the drafted orientation line; italic single sentence ≤ 15 words verified by the ≤80-char cap on body).
    - Last non-empty line is exactly `← [Back to readme](./readme.md)`.
    - Exactly **4** lines match `^_\[placeholder:.*\]_$` (one per principle: Proactivity, Test-when-Reversible, Work Out Loud, AI Powertool).
    - All four section headings present: `## Work`, `### How I Evaluate You`, `### General Expectations`, `### Dev Expectations`.
    - All four principle bullet texts present verbatim (greppable on distinctive phrases): "Test when Reversible, Analyze Otherwise", "Work Out Loud with Intent, not Permission", "Treat AI as a Powertool", "Proactivity & Ownership".
    - Engineering Ladder link + all 5 D-file links present (`./coop-d1.md` through `./staff-d5.md`).
    - Each placeholder body contains ≤ 2 sentences (period count check inside brackets — voice-safety per D-06 + D-29).
    - No inline HTML (`grep -q '<[a-z]' work.md` returns nothing; checked manually if needed).
    - No `values.md` reference anywhere in the file.
  </acceptance_criteria>
  <done>`work.md` exists with the spec'd shape and content; all grep gates pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon reviews work.md diff and approves orientation + placeholder wording</name>
  <files>work.md</files>
  <action>
Pause execution and present the diff to Bdon. Run `cat work.md` (or `git diff --no-index /dev/null work.md` for a diff-style view) to display the full file content. Wait for Bdon's response:

- If Bdon types `approved` — continue to the next task (commit) without changes.
- If Bdon dictates changes (orientation line wording, placeholder descriptions/placement, intro lines, section order — whichever applies to this file), apply those edits to `work.md` using the Edit tool, then re-run the Task 1 grep gates locally to confirm the structural invariants still hold, then continue to the commit task.

Do not commit during this task. Do not stage. This task only modifies `work.md` if Bdon requests changes; otherwise it makes zero file modifications and only waits for the resume signal.
  </action>
  <verify>
    <automated>
test -f work.md \
&amp;&amp; head -3 work.md | grep -qE '^_.{1,90}\._$' \
&amp;&amp; tail -3 work.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' work.md)" -eq 4 ]
    </automated>
  </verify>
  <what-built>
Claude created `work.md` at the repo root with:
1. A drafted italic orientation line at top: `_How I evaluate you, the four principles I expect you to operate by, and the dev ladder._`
2. The full `## Work` section ported verbatim from `readme.md` L25–L47 (per 01-CONTENT-MAP.md), with `### How I Evaluate You` above `### General Expectations` (FIX-01 position preserved).
3. Four `_[placeholder: ...]_` lines — one BELOW each operating principle bullet — drafted in voice-neutral, terse language naming the "2–3 concrete behaviors + what failure looks like" expansion that's coming post-Phase-2.
4. A back-link line at the bottom: `← [Back to readme](./readme.md)`.

This is a port + scaffold per D-30/D-31. The "2–3 concrete behaviors" content itself is NOT in this file — Bdon writes it manually post-Phase-2.
  </what-built>
  <how-to-verify>
1. Run `git diff --staged` or `cat work.md` to see the full file (it's new, so the entire body is the diff).
2. Read the orientation line aloud. Does it sound like you? Is it ≤ 15 words? Does it name what the file covers without inflation? If not, rewrite it inline before approving.
3. Confirm `### How I Evaluate You` appears above `### General Expectations` (FIX-01 post-state).
4. Read each of the four placeholder lines. Each should be voice-neutral (so the eventual copy you write isn't pre-biased), terse, and ≤ 2 sentences inside the brackets. If any placeholder description nudges your eventual wording, ask for a rewrite.
5. Confirm placement: each placeholder sits BELOW its principle bullet. If you'd prefer ABOVE, flip them now (5 swaps).
6. Confirm the four principle bullets and the Dev Expectations paragraph + D1–D5 link list ported verbatim from `readme.md` — including any existing typos (those are voice; Phase 4 may revisit).
7. Confirm the back-link reads exactly `← [Back to readme](./readme.md)` (lowercase `readme.md` — Phase 3 will update to `README.md` during the rename).
  </how-to-verify>
  <read_first>
    - work.md (the file just created)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-29, D-31, D-35, D-36, plus the Claude's Discretion section listing placeholder placement default + orientation wording default)
  </read_first>
  <acceptance_criteria>
    - Bdon types `approved` (commit as drafted), OR
    - Bdon dictates specific changes to the orientation line, placeholder descriptions, or placeholder positions — those changes land in work.md before commit, and re-verification confirms the grep gates from Task 1 still pass.
  </acceptance_criteria>
  <resume-signal>Type "approved" to commit, or specify changes (orientation line wording, placeholder descriptions, ABOVE/BELOW placement, etc.).</resume-signal>
  <done>Bdon has approved the file content; ready to commit.</done>
</task>

<task type="auto">
  <name>Task 3: Commit work.md as a single atomic commit</name>
  <files>work.md</files>
  <read_first>
    - (None — git only; work.md is the only new path to stage)
  </read_first>
  <action>
Stage exactly one path and commit:

```bash
git add work.md
git commit -m "docs(02): create work.md (port + scaffold)

Ports the \`## Work\` section from readme.md per 01-CONTENT-MAP.md
(rows L25, L27, L29, L31, L33, L35, L37, L38, L40, L41-L45):
- \`### How I Evaluate You\` (FIX-01 position above General Expectations)
- 4 operating principle bullets verbatim
- \`### Dev Expectations\` paragraph + D1–D5 link list verbatim

Adds 4 \`_[placeholder: ...]_\` slots per D-29/D-31 — one per principle.
Bdon authors the 2–3-concrete-behaviors expansion post-Phase-2.
Orientation line per D-35; back-link per D-36.

Refs: SPOKE-02"
```

Do NOT stage `style.md` (doesn't exist yet — comes in 02-03). Do NOT stage `playbooks.md` (doesn't exist yet — comes in 02-04). Do NOT stage planning artifacts in this commit.
  </action>
  <verify>
    <automated>
git diff HEAD~1 HEAD --name-only | tr '\n' ' ' | grep -qE "^work\.md $" \
&amp;&amp; git log -1 --pretty=%s | grep -qF "docs(02): create work.md (port + scaffold)" \
&amp;&amp; git log -1 --pretty=%B | grep -qF "Refs: SPOKE-02"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 HEAD --name-only` returns exactly one path: `work.md`.
    - Commit subject is exactly `docs(02): create work.md (port + scaffold)`.
    - Commit body references SPOKE-02 and cites D-29/D-31/D-35/D-36.
    - Working tree is clean.
  </acceptance_criteria>
  <done>`work.md` landed in a single atomic commit; Phase 2 ship list item #2 complete.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `work.md` exists at the repo root with: orientation line + `## Work` section ported per 01-CONTENT-MAP.md + 4 placeholder slots + back-link.
- The diff at HEAD contains exactly one file (`work.md`).
- Bdon approved the orientation line and placeholder descriptions before commit (D-39).
- Phase 4 verifier can grep `_[placeholder:` in `work.md` and find exactly 4 matches; can grep distinctive phrases ("Test when Reversible", "Work Out Loud", "Treat AI as a Powertool", "Proactivity & Ownership") and confirm port fidelity.
</verification>

<success_criteria>
- All four operating-principle bullets ported verbatim from `readme.md` per 01-CONTENT-MAP.md.
- Four `_[placeholder: ...]_` slots present, one per principle, voice-neutral.
- `### How I Evaluate You` above `### General Expectations` (FIX-01 position).
- Engineering Ladder paragraph + D1–D5 link list ported verbatim.
- Orientation line ≤ 15 words, italic, single sentence.
- Back-link present and points to `./readme.md` (lowercase — Phase 3 updates to README.md).
- Single atomic commit.
- Bdon approved before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spoke-files/02-02-SUMMARY.md` recording: the final orientation line wording (if Bdon rewrote it), the final placeholder descriptions (if Bdon rewrote them), placeholder placement (ABOVE/BELOW each principle), the commit SHA, and any voice notes for downstream phases.
</output>
