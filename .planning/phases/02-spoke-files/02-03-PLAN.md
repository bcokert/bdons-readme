---
phase: 02-spoke-files
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - style.md
autonomous: false
requirements: [SPOKE-03]
must_haves:
  truths:
    - "`style.md` exists at the repo root."
    - "`style.md` opens with a single-sentence italic orientation line (≤ 15 words) and closes with the back-link `← [Back to readme](./readme.md)`."
    - "`style.md` contains the three subsections from `readme.md` L49–L63 ported verbatim: `### Feedback and Performance` (3 bullets), `### Communication and Influence` (3 bullets), `### Learning and Memory` (2 bullets)."
    - "The FIX-02 trimmed Communication-and-Slack bullet ports verbatim; no re-edit."
    - "The un-fixed Flexibility bullet (`* Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before`) ports verbatim; Phase 4 QUAL-02 decides its fate, not Phase 2."
    - "The 'my memory is shit' line ports verbatim — protected voice marker under D-06."
    - "`style.md` contains **zero** placeholder markers (D-33: near-pure port, no expansion placeholders)."
    - "Bdon reviewed and approved the diff before commit (D-39)."
    - "The commit is atomic — diff contains only `style.md`."
  artifacts:
    - path: "style.md"
      provides: "Feedback, Communication, and Learning subsections — pure lift-and-reorganize from readme.md"
      contains: "### Feedback and Performance|### Communication and Influence|### Learning and Memory|my memory is shit"
  key_links:
    - from: "style.md top"
      to: "orientation line"
      via: "italic single-sentence ≤15 words"
      pattern: "^_.{1,90}\\._$"
    - from: "style.md bottom"
      to: "./readme.md"
      via: "back-link line"
      pattern: "← \\[Back to readme\\]\\(\\./readme\\.md\\)"
    - from: "style.md body"
      to: "protected voice marker"
      via: "verbatim port"
      pattern: "memory is shit"
---

<objective>
Create `style.md` at the repo root as a near-pure port of the `## Bdons Style` section from `readme.md` (post-Phase-1 lines L49-L63 — updated from 01-CONTENT-MAP.md's older L47-L63 citations which predate FIX-01/FIX-02). Add the D-35 orientation line at the top and the D-36 back-link at the bottom. No placeholders — D-33 locks style.md as a pure lift-and-reorganize per ARCHITECTURE.md's "almost entirely spoke-ready" guidance.

Purpose: Land the second spoke file. Style content already exists in voice; the only Phase 2 work is the structural lift, orientation line, and back-link.

Output: New file `style.md` at repo root; single atomic commit `docs(02): create style.md (port) — Refs: SPOKE-03`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@.planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md
@.planning/phases/01-content-map-and-required-fixes/01-04-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@readme.md

<!-- Line-citation note: post-Phase-1 readme.md is 63 lines total. The `## Bdons Style` section now lives at L49-L63 (heading L49; `### Feedback and Performance` L51 with bullets L52-L54; `### Communication and Influence` L56 with bullets L57-L59; `### Learning and Memory` L61 with bullets L62-L63 incl. the protected "my memory is shit" marker at L63). These line numbers REPLACE the older L47-L63 / L62 citations in 01-CONTENT-MAP.md. -->

<interfaces>
<!-- Exact content to port — extracted verbatim from readme.md post-Phase-1 state (L49-L63). -->
<!-- Lines port unchanged including any existing typos ("undrestand" at L62) — voice protection per D-06. -->

From readme.md L49-L63 (the `## Bdons Style` section):

```markdown
## Bdons Style

### Feedback and Performance
* Giving Feedback - I generally share in the moment or right after while the memory is fresh. What behaviour you did -> what impact it had and why -> Coaching if needed -> outcome if no change happens (for serious stuff)
* My ideal performance review in either direction is not news to anyone, just a summary of recent feedback and coming opportunities
* Receiving Feedback - I like frequent small feedback rather than big batches. Describe the behaviour and why it was wrong, state what outcome/you want or suggest a different behaviour if relevant

### Communication and Influence
* Communication and Slack - I work best with frequent visible signal (positive, negative, "got it").
* Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before
* I love teaching, and tend towards analogies and metaphors to clarify, especially with non technical recipients

### Learning and Memory
* I learn via testing and experimenting, failure is a signal to improve future iterations. I'll often do something manually the first time to really undrestand it, then apply automation/reusability afterwards for efficiency
* My memory is shit for isolated facts - I need to connect things in a graph of understanding or make it visual to retain, and I lean on visual communication wherever possible when aligning people
```

<!-- Drafted orientation line (D-35): voice-neutral, single sentence, italic, ≤ 15 words. Bdon reviews + may rewrite at checkpoint. -->
<!-- Default: -->
`_How I give and receive feedback, communicate on Slack, and learn and retain things._`
(14 content words. Italic. Single sentence. Mirrors the ARCHITECTURE.md hub-tease shape for style.md.)

<!-- Voice notes: -->
<!-- - "my memory is shit" at the second Learning-and-Memory bullet (L63) is a PROTECTED voice marker (D-06). Verbatim port. -->
<!-- - The FIX-02 line "Communication and Slack - I work best with frequent visible signal..." (L57) was already corrected in Phase 1 plan 01-03. Port verbatim. Do NOT re-edit. -->
<!-- - The Flexibility bullet at L58 ("I love data that changes my opinion, and never hold onto...") is UN-FIXED. FIX-03 was withdrawn (see 01-04-SUMMARY.md). Port verbatim. Phase 4 QUAL-02 audit decides fate. Do NOT pre-fix in Phase 2. -->
<!-- - "undrestand" typo at the first Learning-and-Memory bullet (L62) is voice — do not silently fix. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create style.md (near-pure port + orientation + back-link)</name>
  <files>style.md</files>
  <read_first>
    - readme.md (current source — confirm post-Phase-1 state of `## Bdons Style` section L49-L63, including the FIX-02 trimmed Communication-and-Slack bullet at L57)
    - .planning/phases/01-content-map-and-required-fixes/01-CONTENT-MAP.md (the older L47-L63 / L62 citations predate FIX-01/FIX-02; the post-Phase-1 L49-L63 numbering above is the source of truth for port fidelity)
    - .planning/phases/01-content-map-and-required-fixes/01-04-SUMMARY.md (FIX-03 withdrawal — Flexibility bullet ports verbatim, no edit)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-33 near-pure port + zero placeholders, D-34 minor stitching allowed only for orientation + back-link, D-35 orientation, D-36 back-link, D-06 voice rule + protected markers)
    - .planning/research/ARCHITECTURE.md (Self-Contained Spoke pattern, File Map row for style.md "lift-and-reorganize, not a write")
    - .planning/research/PITFALLS.md (Pitfall 8 — polish pass erases voice; the "undrestand" grep is the mitigation)
    - CLAUDE.md (markdown only, GFM, no inline HTML, voice preservation)
  </read_first>
  <action>
Create `style.md` at the repo root using the Write tool. Exact file body (copy this structure — every body bullet ports verbatim from `readme.md` L49-L63, no edits):

```markdown
_How I give and receive feedback, communicate on Slack, and learn and retain things._

## Bdons Style

### Feedback and Performance
* Giving Feedback - I generally share in the moment or right after while the memory is fresh. What behaviour you did -> what impact it had and why -> Coaching if needed -> outcome if no change happens (for serious stuff)
* My ideal performance review in either direction is not news to anyone, just a summary of recent feedback and coming opportunities
* Receiving Feedback - I like frequent small feedback rather than big batches. Describe the behaviour and why it was wrong, state what outcome/you want or suggest a different behaviour if relevant

### Communication and Influence
* Communication and Slack - I work best with frequent visible signal (positive, negative, "got it").
* Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before
* I love teaching, and tend towards analogies and metaphors to clarify, especially with non technical recipients

### Learning and Memory
* I learn via testing and experimenting, failure is a signal to improve future iterations. I'll often do something manually the first time to really undrestand it, then apply automation/reusability afterwards for efficiency
* My memory is shit for isolated facts - I need to connect things in a graph of understanding or make it visual to retain, and I lean on visual communication wherever possible when aligning people

← [Back to readme](./readme.md)
```

Rules:
- **VERBATIM PORT, NO EDITS.** Every bullet, every typo (`undrestand`, `behaviour`), every distinctive marker (`my memory is shit`, `I love data that changes my opinion`) ports unchanged.
- Section headings (`## Bdons Style`, `### Feedback and Performance`, `### Communication and Influence`, `### Learning and Memory`) port unchanged.
- The "Communication and Slack" line (L57) is the FIX-02 post-Phase-1 wording (already trimmed by Bdon). Port that wording verbatim.
- The "Flexibility" line (L58) is the un-fixed pre-Phase-1 wording (FIX-03 withdrawn). Port that wording verbatim.
- Bullet markers stay `*` (not `-`). List shape matches the source.
- Orientation line is the first non-empty line of the file (above `## Bdons Style`).
- Back-link is the last non-empty line of the file.
- **Zero placeholder markers in this file** — D-33 locks style.md as port-only, no expansion scaffolding.
- No HTML. No LaTeX. No inline anchors. No frontmatter.
  </action>
  <verify>
    <automated>
test -f style.md \
&amp;&amp; head -3 style.md | grep -qE '^_How I give and receive feedback.{1,80}\._$' \
&amp;&amp; awk 'NR<=3 &amp;&amp; /^_/ {n=split($0,a," "); if (n>17) {exit 1}}' style.md \
&amp;&amp; tail -3 style.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' style.md)" -eq 0 ] \
&amp;&amp; grep -q '^## Bdons Style$' style.md \
&amp;&amp; grep -q '^### Feedback and Performance$' style.md \
&amp;&amp; grep -q '^### Communication and Influence$' style.md \
&amp;&amp; grep -q '^### Learning and Memory$' style.md \
&amp;&amp; grep -q 'Giving Feedback - I generally share in the moment' style.md \
&amp;&amp; grep -q 'My ideal performance review in either direction is not news to anyone' style.md \
&amp;&amp; grep -q 'Receiving Feedback - I like frequent small feedback' style.md \
&amp;&amp; grep -q 'Communication and Slack - I work best with frequent visible signal' style.md \
&amp;&amp; grep -q 'Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before' style.md \
&amp;&amp; grep -q 'I love teaching, and tend towards analogies and metaphors' style.md \
&amp;&amp; grep -q 'learn via testing and experimenting' style.md \
&amp;&amp; grep -q 'undrestand' style.md \
&amp;&amp; grep -q 'memory is shit for isolated facts' style.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File `style.md` exists at the repo root.
    - First non-empty line matches `^_How I give and receive feedback.{1,80}\._$` (the drafted orientation line; italic single sentence). Additionally, the awk word-count gate `n > 17` (tokens including italic delimiters) must not trip — equivalent to ≤ 15 content words.
    - Note: this prefix-pinned regex verifies the drafted orientation line. If Bdon rewrites the line at the Task 2 checkpoint, Task 1's gate no longer applies; Task 2's looser regex `^_.{1,90}\._$` is the post-checkpoint authoritative gate.
    - Last non-empty line is exactly `← [Back to readme](./readme.md)`.
    - **Exactly 0** lines match `^_\[placeholder:.*\]_$` (D-33 zero-placeholder rule).
    - All three subsection headings present: `### Feedback and Performance`, `### Communication and Influence`, `### Learning and Memory`.
    - All 8 bullets present verbatim, greppable on distinctive phrases listed above.
    - Protected marker `memory is shit for isolated facts` present (L63 source).
    - Typo `undrestand` preserved (L62 source — voice protection, proves no silent polish pass ran — Pitfall 8 mitigation).
    - Flexibility bullet `Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before` present in full (L58 source; FIX-03 was withdrawn; verbatim port is correct).
    - Communication-and-Slack bullet is the FIX-02 trimmed wording: `Communication and Slack - I work best with frequent visible signal (positive, negative, "got it").` (L57 source; no defensive hedge phrases).
    - No inline HTML.
  </acceptance_criteria>
  <done>`style.md` exists with the spec'd shape and verbatim-ported content; all grep gates pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon reviews style.md diff and approves orientation wording</name>
  <files>style.md</files>
  <action>
Pause execution and present the diff to Bdon. Run `cat style.md` (or `git diff --no-index /dev/null style.md` for a diff-style view) to display the full file content. Wait for Bdon's response:

- If Bdon types `approved` — continue to the next task (commit) without changes.
- If Bdon dictates changes (orientation line wording, placeholder descriptions/placement, intro lines, section order — whichever applies to this file), apply those edits to `style.md` using the Edit tool, then re-run the Task 1 grep gates locally to confirm the structural invariants still hold, then continue to the commit task.

Do not commit during this task. Do not stage. This task only modifies `style.md` if Bdon requests changes; otherwise it makes zero file modifications and only waits for the resume signal.
  </action>
  <verify>
    <automated>
test -f style.md \
&amp;&amp; head -3 style.md | grep -qE '^_.{1,90}\._$' \
&amp;&amp; tail -3 style.md | grep -qF '← [Back to readme](./readme.md)' \
&amp;&amp; [ "$(grep -cE '^_\[placeholder:.*\]_$' style.md)" -eq 0 ]
    </automated>
  </verify>
  <what-built>
Claude created `style.md` at the repo root with:
1. A drafted italic orientation line at top: `_How I give and receive feedback, communicate on Slack, and learn and retain things._`
2. The full `## Bdons Style` section ported verbatim from `readme.md` L49–L63 (per 01-CONTENT-MAP.md with L-citations updated post-Phase-1) — three subsections, 8 bullets total, every typo and distinctive marker preserved.
3. **Zero** placeholder markers — D-33 locks style.md as port-only.
4. A back-link line at the bottom: `← [Back to readme](./readme.md)`.

The FIX-02 Communication-and-Slack wording at L57 (Phase 1 outcome) ports verbatim. The un-fixed Flexibility bullet at L58 (FIX-03 was withdrawn) ports verbatim — Phase 4 QUAL-02 audit will revisit it in context if needed.
  </what-built>
  <how-to-verify>
1. Run `git diff --staged` or `cat style.md` to see the full file.
2. Read the orientation line aloud. Does it sound like you? Is it ≤ 15 words? Does it name what the file covers without inflation or HR language? If not, rewrite it inline before approving.
3. Confirm the three subsections are in source order: Feedback and Performance → Communication and Influence → Learning and Memory.
4. Spot-check the 8 bullets against `readme.md` L52-L63 — every word should match, including the typo `undrestand` at L62 (do NOT silently fix this; voice protection per D-06).
5. Confirm the "Communication and Slack" line reads the FIX-02 trimmed wording: `* Communication and Slack - I work best with frequent visible signal (positive, negative, "got it").` and nothing else.
6. Confirm the Flexibility line ports unchanged: `* Flexibility - I love data that changes my opinion, and never hold onto an opinion simply because it was mine before`. (FIX-03 was withdrawn; this is intentional. Phase 4 QUAL-02 audit will revisit.)
7. Confirm the "my memory is shit" line at L63 source ports unchanged — protected voice marker.
8. Confirm the back-link reads exactly `← [Back to readme](./readme.md)` (lowercase — Phase 3 updates).
9. Confirm zero `_[placeholder:` markers anywhere in the file.
  </how-to-verify>
  <read_first>
    - style.md (the file just created)
    - readme.md L49-L63 (for word-for-word comparison)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-33, D-34, D-35, D-36 + Claude's Discretion section for the default orientation wording)
    - .planning/phases/01-content-map-and-required-fixes/01-04-SUMMARY.md (FIX-03 withdrawal record)
  </read_first>
  <acceptance_criteria>
    - Bdon types `approved` (commit as drafted), OR
    - Bdon dictates a different orientation line — that change lands in `style.md` before commit, and the Task 1 grep gates still pass (orientation regex, zero-placeholder count, back-link, all 8 verbatim bullets).
    - Bdon does NOT ask for body bullet edits in this Phase (D-33 + D-34: style.md is port-only; Flexibility-bullet edits are Phase 4 QUAL-02 territory).
  </acceptance_criteria>
  <resume-signal>Type "approved" to commit, or specify a rewrite for the orientation line.</resume-signal>
  <done>Bdon has approved style.md; ready to commit.</done>
</task>

<task type="auto">
  <name>Task 3: Commit style.md as a single atomic commit</name>
  <files>style.md</files>
  <read_first>
    - (None — git only; style.md is the only new path to stage)
  </read_first>
  <action>
Stage exactly one path and commit:

```bash
git add style.md
git commit -m "docs(02): create style.md (port)

Near-pure port of \`## Bdons Style\` from readme.md per 01-CONTENT-MAP.md
(post-Phase-1 lines L49-L63):
- \`### Feedback and Performance\` (3 bullets) verbatim
- \`### Communication and Influence\` (3 bullets, FIX-02 wording at L57 verbatim, Flexibility at L58 verbatim per FIX-03 withdrawal)
- \`### Learning and Memory\` (2 bullets, 'my memory is shit' protected marker at L63 verbatim)

No placeholders (D-33: lift-and-reorganize, not a write).
Orientation line per D-35; back-link per D-36.

Refs: SPOKE-03"
```

Do NOT stage `playbooks.md` (doesn't exist yet). Do NOT stage planning artifacts in this commit. The diff must be exactly `style.md`.
  </action>
  <verify>
    <automated>
git diff HEAD~1 HEAD --name-only | tr '\n' ' ' | grep -qE "^style\.md $" \
&amp;&amp; git log -1 --pretty=%s | grep -qF "docs(02): create style.md (port)" \
&amp;&amp; git log -1 --pretty=%B | grep -qF "Refs: SPOKE-03"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 HEAD --name-only` returns exactly one path: `style.md`.
    - Commit subject is exactly `docs(02): create style.md (port)`.
    - Commit body references SPOKE-03 and cites D-33/D-35/D-36 + FIX-02/FIX-03 verbatim-port rationale.
    - Working tree is clean.
  </acceptance_criteria>
  <done>`style.md` landed in a single atomic commit; Phase 2 ship list item #3 complete.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `style.md` exists at the repo root with: orientation line + three subsections ported verbatim + back-link.
- Zero placeholder markers in `style.md`.
- The diff at HEAD contains exactly one file (`style.md`).
- Bdon approved the orientation line before commit (D-39).
- Phase 4 verifier can grep distinctive phrases ("memory is shit", "Communication and Slack - I work best", "Flexibility - I love data", "undrestand") and confirm verbatim port fidelity.
</verification>

<success_criteria>
- All 8 bullets from `readme.md` L52-L63 ported verbatim (including typos and distinctive markers).
- Three subsections in source order.
- Zero placeholders (D-33).
- Orientation line ≤ 15 words, italic, single sentence.
- Back-link present and points to `./readme.md`.
- Single atomic commit.
- Bdon approved before commit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spoke-files/02-03-SUMMARY.md` recording: the final orientation line wording (if Bdon rewrote it), the commit SHA, and confirmation that the Flexibility bullet (L58 source) ports unchanged (carryforward note for Phase 4 QUAL-02).
</output>
