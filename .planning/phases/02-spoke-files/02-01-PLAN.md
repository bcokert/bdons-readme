---
phase: 02-spoke-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SPOKE-01]
must_haves:
  truths:
    - "REQUIREMENTS.md SPOKE-01 no longer claims `values.md` exists; it states values content stays inline on the hub."
    - "REQUIREMENTS.md HUB-05 lists 3 spokes (work.md, style.md, playbooks.md) plus the D1–D5 block, with no `values.md`."
    - "ROADMAP.md Phase 2 success criteria 1, 2, and 3 each carry a clarifying note that the word range applies after Bdon writes copy; Phase 2 ships scaffold + port only."
    - "A downstream verifier reading REQUIREMENTS.md or ROADMAP.md after this commit sees no contradiction with D-03 (no values.md) or D-26 (playbooks.md ships as scaffold)."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Patched SPOKE-01 wording + patched HUB-05 wording"
      contains: "Values, Priorities, and Career content kept inline"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 2 SC1/SC2/SC3 amended with scaffold-acknowledgment notes"
      contains: "word range applies after Bdon writes the copy"
  key_links:
    - from: ".planning/REQUIREMENTS.md SPOKE-01"
      to: "02-CONTEXT.md D-03 and D-23"
      via: "wording alignment"
      pattern: "no separate `values.md`"
    - from: ".planning/REQUIREMENTS.md HUB-05"
      to: "02-CONTEXT.md D-24"
      via: "spoke list correction"
      pattern: "3 spokes \\(`work.md`, `style.md`, `playbooks.md`\\)"
    - from: ".planning/ROADMAP.md Phase 2 SC1/SC2/SC3"
      to: "02-CONTEXT.md D-41"
      via: "scaffold-ships-Phase-2 clarification"
      pattern: "word range applies after Bdon writes the copy"
---

<objective>
Patch REQUIREMENTS.md and ROADMAP.md so the source-of-truth requirements docs match the locked decisions in 02-CONTEXT.md (D-03, D-23, D-24, D-41). This ships **first** in Phase 2 so every downstream plan, executor, and verifier reads the corrected requirements.

Purpose: Eliminate two carryforward contradictions (SPOKE-01 names `values.md`; HUB-05 lists `values.md` in the spoke link list) and pre-empt a Phase 4 audit failure that would flag Phase 2 for missing the work.md/style.md/playbooks.md word budgets (the scaffold approach is intentional per D-26/D-30/D-33 — the word budgets apply once Bdon writes the copy, not at Phase 2 ship).

Output: A single atomic commit `docs(02): patch SPOKE-01, HUB-05, and Phase 2 SC1-3 (scaffold acknowledgment)` modifying exactly two files: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-spoke-files/02-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Decisions this patch implements verbatim (no executor invention needed) -->

From 02-CONTEXT.md D-23 (SPOKE-01 rewrite — exact text):
> "**SPOKE-01**: Values, Priorities, and Career content kept inline on the hub (no separate `values.md`); content rationale recorded in 01-CONTENT-MAP.md and ARCHITECTURE.md File Map."

From 02-CONTEXT.md D-24 (HUB-05 rewrite — exact text):
> "**HUB-05**: Hub links to all 3 spokes (`work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block"

From 02-CONTEXT.md D-41 (ROADMAP SC1/SC2/SC3 clarifying note — exact text):
> "(word range applies after Bdon writes the copy; Phase 2 ships scaffold + port only)"

Current REQUIREMENTS.md state (to be replaced):
- L19 SPOKE-01: `- [ ] **SPOKE-01**: \`values.md\` exists — holds Values, Priorities, and Career content lifted from current readme; sub-topics inline, in Bdon's voice`
- L14 HUB-05: `- [ ] **HUB-05**: Hub links to all 4 spokes (\`values.md\`, \`work.md\`, \`style.md\`, \`playbooks.md\`) plus the D1–D5 dev-level block`

Current ROADMAP.md state (to be amended — lines may shift; identify by content not line):
- SC1: `\`work.md\` exists and holds all four operating principles ... ; 400–600 words`
- SC2: `\`style.md\` exists and holds Feedback ... ; 300–450 words`
- SC3: `\`playbooks.md\` exists and holds four titled playbooks ... ; 600–900 words`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch SPOKE-01 and HUB-05 in REQUIREMENTS.md; patch Phase 2 SC1/SC2/SC3 in ROADMAP.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — patching SPOKE-01 line and HUB-05 line; the coverage notes paragraph at L97-L99 also references the SPOKE-01/HUB-05 contradiction and should be updated to reflect resolution)
    - .planning/ROADMAP.md (full file — patching Phase 2 success criteria 1, 2, 3)
    - .planning/phases/02-spoke-files/02-CONTEXT.md (D-22 through D-41 — exact patch wording is in D-23, D-24, D-41)
    - CLAUDE.md (markdown-only constraint; no inline HTML)
  </read_first>
  <action>
**Edit 1 — REQUIREMENTS.md HUB-05 (replace exact text):**

Before:
```
- [ ] **HUB-05**: Hub links to all 4 spokes (`values.md`, `work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block
```
After:
```
- [ ] **HUB-05**: Hub links to all 3 spokes (`work.md`, `style.md`, `playbooks.md`) plus the D1–D5 dev-level block
```

**Edit 2 — REQUIREMENTS.md SPOKE-01 (replace exact text):**

Before:
```
- [ ] **SPOKE-01**: `values.md` exists — holds Values, Priorities, and Career content lifted from current readme; sub-topics inline, in Bdon's voice
```
After:
```
- [ ] **SPOKE-01**: Values, Priorities, and Career content kept inline on the hub (no separate `values.md`); content rationale recorded in 01-CONTENT-MAP.md and ARCHITECTURE.md File Map
```

**Edit 3 — REQUIREMENTS.md coverage notes paragraph (L97-L99 area).** Update to record resolution. Replace the existing "Coverage notes:" paragraph with:

```
**Coverage notes:**

SPOKE-01 and HUB-05 were reconciled with the research recommendation in Phase 2 plan 02-01 (2026-05-14): `values.md` is not created in v1; values equations stay inline on the hub. SPOKE-01 now describes the inline-on-hub disposition; HUB-05 now lists 3 spokes (work.md, style.md, playbooks.md) plus the D1–D5 block. See 02-CONTEXT.md D-03, D-23, D-24.
```

**Edit 4 — ROADMAP.md Phase 2 SC1 (append clarifying note).** Locate the line containing `400–600 words` in the Phase 2 Success Criteria block. Append ` (word range applies after Bdon writes the copy; Phase 2 ships scaffold + port only)` to the end of that bullet.

**Edit 5 — ROADMAP.md Phase 2 SC2 (append clarifying note).** Locate the line containing `300–450 words` in the Phase 2 Success Criteria block. Append the same clarifying note.

**Edit 6 — ROADMAP.md Phase 2 SC3 (append clarifying note).** Locate the line containing `600–900 words` in the Phase 2 Success Criteria block. Append the same clarifying note.

Use the Edit tool for each of the six edits. Do NOT use heredoc or rewrite the full files.

Do not modify any other content. Do not touch FIX-01/FIX-02/FIX-03 traceability rows. Do not touch v2 requirements. Do not touch Phase 1, Phase 3, or Phase 4 success criteria. Do not touch the `Progress` table.
  </action>
  <verify>
    <automated>
grep -q "Hub links to all 3 spokes (\`work.md\`, \`style.md\`, \`playbooks.md\`)" .planning/REQUIREMENTS.md \
&amp;&amp; ! grep -q "Hub links to all 4 spokes" .planning/REQUIREMENTS.md \
&amp;&amp; grep -q "Values, Priorities, and Career content kept inline on the hub (no separate \`values.md\`)" .planning/REQUIREMENTS.md \
&amp;&amp; ! grep -q "\`values.md\` exists — holds Values, Priorities, and Career content lifted" .planning/REQUIREMENTS.md \
&amp;&amp; [ "$(grep -c "word range applies after Bdon writes the copy; Phase 2 ships scaffold + port only" .planning/ROADMAP.md)" -eq 3 ]
    </automated>
  </verify>
  <acceptance_criteria>
    - HUB-05 line in REQUIREMENTS.md contains the exact string `Hub links to all 3 spokes (\`work.md\`, \`style.md\`, \`playbooks.md\`)`.
    - REQUIREMENTS.md no longer contains the string `Hub links to all 4 spokes`.
    - SPOKE-01 line in REQUIREMENTS.md contains the exact string `Values, Priorities, and Career content kept inline on the hub (no separate \`values.md\`)`.
    - REQUIREMENTS.md no longer contains the string ``\`values.md\` exists — holds Values, Priorities, and Career content lifted``.
    - ROADMAP.md contains exactly **3** occurrences of the string `word range applies after Bdon writes the copy; Phase 2 ships scaffold + port only`.
    - REQUIREMENTS.md coverage notes paragraph references D-03, D-23, D-24 by ID.
    - Only `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` are modified (no spoke files created in this task).
  </acceptance_criteria>
  <done>Both files patched per D-23/D-24/D-41; greps pass; no other files touched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the requirements + roadmap patch as a single atomic commit</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - (None — git only; staging two paths already modified in Task 1)
  </read_first>
  <action>
Stage exactly two paths and commit:

```bash
git add .planning/REQUIREMENTS.md .planning/ROADMAP.md
git commit -m "docs(02): patch SPOKE-01, HUB-05, and Phase 2 SC1-3 (scaffold acknowledgment)

Reconciles two carryforward contradictions before Phase 2 spoke files ship:
- SPOKE-01 no longer references \`values.md\` (D-03, D-23)
- HUB-05 now lists 3 spokes, not 4 (D-24)
- Phase 2 SC1/SC2/SC3 word ranges acknowledge scaffold-ships-Phase-2 (D-41)

Refs: SPOKE-01"
```

Do NOT stage spoke files. Do NOT stage other planning artifacts. The diff must be exactly the two paths.
  </action>
  <verify>
    <automated>
git diff HEAD~1 HEAD --name-only | sort | tr '\n' ' ' | grep -qE "^\.planning/REQUIREMENTS\.md \.planning/ROADMAP\.md $"
    </automated>
  </verify>
  <acceptance_criteria>
    - `git diff HEAD~1 HEAD --name-only` returns exactly two paths: `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` (no spoke files, no STATE.md, no other planning files).
    - Commit subject is `docs(02): patch SPOKE-01, HUB-05, and Phase 2 SC1-3 (scaffold acknowledgment)`.
    - Commit body references D-03, D-23, D-24, D-41.
    - Working tree is clean after commit (`git status --porcelain` returns empty for these paths).
  </acceptance_criteria>
  <done>Single atomic commit landed; downstream plans now read the corrected requirements docs.</done>
</task>

</tasks>

<verification>
After both tasks, the following must be TRUE:
- REQUIREMENTS.md SPOKE-01 reflects D-23 verbatim.
- REQUIREMENTS.md HUB-05 reflects D-24 verbatim.
- ROADMAP.md Phase 2 SC1/SC2/SC3 each carry the D-41 clarifying note.
- The commit is atomic: exactly the two doc paths, nothing else.
- No spoke files (`work.md`, `style.md`, `playbooks.md`) exist yet — those land in 02-02 / 02-03 / 02-04.
</verification>

<success_criteria>
- Two-file patch ships as a single atomic commit before any spoke file is created.
- Downstream Phase 2 plans (02-02, 02-03, 02-04) and Phase 4 verifiers read the corrected requirements.
- No regression to FIX-01, FIX-02, FIX-03, or any non-Phase-2 success criterion.
</success_criteria>

<output>
After completion, create `.planning/phases/02-spoke-files/02-01-SUMMARY.md` recording: the six edits applied, the commit SHA, and confirmation that SPOKE-01/HUB-05/SC1-3 now align with D-03/D-23/D-24/D-41.
</output>
