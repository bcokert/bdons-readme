---
phase: 04-voice-and-link-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-voice-and-link-audit/04-AUDIT.md
  - README.md
  - work.md
  - style.md
  - playbooks.md
autonomous: false
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
tags:
  - markdown
  - audit
  - voice
  - links

must_haves:
  truths:
    - "Every cross-file link in the hub, spokes, and D-files resolves via git ls-files (QUAL-01 / ROADMAP SC1)"
    - "Trust-claim audit yields zero unanchored aspirational claims — each surviving `always` / `never` / `love` / `hate` / `I will` match has either a behavioral anchor or a Bdon-confirmed defer/no-action disposition (QUAL-02 / ROADMAP SC2)"
    - "Hub-standalone test passes — Claude reads README.md alone and fills five bullets (identity / values / career thesis / four-category map / last-reviewed) and Bdon confirms (QUAL-03 / ROADMAP SC3)"
    - "Voice read-aloud test passes per spoke: zero HR-coded glossary matches, zero warmup-phrase matches, distinctive protected markers intact (ROADMAP SC4)"
    - "Every Phase 3 03-REVIEW.md finding (WR-01..WR-04 + IN-01..IN-06) plus the FIX-03 Flexibility carryforward plus the PITFALLS Pitfall 2 power-asymmetry item has an explicit disposition row in 04-AUDIT.md (D-04-13 + D-04-04)"
    - "Every approved fix lands as an atomic per-thematic-group commit (D-04-12 + D-04-03)"
  artifacts:
    - path: ".planning/phases/04-voice-and-link-audit/04-AUDIT.md"
      provides: "Consolidated audit report (D-04-02 schema: id, check, location, evidence, recommendation, disposition) + Hub Standalone reader summary + Voice Read-Aloud notes"
      contains: "04-AUDIT"
    - path: "README.md"
      provides: "Hub with WR-03 (apapting -> adapting) and any other approved fixes applied; size budget preserved (≤380w / ≤55L)"
      contains: "Bdon's Manager Readme"
    - path: "work.md"
      provides: "Spoke with WR-02 (myt -> my), WR-04 (before before -> before), and any approved H1 / IN-03 fixes applied"
      contains: "Work"
    - path: "style.md"
      provides: "Spoke with IN-01 (undrestand -> understand) and any approved C-06 / C-11 fixes applied"
      contains: "Feedback and Performance"
    - path: "playbooks.md"
      provides: "Spoke with any approved C-01 H1 fix applied"
      contains: "Playbooks"
  key_links:
    - from: "README.md"
      to: "./work.md, ./style.md, ./playbooks.md, ./coop-d1.md..staff-d5.md"
      via: "Drill in info-scent link blocks + Dev Expectations block (8 outbound relative links)"
      pattern: '\]\(\./[^)]+\.md(#[^)]*)?\)'
    - from: "README.md"
      to: "work.md#dev-expectations"
      via: "Deep-link from hub Dev Expectations block; anchor must match ### Dev Expectations heading slug"
      pattern: 'work\.md#dev-expectations'
    - from: "work.md / style.md / playbooks.md"
      to: "./README.md"
      via: "Back-link line at file bottom"
      pattern: '\[Back to readme\]\(\./README\.md\)'
---

<objective>
Run the four locked Phase 4 checks (QUAL-01 link integrity, QUAL-02 trust-claim + voice, QUAL-03 hub-standalone, ROADMAP SC4 voice read-aloud) plus the eleven carryforward items (10 from Phase 3 03-REVIEW.md per D-04-13, 1 from PITFALLS.md Pitfall 2 per D-04-04). Consolidate findings into a single `04-AUDIT.md` per D-04-02. Pause at a `checkpoint:human-verify` so Bdon walks the table and confirms or overrides each row's disposition. Apply approved `fix-now` items as atomic per-thematic-group commits per D-04-12. Run the hub-standalone test AFTER fixes land (D-04-14) so it sees the shipped post-fix state.

Purpose: Close out QUAL-01..QUAL-03 before the doc goes live in onboarding. The audit is hybrid grep-then-judge (D-04-01) — automated greps surface every candidate, Bdon's judgment decides each. Nothing voice-sensitive auto-fixes (PITFALLS Pitfall 8).

Output: `04-AUDIT.md` with structured findings table + Hub Standalone reader summary + Voice Read-Aloud notes; 1..N atomic fix-commits per Bdon's approved dispositions; all four files (`README.md`, `work.md`, `style.md`, `playbooks.md`) updated as approved with size + voice invariants preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-and-link-audit/04-CONTEXT.md
@.planning/phases/04-voice-and-link-audit/04-RESEARCH.md
@.planning/phases/03-hub-restructure-and-rename/03-REVIEW.md
@.planning/phases/02-spoke-files/02-CONTEXT.md

<interfaces>
<!-- Pre-computed audit content extracted from RESEARCH.md. Executor uses these verbatim. -->
<!-- Source files under audit (post-Phase-3 shipped state, verified 2026-05-14): -->

Hub (README.md) — 316w / 48L, headroom 64w / 7L under HUB-02 budget. H1 `# Bdon's Manager Readme`.
work.md — port + 4 expansion placeholders. H2 `## Work`. Anchor `#dev-expectations` resolves to `### Dev Expectations` at L25.
style.md — near-pure port. H2 `## Bdons Style` (no apostrophe — IN-02 carryforward).
playbooks.md — pure scaffold with 4 playbook placeholders. H2 `## Playbooks`.
coop-d1.md, junior-d2.md, intermediate-d3.md, senior-d4.md, staff-d5.md — pre-existing; voice-edits OUT of scope (D-files predate the project per D-04-04 / "Deferred Ideas").

D-04-02 04-AUDIT.md schema (per-row): `id` | `check` | `location` | `evidence` | `recommendation` | `disposition`
- `check` ∈ { QUAL-01, QUAL-02, QUAL-03, SC4, carryforward }
- `disposition` ∈ { fix-now, defer-decision-for-Bdon, defer (with where-it-goes note), no-action }
- Default dispositions follow D-04-13 (Phase 3 carryforward) and D-04-04 (Flexibility line, power-asymmetry).

D-04-11 protected markers — NEVER flagged by any voice grep, NEVER edited by audit:
- `my memory is shit`
- `Autonomy × ∫(Capability × Confidence)` (and the inline-code Team Net Impact equation)
- `Learning -> Freedom -> Adaptability -> Calm`
- `Family -> Health -> Security`
- `I'm more of a how guy than an ideas guy`
- The italic orientation lines at the top of each spoke (`_How I evaluate peeps..._`, `_How I give and receive feedback..._`, `_How I run evaluations..._`)
- The `_[placeholder: ...]_` scaffold markers in `work.md` and `playbooks.md` (D-29 from Phase 2)

D-04-09 link-integrity procedure:
1. Extract every `\[[^]]+\]\([^)]+\)` link from all 9 files.
2. External `http(s)://` → out of scope (skip).
3. Pure `#fragment` → flag as unexpected (none currently present).
4. Relative file → verify with `git ls-files | grep -Fx <target>`.
5. Anchor portion → grep target file for heading whose GFM slug matches the fragment (lowercase, strip non-`[a-z0-9 -]`, spaces→hyphens).

D-04-08 trust-claim regex (POSIX extended, case-insensitive):
```
\b(always|never|love|hate)\b|\bI will\b
```

D-04-10 HR-coded language glossary (POSIX extended, case-insensitive):
```
\b(stakeholders?|alignment|leverage|synergize|synergy|deliverables?|operationalize|circle back|table this|value-add|low-hanging fruit|move the needle|at the end of the day)\b
```

RESEARCH.md verified outputs (re-run at Task 1 start to confirm state hasn't drifted since 2026-05-14):
- Trust-claim grep returns exactly 6 matches across all 9 files. Known lines: `style.md:12` (love + never in Flexibility line), `style.md:13` (love in teaching line), `README.md:12` (hate in Calm bullet), `README.md:16` (always in Family bullet), `intermediate-d3.md:8` (never), `senior-d4.md:9` (never).
- HR-glossary grep returns 0 matches.
- Warmup-phrase grep returns 0 matches.
- Banned-GFM-construct grep returns 0 matches.
- Phase 3 typo grep — `\bmyt\b|apapting|before before|undrestand|orgs specific` — returns 5 matches as documented in 03-REVIEW.md.
- All 9 hub link targets resolve via `git ls-files`. The `./work.md#dev-expectations` anchor resolves to `### Dev Expectations` at `work.md:25`.

Carryforward audit-row seeds (from RESEARCH.md "Phase 3 Carryforward Items — Audit-Row Templates"):

| ID | Source | File:Line | Default Disposition |
|----|--------|-----------|---------------------|
| C-01 | 03-REVIEW.md WR-01 | work.md:3, style.md:3, playbooks.md:3 | fix-now — add `# Work` / `# Style` / `# Playbooks` H1 |
| C-02 | 03-REVIEW.md WR-02 | work.md:1 | fix-now — `myt` → `my` |
| C-03 | 03-REVIEW.md WR-03 | README.md:11 | fix-now — `apapting` → `adapting` |
| C-04 | 03-REVIEW.md WR-04 | work.md:17 | fix-now — remove duplicate `before` |
| C-05 | 03-REVIEW.md IN-01 | style.md:16 | fix-now — `undrestand` → `understand` |
| C-06 | 03-REVIEW.md IN-02 | style.md:3 | defer-decision-for-Bdon — `## Bdons Style` apostrophe vs H1-promote |
| C-07 | 03-REVIEW.md IN-03 | work.md:26 | defer-decision-for-Bdon — `orgs` → `org's` |
| C-08 | 03-REVIEW.md IN-04 | work.md:26 | no-action — Vidyard SSO external link is intentional |
| C-09 | 03-REVIEW.md IN-05 | README.md:38–44 | defer-decision-for-Bdon — redundant Dev Expectations link |
| C-10 | 03-REVIEW.md IN-06 | playbooks.md / work.md scaffold | no-action — scaffold by design (D-29) |
| C-11 | PITFALLS.md Pitfall 2 LOW | style.md Feedback section | defer-decision-for-Bdon — power-asymmetry one-line acknowledgment |
| F-01 | FIX-03 withdrawn 2026-05-12 | style.md:12 | defer-decision-for-Bdon — Flexibility line (love + never) |

D-04-12 atomic-commit grouping (Task 3 fixes; Claude's-Discretion ordering within Wave per D-04-12):
- Commit 1 (start of Task 2): `docs(04): audit findings` — `04-AUDIT.md` lands as a single artifact.
- Commit 2 (Task 3): `docs(04): fix README.md hub typos` — `apapting` → `adapting`.
- Commit 3 (Task 3): `docs(04): fix work.md typos + add H1` — `myt` → `my`, `before before` → `before`, add `# Work` H1 (if C-01 approved), apply C-07 / IN-03 if approved.
- Commit 4 (Task 3): `docs(04): fix style.md typos + add H1` — `undrestand` → `understand`, add `# Style` H1 (if C-01 approved), apply C-06 if approved.
- Commit 5 (Task 3): `docs(04): add H1 to playbooks.md` — add `# Playbooks` H1 (if C-01 approved).
- Commit N+1 (per voice-judgment file): `docs(04): apply voice-judgment fixes to <file>` — only if Bdon approves any defer-decision rows for fix-now.
- (Commits per Bdon's actual dispositions; deferred and no-action rows produce no commit.)

Voice / structural invariants the audit MUST preserve:
- `README.md` ≤ 380w / ≤ 55L (HUB-02). After every hub fix-commit, verify with `wc -w` and `wc -l`.
- D-04-11 protected markers untouched.
- GFM-only output (no inline HTML, no LaTeX, no `<details>`).
- Distinctive voice markers (Pitfall 8). No Claude-initiated rewrites — only Bdon-confirmed fixes execute.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated audit checks and write 04-AUDIT.md</name>
  <files>.planning/phases/04-voice-and-link-audit/04-AUDIT.md</files>
  <read_first>
    - README.md (hub — most-read surface; in scope for every check)
    - work.md (spoke — port + 4 placeholders; H2 `## Work` at L3; anchor target for hub deep-link)
    - style.md (spoke — near-pure port; H2 `## Bdons Style` at L3; contains Flexibility line at L12)
    - playbooks.md (spoke — pure scaffold; H2 `## Playbooks` at L3; 4 placeholders)
    - coop-d1.md, junior-d2.md, intermediate-d3.md, senior-d4.md, staff-d5.md (D-files in scope for QUAL-01 link verification and QUAL-02 trust-claim grep ONLY — voice edits explicitly OUT of scope per D-04-04 "Deferred Ideas")
    - .planning/phases/03-hub-restructure-and-rename/03-REVIEW.md (the 10 carryforward findings WR-01..WR-04, IN-01..IN-06; each gets an audit row per D-04-13)
    - .planning/phases/04-voice-and-link-audit/04-RESEARCH.md "Trust-Claim Audit", "Link Integrity Check", "Voice Audit", "Phase 3 Carryforward Items" sections (verified grep outputs + audit-row seeds)
    - .planning/phases/04-voice-and-link-audit/04-CONTEXT.md (D-04-01..D-04-15 locked decisions)
    - .planning/phases/02-spoke-files/02-CONTEXT.md D-29 (placeholder marker shape — must be excluded from voice grep results) and D-36 (back-link convention — must resolve in link check)
  </read_first>
  <action>
Execute the four automated audit checks below in this order. Capture the raw output of every grep. Then write `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` consolidating findings into the D-04-02 schema (`id` | `check` | `location` | `evidence` | `recommendation` | `disposition`). Do NOT apply any fixes in this task — fixes happen in Task 3 after Bdon's checkpoint.

**Step 1 — Pre-audit verification (RESEARCH.md "Pattern 4"):** Confirm shipped state matches RESEARCH.md assumptions.

```bash
[ "$(git ls-files | grep -cx 'README.md')" -eq 1 ] || echo "FAIL: README.md not in git index"
[ "$(git ls-files | grep -cx 'readme.md')" -eq 0 ] || echo "FAIL: lowercase readme.md still tracked"
grep -nE '^### Dev Expectations\b' work.md || echo "FAIL: Dev Expectations anchor target missing"
```

If any FAIL line appears, STOP and surface the drift to the user before continuing.

**Step 2 — QUAL-01 Link Integrity Check (D-04-09).** Run from repo root:

```bash
# A. Extract every relative .md link target from hub + spokes
grep -hoE '\]\(\./[^)]+\.md(#[^)]*)?\)' README.md work.md style.md playbooks.md | \
  sed -E 's|^\]\(\./||; s|\)$||' | sort -u

# B. For each unique target file portion, verify presence in git index
#    (record `git ls-files | grep -Fx '<target-file>'` returns exactly 1 line)
# C. For each anchor portion (currently only `dev-expectations`), grep target file for
#    matching heading slug per RESEARCH.md "GFM Slug Algorithm"
grep -nE '^#{1,6} ' work.md   # confirm `### Dev Expectations` is present
```

Expected (per RESEARCH.md verified state): all 9 outbound relative links resolve; `work.md#dev-expectations` anchor resolves to `### Dev Expectations` at `work.md:25`. Record each link's resolution as a row in 04-AUDIT.md (or a single summary row `L-00 | QUAL-01 | (all hub/spoke .md links) | <count> links verified | none | no-action — all pass` if all pass).

**Step 3 — QUAL-02 Trust-Claim Audit (D-04-08).** Run:

```bash
grep -niE '\b(always|never|love|hate)\b|\bI will\b' \
  README.md work.md style.md playbooks.md \
  coop-d1.md junior-d2.md intermediate-d3.md senior-d4.md staff-d5.md
```

Expected (per RESEARCH.md verified output) — exactly 6 matches. Each becomes a row in 04-AUDIT.md with the surrounding sentence as evidence and the default disposition from RESEARCH.md "Per-Match Default Disposition":

| # | File:Line | Match | Default Disposition |
|---|-----------|-------|---------------------|
| T-01 | style.md:12 | `love` + `never` in Flexibility line | defer-decision-for-Bdon (FIX-03 was withdrawn 2026-05-12; this row IS F-01 — merge under a single id) |
| T-02 | style.md:13 | `love` in teaching line | defer-decision-for-Bdon |
| T-03 | README.md:12 | `hate` in Calm/Drama bullet | defer-decision-for-Bdon |
| T-04 | README.md:16 | `always` in Family bullet | defer-decision-for-Bdon |
| T-05 | intermediate-d3.md:8 | `never` in D3 bullet | no-action — pre-existing D-file content |
| T-06 | senior-d4.md:9 | `never` in D4 bullet | no-action — pre-existing D-file content |

For each trust-claim row, include in the `recommendation` column 1–3 options drawn from RESEARCH.md "Behaviorally Anchored — Concrete Templates" (preserve / anchor with behavioral example / soften to falsifiable claim). Bdon picks at checkpoint.

**Step 4 — QUAL-02 / SC4 Voice Audit (D-04-10 HR glossary + warmup phrases + banned GFM constructs).** Run:

```bash
# HR-coded language
grep -niE '\b(stakeholders?|alignment|leverage|synergize|synergy|deliverables?|operationalize|circle back|table this|value-add|low-hanging fruit|move the needle|at the end of the day)\b' \
  README.md work.md style.md playbooks.md

# Warmup phrases
grep -niE "I'?d like to think|In this document|Going forward|It's important to note|Without further ado|Before we begin|First and foremost" \
  README.md work.md style.md playbooks.md

# Banned GFM constructs (defense in depth)
grep -nE '<(div|details|table|svg|a name|a id)|\$\$|<!DOCTYPE' \
  README.md work.md style.md playbooks.md

# Bolded-inline-header AI-bullet pattern (RESEARCH.md "Inflation Markers")
grep -nE '^\* \*\*[^*]+\*\*:' \
  README.md work.md style.md playbooks.md
```

Expected (per RESEARCH.md verified state) — zero matches across all four greps. Record a single `V-00 | SC4 | (4 voice greps) | 0 / 0 / 0 / 0 matches | none | no-action — all clean` row if all pass. If any match surfaces (unexpected), record a row per match with `defer-decision-for-Bdon` (could be intentional voice, could be a slip).

**Step 5 — Phase 3 Carryforward (D-04-13) typo verification.** Run:

```bash
grep -nE '\bmyt\b|apapting|before before|undrestand|orgs specific' \
  README.md work.md style.md playbooks.md
```

Expected — 5 matches (one for each of WR-02, WR-03, WR-04, IN-01, IN-03 per 03-REVIEW.md). Each becomes its C-XX row in 04-AUDIT.md per the seed table in the `<interfaces>` block above. Also include rows C-01 (spoke H1s), C-06 (Bdons Style heading), C-08 (SSO link no-action), C-09 (redundant Dev Expectations link), C-10 (scaffold by design no-action), C-11 (power-asymmetry from PITFALLS Pitfall 2) — these do NOT come from the typo grep but are required carryforward rows per D-04-13 / D-04-04.

**Step 6 — Read-aloud notes (SC4 binding gate).** For each of `README.md`, `work.md`, `style.md`, `playbooks.md`, read the entire file once and write a short `## Voice Read-Aloud — <file>` block in 04-AUDIT.md noting whether it sounds like Bdon (terse, deadpan, distinctive markers intact). Most rows should be `disposition: preserve`. Only flag sentences that DON'T sound like Bdon as `defer-decision-for-Bdon`. D-04-11 protected markers are explicitly NOT candidates. The `_[placeholder: ...]_` scaffold lines are explicitly NOT candidates (D-29).

**Step 7 — Write 04-AUDIT.md.** Assemble `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` with this exact top-level structure (Claude's-Discretion on whether to render row blocks as a single table or per-check tables; schema is fixed):

```markdown
# Phase 4 Audit Report

**Audited:** YYYY-MM-DD
**Files in scope:** README.md, work.md, style.md, playbooks.md, coop-d1.md, junior-d2.md, intermediate-d3.md, senior-d4.md, staff-d5.md
**Checks:** QUAL-01 (link integrity), QUAL-02 (trust-claim + voice), QUAL-03 (hub-standalone — RUN POST-FIX in Task 3), ROADMAP SC4 (voice read-aloud)

## Findings Summary

| id | check | location | evidence | recommendation | disposition |
|----|-------|----------|----------|----------------|-------------|
| L-XX | QUAL-01 | ... | ... | ... | ... |
| T-XX | QUAL-02 | ... | ... | ... | ... |
| V-XX | SC4 | ... | ... | ... | ... |
| C-XX | carryforward | ... | ... | ... | ... |
| F-01 | QUAL-02 / carryforward | style.md:12 | ... | ... | defer-decision-for-Bdon |

## Voice Read-Aloud Notes

### README.md
- (one short paragraph or bullet list — what sounds like Bdon; what (if anything) doesn't)

### work.md
- (same)

### style.md
- (same — flag the Flexibility line and the teaching line per T-01 / T-02 cross-ref)

### playbooks.md
- (same — note that placeholder content is excluded per D-29; only orientation italic + H3 titles + italic intros are read)

## Hub Standalone Test — Reader Summary

_(POST-FIX — populated in Task 3 Step 3 per D-04-14. Placeholder here for now.)_

## Open Questions for Bdon

(Mirror RESEARCH.md "Open Questions" section: Flexibility line / `## Bdons Style` heading / power-asymmetry one-liner / redundant Dev Expectations link / `behaviours` British spelling / `orgs specific` apostrophe. Each maps to a row id above.)
```

**Step 8 — Commit.** Atomic commit (D-04-12):

```
docs(04): audit findings

Phase 4 close-out audit report. QUAL-01 (link integrity) and the
mechanical SC4 voice greps all pass cleanly. QUAL-02 trust-claim
grep surfaces 6 matches (4 hub/spoke + 2 D-file). Phase 3 03-REVIEW.md
carryforward + FIX-03 Flexibility + PITFALLS Pitfall 2 power-asymmetry
rows seeded with default dispositions per D-04-13 / D-04-04. Awaiting
Bdon checkpoint (Task 2) to confirm dispositions before any fix lands.
```
  </action>
  <verify>
    <automated>[ -f .planning/phases/04-voice-and-link-audit/04-AUDIT.md ] && [ "$(git ls-files | grep -cx '.planning/phases/04-voice-and-link-audit/04-AUDIT.md')" -eq 1 ] && grep -q '^# Phase 4 Audit Report' .planning/phases/04-voice-and-link-audit/04-AUDIT.md && grep -q 'QUAL-01' .planning/phases/04-voice-and-link-audit/04-AUDIT.md && grep -q 'QUAL-02' .planning/phases/04-voice-and-link-audit/04-AUDIT.md && grep -q 'SC4' .planning/phases/04-voice-and-link-audit/04-AUDIT.md && [ "$(grep -niE '\b(always|never|love|hate)\b|\bI will\b' README.md work.md style.md playbooks.md coop-d1.md junior-d2.md intermediate-d3.md senior-d4.md staff-d5.md | wc -l)" -eq 6 ] && [ "$(grep -niE '\b(stakeholders?|alignment|leverage|synergize|synergy|deliverables?|operationalize|circle back|table this|value-add|low-hanging fruit|move the needle|at the end of the day)\b' README.md work.md style.md playbooks.md | wc -l)" -eq 0 ] && [ "$(grep -nE '\bmyt\b|apapting|before before|undrestand|orgs specific' README.md work.md style.md playbooks.md | wc -l)" -eq 5 ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` exists and is tracked in git (`git ls-files | grep -Fx '.planning/phases/04-voice-and-link-audit/04-AUDIT.md'` returns 1 line)
    - 04-AUDIT.md contains the heading `# Phase 4 Audit Report` (grep returns ≥ 1 match)
    - 04-AUDIT.md references all four checks: `grep -c 'QUAL-01' 04-AUDIT.md` ≥ 1, `grep -c 'QUAL-02' 04-AUDIT.md` ≥ 1, `grep -c 'QUAL-03' 04-AUDIT.md` ≥ 1, `grep -c 'SC4' 04-AUDIT.md` ≥ 1
    - 04-AUDIT.md contains rows for at least the 11 named carryforward items: `grep -cE '^\| C-(0[1-9]|10|11) \|' 04-AUDIT.md` returns ≥ 11 OR equivalent count if rendered as non-table list (grep `'C-01\|C-02\|C-03\|C-04\|C-05\|C-06\|C-07\|C-08\|C-09\|C-10\|C-11'` returns ≥ 11 distinct ids)
    - Trust-claim grep on shipped state still returns exactly 6 matches: `grep -niE '\b(always|never|love|hate)\b|\bI will\b' <all 9 files> | wc -l` returns 6
    - HR-coded language grep on shipped state still returns 0 matches: `grep -niE '\b(stakeholders?|alignment|leverage|synergize|synergy|deliverables?|operationalize|circle back|table this|value-add|low-hanging fruit|move the needle|at the end of the day)\b' <4 audit files> | wc -l` returns 0
    - Warmup-phrase grep returns 0 matches: `grep -niE "I'?d like to think|In this document|Going forward|It's important to note|Without further ado|Before we begin|First and foremost" <4 audit files> | wc -l` returns 0
    - Banned-construct grep returns 0 matches: `grep -nE '<(div|details|table|svg|a name|a id)|\$\$|<!DOCTYPE' <4 audit files> | wc -l` returns 0
    - Phase 3 typo grep returns exactly 5 matches (pre-fix): `grep -nE '\bmyt\b|apapting|before before|undrestand|orgs specific' README.md work.md style.md playbooks.md | wc -l` returns 5
    - Link target verification: `git ls-files | grep -Fx 'work.md'` returns 1; same for `style.md`, `playbooks.md`, `coop-d1.md`, `junior-d2.md`, `intermediate-d3.md`, `senior-d4.md`, `staff-d5.md`, `README.md`
    - Anchor target heading exists: `grep -nE '^### Dev Expectations\b' work.md` returns 1 line
    - Hub size budget unchanged (Task 1 makes no file edits): `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55
    - Atomic commit landed: `git log -1 --format=%s` matches `docs(04): audit findings`
    - 04-AUDIT.md "Hub Standalone Test" section is present but explicitly notes "POST-FIX" / "populated in Task 3" — the test does NOT run pre-fix (D-04-14, RESEARCH.md Pitfall 2)
  </acceptance_criteria>
  <done>
    `04-AUDIT.md` exists at `.planning/phases/04-voice-and-link-audit/04-AUDIT.md`, populated with: a Findings Summary table covering QUAL-01 link verification, QUAL-02 trust-claim rows (6 from grep + F-01 cross-reference), SC4 voice greps (HR / warmup / banned / inflation — all 0 today), and the 11 carryforward rows (C-01..C-11). Voice Read-Aloud Notes section drafted per spoke. Hub Standalone Test section is placeholder pending Task 3. One atomic commit `docs(04): audit findings` lands the file. No source file is edited in this task.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bdon walks the audit table and confirms dispositions</name>
  <files>.planning/phases/04-voice-and-link-audit/04-AUDIT.md (read-only here; Bdon may annotate dispositions inline if overriding any default)</files>
  <what-built>
    Task 1 wrote `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` with every finding from the four automated checks (QUAL-01 link integrity, QUAL-02 trust-claim, SC4 voice-greps, Phase 3 carryforward typos) plus the named carryforward rows (C-01..C-11 + F-01). Each row has a default disposition (`fix-now`, `defer-decision-for-Bdon`, `defer (v2)`, `no-action`) per D-04-13 / D-04-04 / RESEARCH.md seed tables. No source file has been edited yet — the audit is a read-only artifact at this point. The hub-standalone test is deliberately NOT run yet (D-04-14: it runs post-fix in Task 3).
  </what-built>
  <action>
This is a `checkpoint:human-verify` task — Bdon performs the verification described in `<how-to-verify>`. Claude pauses execution until Bdon types `approved` (with any per-row dispositions noted inline) or describes revisions needed. No automation runs in this task; the disposition decisions Bdon records here drive Task 3's atomic fix-commits.

If Bdon overrides any default disposition during the walk-through, update the affected row(s) inline in `04-AUDIT.md` BEFORE resuming Task 3. If Bdon requests a regrouping of the atomic-commit boundaries listed in the `<interfaces>` block of this plan, capture the new grouping as a comment block at the top of the 04-AUDIT.md Findings Summary section before Task 3 begins.
  </action>
  <how-to-verify>
1. Open `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` and read top to bottom.
2. For each finding row, confirm or override the default disposition. Use one of: `fix-now` (apply the recommendation), `defer (v2)` (note where it goes), `no-action` (log only, no edit).
3. Pay extra attention to these rows (RESEARCH.md "Open Questions"):
   - **F-01 / T-01: Flexibility line in `style.md:12`** (FIX-03 was withdrawn 2026-05-12). Three options in the recommendation column: preserve as-is, anchor with behavioral example, soften per FIX-03 original. Bdon's call.
   - **C-06: `## Bdons Style` heading**. Two options: keep H2 + add apostrophe → `## Bdon's Style`, OR apply C-01 H1-promotion → `# Style` (resolves both grammar and hierarchy in one move). Bdon picks.
   - **C-09: redundant `Dev Expectations →` link on hub**. Two options: keep current shape (HUB-04/HUB-05 satisfied; visual ambiguity acceptable) OR add `_(section within Work)_` tease modifier. Bdon picks.
   - **C-11: power-asymmetry one-line acknowledgment in `style.md` Feedback section**. Two options: add a one-line lede (PITFALLS Pitfall 2 template — see RESEARCH.md "Behaviorally Anchored — Concrete Templates" → "Acknowledge the asymmetry"), OR defer to v2.
   - **C-07: `orgs specific` apostrophe in `work.md:26`**. Default is `defer-decision`; RESEARCH.md notes this is the least-judgmental of the IN items and could reasonably be `fix-now`. Bdon picks.
   - **A6: `behaviours` British spelling 2x in `style.md`**. NOT in the original typo list. RESEARCH.md surfaces it as `defer-decision-for-Bdon` — intentional voice or accidental?
4. For each row Bdon defers (`defer (v2)`), note where it goes (e.g., "v2 — Hub TL;DR candidate", "v2 — feedback-section expansion", or "post-Phase-4 manual").
5. For each `fix-now` confirmation, confirm Bdon is ok with the atomic-commit grouping in the `<interfaces>` block above (or request a regrouping before Task 3 begins).
6. When done, type `approved` (with any deltas noted inline) or describe needed revisions to 04-AUDIT.md before Task 3 begins.

The verification IS the audit — there is no automated check that can decide voice. Bdon's dispositions are the binding output of this task.
  </how-to-verify>
  <verify>
    <automated>[ -f .planning/phases/04-voice-and-link-audit/04-AUDIT.md ] && grep -q '^# Phase 4 Audit Report' .planning/phases/04-voice-and-link-audit/04-AUDIT.md && [ -z "$(git status --porcelain README.md work.md style.md playbooks.md)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Bdon's checkpoint message ends with `approved` (with any inline per-row overrides) OR is a clear list of revisions to make to 04-AUDIT.md before Task 3 begins. The execution log captures the disposition decisions as the durable audit trail for Phase 4.
    - If Bdon overrode any default disposition, `04-AUDIT.md` was updated inline before Task 3 begins (the file reflects the post-checkpoint state, not the Task 1 defaults).
    - No source files are modified in this task: `git status --porcelain README.md work.md style.md playbooks.md` returns empty.
    - No commits land in this task — disposition decisions are checkpoint output, not git history. Fix-commits all happen in Task 3.
  </acceptance_criteria>
  <done>
    Bdon has walked the entire `04-AUDIT.md` table and recorded a confirmed disposition (`fix-now`, `defer (v2)`, `no-action`) for every row. Any deferred row carries an explicit "where it goes" note. The checkpoint message ends with `approved`. Task 3 may now execute against the confirmed disposition set.
  </done>
  <resume-signal>Type "approved" (with any per-row deltas noted) or describe revisions needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply Bdon-approved fixes (atomic per-thematic commits) + Hub Standalone Test</name>
  <files>README.md, work.md, style.md, playbooks.md, .planning/phases/04-voice-and-link-audit/04-AUDIT.md</files>
  <read_first>
    - .planning/phases/04-voice-and-link-audit/04-AUDIT.md (Bdon-confirmed dispositions from Task 2; the source of truth for what fixes execute)
    - README.md (target of WR-03 / C-03 fix and possibly C-09 redundant-link tease)
    - work.md (target of WR-02 / C-02, WR-04 / C-04, C-01 H1, possibly C-07 / IN-03 apostrophe)
    - style.md (target of IN-01 / C-05, C-01 H1, possibly C-06 heading change, possibly F-01 / T-01 Flexibility-line fix, possibly C-11 power-asymmetry one-liner)
    - playbooks.md (target of C-01 H1 if approved)
    - .planning/phases/04-voice-and-link-audit/04-CONTEXT.md D-04-11 (protected markers — never edited even if they appear in a fix range) and D-04-12 (atomic-commit grouping)
    - .planning/phases/04-voice-and-link-audit/04-RESEARCH.md "Behaviorally Anchored — Concrete Templates" (exact rewrite shapes for trust-claim fixes, if Bdon approves any), "Atomic Commit Template" (commit message shape)
  </read_first>
  <action>
**Step 1 — Apply fixes per file, in atomic per-thematic commits (D-04-12 grouping).** Execute only the rows Bdon confirmed `fix-now` in Task 2. Order within Wave is Claude's-Discretion; recommended order below. Each substep is its own commit. If Bdon's dispositions skip a substep entirely (no `fix-now` rows for that file/theme), skip the commit and proceed to the next.

**Step 1a — README.md typo fix (WR-03 / C-03)** (if `fix-now`):

Edit `README.md` line 11:
- Before: `* Adaptability: I prefer apapting over resisting, and all my opinions are loosely held`
- After: `* Adaptability: I prefer adapting over resisting, and all my opinions are loosely held`

After the edit, verify size budget: `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55. (A single word swap can't breach this, but verify.)

Commit:
```
docs(04): fix apapting typo in hub Values block

Resolves WR-03 from Phase 3 03-REVIEW.md. apapting -> adapting.
Voice unchanged; word swap only.
```

If Bdon also approved any C-09 (redundant Dev Expectations link) fix-shape in Task 2, apply it in the same commit. If C-09 fix is "preserve current shape", do nothing for C-09.

**Step 1b — work.md typo + H1 fix (WR-02 / C-02, WR-04 / C-04, C-01 H1, possibly C-07 / IN-03)** (if `fix-now`):

Edit `work.md`:
- Line 1: `_How I evaluate peeps, myt four general principles, and specific role expectations._` → `_How I evaluate peeps, my four general principles, and specific role expectations._` (C-02).
- Line 17: `... set a time limit for said feedback before before you take action.` → `... set a time limit for said feedback before you take action.` (C-04).
- If C-01 H1 approved: add `# Work` as line 1 (then blank line, then existing orientation italic; demote/remove existing `## Work` at L3 — if Bdon's recommended shape is the H1-replacement-of-H2 from 03-REVIEW.md WR-01 example, delete the `## Work` line entirely; if Bdon prefers keeping the H2 as a section divider, leave it. Default per RESEARCH.md / 03-REVIEW.md: replace H2 with H1).
- If C-07 / IN-03 approved: change `[orgs specific definitions]` → `[org's specific definitions]` on line 26.

Confirm the anchor target heading is preserved (RESEARCH.md verified slug `#dev-expectations` is unaffected by H1/H2 changes elsewhere in the file): `grep -nE '^### Dev Expectations\b' work.md` returns exactly 1 line.

Commit:
```
docs(04): fix work.md typos + add H1

- WR-02 / C-02: myt -> my (orientation italic line 1)
- WR-04 / C-04: remove duplicate `before` (line 17)
- C-01 / WR-01: add `# Work` H1 (only if Bdon approved in Task 2)
- C-07 / IN-03: orgs -> org's possessive (only if Bdon approved)

Anchor work.md#dev-expectations preserved (still resolves to ### Dev Expectations).
Voice unchanged; mechanical corrections only.
```

**Step 1c — style.md typo + H1 + voice-judgment fixes (IN-01 / C-05, C-01 H1, C-06 heading, possibly F-01 / T-01 Flexibility, possibly C-11 power-asymmetry)** (if `fix-now`):

Edit `style.md`:
- Line 16: `... to really undrestand it, ...` → `... to really understand it, ...` (C-05).
- If C-01 H1 approved AND C-06 resolution is "H1-promote" (RESEARCH.md A5 — most likely default): replace `## Bdons Style` at line 3 with `# Style` (this resolves both C-01 AND C-06 in one edit).
- If C-01 H1 approved AND C-06 resolution is "keep H2 + add apostrophe": add `# Style` as line 3 H1 AND change `## Bdons Style` to `## Bdon's Style` (slug unaffected).
- If F-01 / T-01 Flexibility line `fix-now: anchor`: replace `style.md:12` with Bdon's chosen template from RESEARCH.md "Behaviorally Anchored — Concrete Templates" → "Add a behavioral example inline" or "Soften to falsifiable claim". Bdon's exact text from Task 2.
- If C-11 power-asymmetry approved: insert the one-line lede above the Receiving Feedback bullet per RESEARCH.md "Acknowledge the asymmetry" template (Bdon's exact text from Task 2).

Commit:
```
docs(04): fix style.md typos + add H1

- IN-01 / C-05: undrestand -> understand
- C-01 / WR-01: add `# Style` H1 (only if Bdon approved)
- C-06 / IN-02: resolve `## Bdons Style` per Bdon's pick (only if approved)
- F-01: anchor or soften Flexibility line (only if Bdon picked fix-now)
- C-11: add power-asymmetry one-liner (only if Bdon picked fix-now)

Voice-judgment fixes are Bdon-authored text per checkpoint approval.
```

If F-01 and/or C-11 land, those changes are voice-sensitive — do NOT smooth or paraphrase Bdon's exact approved text. Use the Edit tool with the verbatim string from 04-AUDIT.md.

**Step 1d — playbooks.md H1 fix (C-01)** (if `fix-now`):

Edit `playbooks.md`:
- If C-01 H1 approved AND Bdon's resolution is "H1-promote": replace `## Playbooks` at line 3 with `# Playbooks`.
- If C-01 H1 approved AND Bdon's resolution is "add H1 above existing H2": insert `# Playbooks` and a blank line at line 3, keeping `## Playbooks` (or removing it — Bdon's call).

Commit:
```
docs(04): add H1 to playbooks.md

Resolves WR-01 / C-01 from Phase 3 03-REVIEW.md. `# Playbooks` H1 added
so the spoke has a rendered title (MD041 satisfied; consistent with hub).
Scaffold placeholder structure unchanged.
```

**Step 2 — Re-verify invariants after all fix-commits land.** Run:

```bash
# Size budget
wc -w < README.md ; wc -l < README.md
# Phase 3 typos all gone (post-fix)
grep -cE '\bmyt\b|apapting|before before|undrestand' README.md work.md style.md playbooks.md
# H1 present on every spoke (if C-01 fix-now confirmed)
grep -c '^# ' work.md style.md playbooks.md
# Link integrity still passes
git ls-files | grep -Fx 'work.md' ; git ls-files | grep -Fx 'style.md' ; git ls-files | grep -Fx 'playbooks.md'
grep -nE '^### Dev Expectations\b' work.md
# Protected markers untouched
grep -c 'my memory is shit' style.md
grep -c 'Autonomy × ' README.md
grep -c "I'm more of a how guy" README.md
```

**Step 3 — Hub-Standalone Test (D-04-14, POST-FIX).** This step runs ONLY after Steps 1a–1d are committed (and only after Bdon has confirmed `approved` at the Task 2 checkpoint). Procedure per RESEARCH.md "Hub-Standalone Mechanic":

1. **Context isolation.** Read ONLY `README.md` (the post-fix shipped state). Do NOT reference any other file in this step. (If multiple files were already loaded in this session, the executor MUST mentally restrict context to only `README.md` content for the bullets below. The 5-bullet summary must be derivable from `README.md` alone — if a bullet draws on content not present in the hub, that's a QUAL-03 finding, not a pass.)

2. **Write the 5-bullet "What I Learned" summary** under the placeholder `## Hub Standalone Test — Reader Summary` section in `04-AUDIT.md` (replacing the Task 1 placeholder):

   - **Bullet 1 — Who is Bdon (identity).**
   - **Bullet 2 — What he values and prioritizes (the two chains).**
   - **Bullet 3 — His career thesis (the equation context — what he optimizes for).**
   - **Bullet 4 — The four operating-mechanics categories (Work / Style / Playbooks / Dev Expectations) — one phrase each.**
   - **Bullet 5 — How to know when the hub was last reviewed.**

3. **Self-assess each bullet.** For any bullet that could not be filled from `README.md` alone, note explicitly "INSUFFICIENT — bullet draws on `<other-file>`". Per RESEARCH.md "Sanity-Check Against Current Hub State" dry-run, all 5 bullets should pass against the post-fix hub.

4. **Commit the updated 04-AUDIT.md:**
```
docs(04): hub-standalone test — 5-bullet reader summary

Post-fix hub-standalone test per D-04-14. Claude read README.md
in isolation and wrote the 5-bullet summary covering identity,
values/priorities, career thesis, four-category mechanics map,
last-reviewed date. <Note: all 5 bullets pass / one or more bullets
INSUFFICIENT> — Bdon validates at the QUAL-03 checkpoint (Task 4 of
04-02-PLAN.md).
```

If the test reveals an INSUFFICIENT bullet, append a recommendation row to 04-AUDIT.md (`H-01 | QUAL-03 | README.md | <which bullet failed> | <recommended hub edit within 380w/55L budget> | defer-decision-for-Bdon`). Bdon picks "add signal" (re-runs Task 3 sub-step with a hub edit, then re-runs the test) or "defer to v2" (QUAL-03 ships as `Partial` — to be flipped in 04-02-PLAN.md accordingly).

**Step 4 — Final invariant verification.** Confirm:

- `README.md` ≤ 380w / ≤ 55L (HUB-02 budget preserved through every fix-commit).
- All 5 protected markers from D-04-11 still present (counts above).
- Phase 3 typo grep returns 0 matches across the four audit files (`grep -cE '\bmyt\b|apapting|before before|undrestand' README.md work.md style.md playbooks.md` → 0).
- Trust-claim grep on the post-fix state — record the new count (will be 6 if Bdon deferred F-01 and the other QUAL-02 rows; will be lower if Bdon approved `fix-now: anchor` for any of T-01..T-04).
- All link targets still resolve via `git ls-files`.
- Hub anchor target `### Dev Expectations` still present in `work.md`.
- Atomic commit count: at least 2 (Task 1 audit-findings commit + 1 fix-commit), typically 4–6 depending on Bdon's `fix-now` dispositions.

Do NOT touch `.planning/REQUIREMENTS.md` or `.planning/PROJECT.md` in this task — those flips happen in 04-02-PLAN.md (D-04-12 final commit). This plan ends with all approved file fixes committed and the hub-standalone test recorded in 04-AUDIT.md.
  </action>
  <verify>
    <automated>[ "$(wc -w < README.md)" -le 380 ] && [ "$(wc -l < README.md)" -le 55 ] && [ "$(grep -cE '\bmyt\b|apapting|before before|undrestand' README.md work.md style.md playbooks.md)" -eq 0 ] && grep -q 'my memory is shit' style.md && grep -q 'Autonomy × ' README.md && grep -q "I'm more of a how guy" README.md && [ "$(git ls-files | grep -cx 'work.md')" -eq 1 ] && [ "$(git ls-files | grep -cx 'style.md')" -eq 1 ] && [ "$(git ls-files | grep -cx 'playbooks.md')" -eq 1 ] && grep -nE '^### Dev Expectations\b' work.md && grep -q 'Hub Standalone Test' .planning/phases/04-voice-and-link-audit/04-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - Hub size invariant preserved: `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55
    - All five Phase 3 typo strings are gone from the four audit files: `grep -cE '\bmyt\b|apapting|before before|undrestand|orgs specific' README.md work.md style.md playbooks.md` returns 0 IF every `fix-now` row was approved; if Bdon deferred C-07 (`orgs specific`), the count is exactly 1 with the only remaining match being `orgs specific` on `work.md:26`
    - Protected markers (D-04-11) intact: `grep -c 'my memory is shit' style.md` ≥ 1, `grep -c 'Autonomy ×' README.md` ≥ 1, `grep -c "I'm more of a how guy" README.md` ≥ 1, `grep -c 'Learning -> Freedom' README.md` ≥ 1, `grep -c 'Family -> Health' README.md` ≥ 1
    - If C-01 H1 fix approved: `grep -c '^# Work$' work.md` ≥ 1 AND `grep -c '^# Style$' style.md` ≥ 1 AND `grep -c '^# Playbooks$' playbooks.md` ≥ 1
    - Link integrity preserved (post-fix): `git ls-files | grep -Fx 'work.md'` returns 1; same for `style.md`, `playbooks.md`, `README.md`; `grep -nE '^### Dev Expectations\b' work.md` returns 1
    - 04-AUDIT.md now contains the 5-bullet Hub Standalone Test reader summary: `grep -c '^## Hub Standalone Test' .planning/phases/04-voice-and-link-audit/04-AUDIT.md` ≥ 1; the section has exactly 5 bullets (executor verifies by reading); section is NOT the Task 1 placeholder (the word "placeholder" or "POST-FIX" no longer appears under the heading)
    - Atomic commit count: `git log --oneline --since="<task-2-completion-time>" | wc -l` returns ≥ 2 (one fix-commit + one hub-standalone-test commit, at minimum; up to 5 if every fix-now substep ran) — equivalently, `git log -10 --format=%s | grep -c '^docs(04):'` returns ≥ 3 (audit findings + fix-commits + hub-standalone-test)
    - REQUIREMENTS.md and PROJECT.md are NOT modified in this task: `git diff HEAD~10 HEAD -- .planning/REQUIREMENTS.md` shows no changes since end-of-Phase-3 and `git diff HEAD~10 HEAD -- .planning/PROJECT.md` shows no changes since end-of-Phase-3 (those flips are 04-02-PLAN.md territory)
    - Banned-GFM-construct grep still returns 0: `grep -cE '<(div|details|table|svg|a name|a id)|\$\$|<!DOCTYPE' README.md work.md style.md playbooks.md` returns 0
    - HR-coded language grep still returns 0: `grep -niE '\b(stakeholders?|alignment|leverage|synergize|synergy|deliverables?|operationalize|circle back|table this|value-add|low-hanging fruit|move the needle|at the end of the day)\b' README.md work.md style.md playbooks.md | wc -l` returns 0
  </acceptance_criteria>
  <done>
    All Bdon-approved `fix-now` rows from 04-AUDIT.md are applied as atomic per-thematic-group commits. Hub size budget preserved (≤ 380w / ≤ 55L). Phase 3 typo strings gone from audit files (`orgs specific` may remain only if C-07 deferred). Protected markers untouched. Hub-standalone 5-bullet reader summary added to 04-AUDIT.md (post-fix); section explicitly notes whether all 5 bullets passed or one+ were INSUFFICIENT. REQUIREMENTS.md / PROJECT.md NOT touched — those flips ship in 04-02-PLAN.md. At least 2 atomic fix-commits + 1 hub-standalone-test commit land in this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file system → git index → GitHub | Audit reads 9 markdown files; fix-commits write 1–4 of them + 04-AUDIT.md; GitHub auto-renders the result. No external service is touched. |
| Bdon's voice (source artifact) → Claude's edit suggestions | The Phase 4 audit produces edit recommendations. Any auto-applied edit would tamper with the trust signal (Bdon's voice). The Task 2 checkpoint is the trust boundary that prevents this. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Voice-sensitive prose during audit (Pitfall 8 — "polish pass erases voice") | mitigate | Every voice-judgment row (`F-01`, `T-01..T-04`, `C-06`, `C-11`) defaults to `defer-decision-for-Bdon`. The Task 2 checkpoint is a blocking gate; no `fix-now` executes without Bdon's explicit confirmation per row. Task 3 acceptance criteria pin protected-marker counts via grep so any silent erasure is caught. |
| T-04-02 | Tampering | Hub size budget (HUB-02 ≤ 380w / ≤ 55L) during fixes | mitigate | Every hub fix-commit verifies `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55 in acceptance criteria. Pre-fix headroom is 64w / 7L — meaningful but not unlimited. If a Bdon-approved fix breaches the budget, the executor halts and surfaces the conflict. |
| T-04-03 | Spoofing / Tampering | Anchor target `./work.md#dev-expectations` during work.md H1 / structural edits | mitigate | RESEARCH.md "GFM Slug Algorithm" verifies the anchor depends only on heading TEXT, not LEVEL. Both Task 1 acceptance and Task 3 acceptance grep `^### Dev Expectations\b' work.md` to confirm the heading text (and therefore the slug `dev-expectations`) is preserved through any H1-addition or H2-demotion edit. |
| T-04-04 | Repudiation | Audit findings rationale (which decisions were Bdon's, which were Claude's defaults) | mitigate | Every row in 04-AUDIT.md records `disposition` and an explicit recommendation. Bdon's confirmations land via the Task 2 checkpoint message ("approved" or per-row overrides) so the GSD execution log captures the audit trail. Atomic per-thematic commit messages name the specific carryforward IDs they resolve (`WR-02 / C-02`, `WR-04 / C-04`, etc.) — git history is the durable record. |
| T-04-05 | Information Disclosure | macOS APFS case-insensitivity false-positive on `readme.md` / `README.md` link resolution | mitigate | D-04-09 mandates `git ls-files | grep -Fx` (case-sensitive, matches Linux render host) for all link-integrity checks. Acceptance criteria in both Task 1 and Task 3 use `git ls-files` exclusively; `test -e` is forbidden. Phase 3 03-02 SUMMARY already documented this gotcha. |
| T-04-06 | Denial of Service | Broken hub deep-link `./work.md#dev-expectations` if the spoke heading text drifts | mitigate | Pre-audit verification (Task 1 Step 1) AND post-fix verification (Task 3 Step 2) both run `grep -nE '^### Dev Expectations\b' work.md` and fail loud if absent. If a fix accidentally retitles the section (e.g., to "## Dev Expectations" or "### Developer Expectations"), the hub link 404s on GitHub render — caught before commit. |
| T-04-07 | Elevation of Privilege | Auto-introduced npm/python tooling (markdownlint, markdown-link-check) that Phase 4 doesn't sanction | accept | RESEARCH.md "Common Pitfalls" Pitfall 3 explicitly bars this. Plan tasks specify only `git`, `grep`, `wc` — all already-on-machine. No `npm install`, no `pip install`. If an executor surfaces a tooling-gap, they pause and surface (do not silently install). Low likelihood given explicit task wording. |
| T-04-08 | Information Disclosure | D-file pre-existing content auto-edited for voice (out of scope per D-04-04) | mitigate | Trust-claim grep covers D-files (T-05, T-06 rows in 04-AUDIT.md) but their default disposition is `no-action — pre-existing D-file content`. Task 3 fixes ONLY operate on `README.md`, `work.md`, `style.md`, `playbooks.md` — the four audit-edit-scope files. D-files are explicitly NOT in the `files_modified` frontmatter. |
</threat_model>

<verification>
After all three tasks complete:

- `.planning/phases/04-voice-and-link-audit/04-AUDIT.md` exists, tracked in git, contains all four check sections (QUAL-01, QUAL-02, SC4, carryforward) plus the post-fix Hub Standalone reader summary plus Voice Read-Aloud notes per spoke.
- All Bdon-approved `fix-now` rows have landed as per-thematic atomic commits (`git log -10 --format=%s | grep -c '^docs(04):'` returns ≥ 3).
- Hub size invariant intact: `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55.
- All link targets resolve via `git ls-files` (post-fix re-verification matches Task 1 pre-fix state).
- Anchor target `### Dev Expectations` preserved in `work.md`.
- All 5 D-04-11 protected markers grep ≥ 1 in their respective files.
- Phase 3 typo strings gone (`grep -cE '\bmyt\b|apapting|before before|undrestand' README.md work.md style.md playbooks.md` returns 0); `orgs specific` count depends on Bdon's C-07 disposition.
- HR-coded language grep + warmup-phrase grep + banned-GFM-construct grep all return 0 (post-fix, same as pre-fix — voice didn't degrade).
- REQUIREMENTS.md and PROJECT.md UNCHANGED in this plan — that flip is 04-02-PLAN.md's job.
</verification>

<success_criteria>
- QUAL-01 evidence: link-integrity table in 04-AUDIT.md confirms all relative .md links + the `dev-expectations` anchor resolve via `git ls-files`. (Closes ROADMAP Phase 4 SC1.)
- QUAL-02 evidence: 04-AUDIT.md has a disposition row for each of the 6 trust-claim matches; HR-coded grep and warmup-phrase grep both 0; banned-GFM-construct grep 0. Bdon's approved fixes (if any) preserve voice per Pitfall 8. (Closes ROADMAP Phase 4 SC2.)
- QUAL-03 evidence: 04-AUDIT.md "Hub Standalone Test — Reader Summary" section has 5 bullets written from `README.md` alone (post-fix). Bdon validates at the 04-02-PLAN.md QUAL-03 checkpoint; if INSUFFICIENT bullets appeared, this plan logged them with recommendations. (Sets up ROADMAP Phase 4 SC3; final flip in 04-02-PLAN.md.)
- ROADMAP SC4 evidence: 04-AUDIT.md "Voice Read-Aloud Notes" section per spoke; flagged sentences (if any) carry Bdon's per-row disposition; distinctive markers intact (verified by grep in acceptance criteria).
- Carryforward evidence: every Phase 3 03-REVIEW.md finding (WR-01..04 + IN-01..06) has a `C-NN` row; FIX-03 Flexibility line has an `F-01` row; PITFALLS Pitfall 2 power-asymmetry has a `C-11` row. Each has a Bdon-confirmed disposition.
- Atomic-commit discipline: ≥ 3 commits land (`docs(04): audit findings` + ≥ 1 fix-commit + `docs(04): hub-standalone test — 5-bullet reader summary`), each independently revertable, each commit message names its resolved IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-and-link-audit/04-01-SUMMARY.md` capturing:
- Total findings count by check (QUAL-01 / QUAL-02 / SC4 / carryforward).
- Bdon's per-row dispositions in summary (e.g., "8 fix-now applied, 3 defer (v2), 2 no-action").
- List of atomic fix-commit SHAs landed in Task 3, each with the IDs it resolves.
- Hub-Standalone Test outcome (all 5 bullets passed / N bullets INSUFFICIENT with recommendation).
- Post-fix grep snapshot (trust-claim grep count, HR-grep count, typo grep count, link verification, protected-marker presence).
- Note for 04-02-PLAN.md: which QUAL-* requirements should flip to Complete vs Partial vs (rare) stay Pending.
- Note for v2: every row Bdon deferred with explicit "where it goes" tag (e.g., "Hub TL;DR — v2", "Feedback-section expansion — v2", "post-Phase-4 manual").
</output>
</content>
</invoke>