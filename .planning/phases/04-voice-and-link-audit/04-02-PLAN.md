---
phase: 04-voice-and-link-audit
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
tags:
  - markdown
  - requirements
  - phase-closeout

must_haves:
  truths:
    - "REQUIREMENTS.md reflects the audit outcome — QUAL-01..QUAL-03 flip from `[ ]`/`Pending` to `[x]`/`Complete` (or `Partial` if Bdon deferred any check at the Task 1 Step 3 hub-standalone checkpoint per D-04-14)"
    - "PROJECT.md Active list moves QUAL-02 (the listed active requirement) to Validated; Key Decisions table gains a Phase 4 close-out row capturing dispositions Bdon picked"
    - "Doc-only patch lands as a single atomic commit `docs(04): mark QUAL-01..QUAL-03 complete in REQUIREMENTS.md` mirroring Phase 2 02-01 and Phase 3 03-02 Task 2 precedents"
    - "No source file (README.md, work.md, style.md, playbooks.md) is touched in this plan — all content fixes shipped in 04-01-PLAN.md Task 3"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "QUAL-01..QUAL-03 marked complete (or partial), Traceability rows updated, Last-updated footer bumped"
      contains: "- [x] **QUAL-01"
    - path: ".planning/PROJECT.md"
      provides: "Active requirement QUAL-02 moved to Validated; Phase 4 close-out logged in Key Decisions table"
      contains: "Phase 4"
  key_links:
    - from: ".planning/REQUIREMENTS.md Hub-of-truth"
      to: "Phase 4 audit outcome in 04-AUDIT.md + 04-01-SUMMARY.md"
      via: "QUAL-01..QUAL-03 checklist and Traceability rows flip per audit close-out"
      pattern: '- \[x\] \*\*QUAL-0[1-3]\*\*'
    - from: ".planning/PROJECT.md Active list"
      to: "Validated section"
      via: "Phase 4 close-out moves QUAL-02 voice-preservation row down"
      pattern: 'Validated'
---

<objective>
Mark QUAL-01..QUAL-03 complete in `.planning/REQUIREMENTS.md` (or `Partial` per any Bdon-deferred check from 04-01 Task 2/3) and reconcile `.planning/PROJECT.md` to reflect the Phase 4 close-out. This is a doc-only patch — no source file is touched. Mirrors the Phase 2 02-01 and Phase 3 03-02 Task 2 precedent of landing the REQUIREMENTS.md check-off as a single atomic commit at the end of the phase.

Purpose: Close Phase 4 by bringing the source-of-truth requirements docs in sync with the shipped audit + fix work. Downstream readers (next milestone planner, code reviewer, anyone running `gsd-sdk query state.load`) see correct status. Avoids the failure mode where REQUIREMENTS still shows `Pending` after the phase ships.

Output: REQUIREMENTS.md with QUAL-01..QUAL-03 flipped to `[x]`/`Complete` (or `Partial`), Last-updated footer bumped; PROJECT.md Active list shows QUAL-02 moved to Validated and Key Decisions table updated; one atomic commit lands the patch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-voice-and-link-audit/04-CONTEXT.md
@.planning/phases/04-voice-and-link-audit/04-01-PLAN.md
@.planning/phases/03-hub-restructure-and-rename/03-02-PLAN.md

<interfaces>
<!-- Exact lines to patch in REQUIREMENTS.md and PROJECT.md. Extracted verbatim. -->

REQUIREMENTS.md current state (from L33–L35 — Quality section):
```
- [ ] **QUAL-01**: All cross-file links resolve — no broken links across hub, spokes, and D-files
- [ ] **QUAL-02**: Voice preserved across all files — terse, deadpan, structured, no warmup, no inflation, no HR-coded language; no aspirational claims unanchored in behavior
- [ ] **QUAL-03**: Hub standalone test passes — a reader can grasp Bdon's identity + the *categories* of his operating mechanics from the hub alone, without clicking any link
```

Target state if ALL three checks pass (full audit ship):
```
- [x] **QUAL-01**: All cross-file links resolve — no broken links across hub, spokes, and D-files
- [x] **QUAL-02**: Voice preserved across all files — terse, deadpan, structured, no warmup, no inflation, no HR-coded language; no aspirational claims unanchored in behavior
- [x] **QUAL-03**: Hub standalone test passes — a reader can grasp Bdon's identity + the *categories* of his operating mechanics from the hub alone, without clicking any link
```

If Bdon deferred any check (rare — happens if the QUAL-03 hub-standalone test surfaced INSUFFICIENT bullets that Bdon chose to defer-to-v2 per D-04-14): the deferred check stays `[ ]` AND its row in the Traceability table reads `Partial` (not `Complete`). 04-01-SUMMARY.md from 04-01-PLAN.md records which check (if any) shipped Partial.

REQUIREMENTS.md current Traceability rows (from L87–L89):
```
| QUAL-01 | Phase 4 | Pending |
| QUAL-02 | Phase 4 | Pending |
| QUAL-03 | Phase 4 | Pending |
```

Target (default — all pass):
```
| QUAL-01 | Phase 4 | Complete |
| QUAL-02 | Phase 4 | Complete |
| QUAL-03 | Phase 4 | Complete |
```

REQUIREMENTS.md "Last updated" footer at L103 (current):
```
*Last updated: 2026-05-14 — HUB-01..HUB-06 marked complete after Phase 3 ship*
```

Target:
```
*Last updated: YYYY-MM-DD — QUAL-01..QUAL-03 marked complete after Phase 4 audit ship*
```
(Use today's date.)

PROJECT.md Active list current (from L16–L17):
```
- [ ] `playbooks.md` copy drafted, holding Evaluate / Delegate / 1:1s / Decisions inline, in Bdon's voice (Phase 2 shipped scaffold + placeholders; Bdon-authored copy still pending)
- [ ] Voice preserved across all files: terse, deadpan, structured, no warmup, no inflation (Phase 4 audit — QUAL-02)
```

Target (move the QUAL-02 active line to Validated):
- The "playbooks.md copy drafted" item stays in Active (it's a post-Phase-4 manual content task, not a Phase 4 deliverable).
- The "Voice preserved..." item moves up to Validated with `*Validated in Phase 4 (Voice and Link Audit)*` annotation.

PROJECT.md Key Decisions table will need a Phase 4 close-out row. Pattern from prior phases (see existing rows): one row per phase with the date and a short summary of what shipped. Phase 4 row template:
```
| 2026-MM-DD | Phase 4 ship | Audit + fix close-out: <N> fix-commits landed for Phase 3 carryforward typos + spoke H1s; QUAL-01 / QUAL-02 / QUAL-03 marked Complete; <list of any deferred items, e.g., 'C-09 redundant Dev Expectations link — accepted as-is', 'C-11 power-asymmetry — deferred to v2'>. See 04-AUDIT.md for full disposition log. |
```
(Executor fills in `N` and the deferred-items list from 04-01-SUMMARY.md.)

D-04-12 commit pattern (mirrors Phase 3 03-02 Task 2, Phase 2 02-01):
```
docs(04): mark QUAL-01..QUAL-03 complete in REQUIREMENTS.md

Phase 4 close-out: link integrity, voice/trust-claim audit, and
hub-standalone test pass per 04-AUDIT.md. Updates REQUIREMENTS.md
checklist + Traceability table + Last-updated footer; reconciles
PROJECT.md Active → Validated for QUAL-02; logs Phase 4 close-out
in Key Decisions.
```

D-04-15 scope discipline reminder: this plan touches ONLY `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md`. No source file edits. No new requirements added. If 04-01-SUMMARY.md surfaced any new ideas during the audit, they go to PROJECT.md "Out of Scope" or REQUIREMENTS.md "v2 Requirements" — NOT to v1 Active.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch REQUIREMENTS.md + PROJECT.md to reflect Phase 4 close-out</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — Quality checklist at L33–L35, Traceability rows for QUAL-01..QUAL-03 at L87–L89, Last-updated footer at L103)
    - .planning/PROJECT.md (Active list at L16–L17 containing the QUAL-02 voice line; Validated section above; Key Decisions table — find via grep for `Key Decisions`)
    - .planning/phases/04-voice-and-link-audit/04-01-SUMMARY.md (definitive record of which checks passed Complete vs Partial; deferred-items list; fix-commit count)
    - .planning/phases/04-voice-and-link-audit/04-AUDIT.md (cross-reference if 04-01-SUMMARY.md needs disambiguation on any disposition)
    - .planning/phases/03-hub-restructure-and-rename/03-02-PLAN.md Task 2 (precedent for this same patch pattern, applied to HUB-01..HUB-06 in Phase 3 — use as the canonical reference for commit message shape and edit discipline)
  </read_first>
  <action>
**Step 1 — Determine the disposition per QUAL.** Read `.planning/phases/04-voice-and-link-audit/04-01-SUMMARY.md`. For each of QUAL-01, QUAL-02, QUAL-03, record:

- `Complete` — all audit findings under this check were resolved (every `fix-now` landed; every `defer (v2)` is logged with its destination; every `no-action` is captured in 04-AUDIT.md).
- `Partial` — at least one finding under this check was deferred WITHOUT a resolved disposition and Bdon explicitly tagged it `QUAL-XX partial` in the Task 2 checkpoint (or in the Task 3 hub-standalone QUAL-03 outcome).

Default expectation per RESEARCH.md: QUAL-01 Complete, QUAL-02 Complete (deferred-to-v2 rows are still "resolved" with a destination — that's a Complete disposition), QUAL-03 Complete. If the hub-standalone test in 04-01 Task 3 Step 3 surfaced an INSUFFICIENT bullet AND Bdon picked "defer to v2", QUAL-03 is `Partial`. Verify against 04-01-SUMMARY.md before patching.

**Step 2 — Patch REQUIREMENTS.md.** Three edits:

**2a. Quality checklist (L33–L35).** For each QUAL with disposition `Complete`, flip `- [ ]` to `- [x]`. For each `Partial`, leave `- [ ]` and append ` *(Partial — see 04-AUDIT.md)*` to the line. Bullet text stays otherwise verbatim.

Default (all Complete):
```
- [x] **QUAL-01**: All cross-file links resolve — no broken links across hub, spokes, and D-files
- [x] **QUAL-02**: Voice preserved across all files — terse, deadpan, structured, no warmup, no inflation, no HR-coded language; no aspirational claims unanchored in behavior
- [x] **QUAL-03**: Hub standalone test passes — a reader can grasp Bdon's identity + the *categories* of his operating mechanics from the hub alone, without clicking any link
```

Partial example (only if Bdon explicitly tagged QUAL-03 partial):
```
- [x] **QUAL-01**: All cross-file links resolve — no broken links across hub, spokes, and D-files
- [x] **QUAL-02**: Voice preserved across all files — terse, deadpan, structured, no warmup, no inflation, no HR-coded language; no aspirational claims unanchored in behavior
- [ ] **QUAL-03**: Hub standalone test passes — a reader can grasp Bdon's identity + the *categories* of his operating mechanics from the hub alone, without clicking any link *(Partial — see 04-AUDIT.md)*
```

**2b. Traceability table (L87–L89).** For each `Complete` QUAL, change Status column from `Pending` to `Complete`. For each `Partial`, change to `Partial`. Bullet structure and column widths stay exactly as-is.

Default:
```
| QUAL-01 | Phase 4 | Complete |
| QUAL-02 | Phase 4 | Complete |
| QUAL-03 | Phase 4 | Complete |
```

**2c. Last-updated footer (L103).** Replace:
```
*Last updated: 2026-05-14 — HUB-01..HUB-06 marked complete after Phase 3 ship*
```
With:
```
*Last updated: YYYY-MM-DD — QUAL-01..QUAL-03 marked complete after Phase 4 audit ship*
```
(Use today's date in YYYY-MM-DD form.)

Do NOT touch any other content in REQUIREMENTS.md. Do NOT change SPOKE-* (they belong to Phase 2 and are Bdon-content-pending separately), FIX-* (Phase 1), or HUB-* (Phase 3 — already Complete). Do NOT alter Coverage notes, Out of Scope table, or v2 Requirements section.

**Step 3 — Patch PROJECT.md.**

**3a. Move QUAL-02 row from Active to Validated.** Active list currently has:
```
- [ ] Voice preserved across all files: terse, deadpan, structured, no warmup, no inflation (Phase 4 audit — QUAL-02)
```
Cut this line from Active. Add to the Validated section (above Active) as:
```
- [x] Voice preserved across all files: terse, deadpan, structured, no warmup, no inflation — *Validated in Phase 4 (Voice and Link Audit) — see 04-AUDIT.md*
```
(Match the existing Validated-section formatting — italic-postfix `*Validated in Phase N (Phase Name) — ...*` style.)

Leave the other Active line (`playbooks.md copy drafted...`) untouched — it's a post-Phase-4 Bdon-authored content task, not a Phase 4 deliverable.

**3b. Add Phase 4 close-out row to Key Decisions table.** Locate the Key Decisions table in PROJECT.md (grep for `Key Decisions` heading). Append a new row at the bottom:
```
| YYYY-MM-DD | Phase 4 ship | Audit + fix close-out: <N> atomic fix-commits applied for Phase 3 03-REVIEW.md carryforward (WR-01..04 + IN-01..06 dispositions); QUAL-01 / QUAL-02 / QUAL-03 → Complete <or "QUAL-03 → Partial" if applicable>. Bdon's per-row dispositions logged in 04-AUDIT.md; deferred items: <list from 04-01-SUMMARY.md, e.g., "C-11 power-asymmetry one-liner → v2", "C-09 redundant Dev Expectations link → accepted as-is", "F-01 Flexibility line → preserve as-is per Bdon checkpoint">. |
```

Fill `N`, the QUAL outcome, and the deferred-items list from 04-01-SUMMARY.md. Use today's date.

If the Key Decisions table in PROJECT.md uses a different row schema than the example above, match the existing schema verbatim — do NOT introduce a new column. Read the table headers first; align with them.

Do NOT touch other PROJECT.md sections (What This Is, Core Value, Out of Scope, Context, Validated list above QUAL-02's new position).

**Step 4 — Commit.** Single atomic commit per D-04-12:

```
docs(04): mark QUAL-01..QUAL-03 complete in REQUIREMENTS.md

Phase 4 close-out: link integrity, voice/trust-claim audit, and
hub-standalone test all pass per 04-AUDIT.md. Updates REQUIREMENTS.md
checklist (3x [ ] -> [x]), Traceability rows (3x Pending -> Complete),
and Last-updated footer. PROJECT.md Active → Validated for QUAL-02
voice preservation. New Key Decisions row records Phase 4 dispositions
and deferred-to-v2 items.

Mirrors Phase 2 02-01 and Phase 3 03-02 Task 2 patch patterns.
```

(If any QUAL shipped Partial, adjust the commit body's `3x [ ] -> [x]` count and `3x Pending -> Complete` count accordingly, and add a "QUAL-XX shipped Partial — see 04-AUDIT.md" line.)
  </action>
  <verify>
    <automated>[ "$(grep -cE '^- \[(x| )\] \*\*QUAL-0[1-3]\*\*' .planning/REQUIREMENTS.md)" -eq 3 ] && [ "$(grep -c '^- \[x\] \*\*QUAL-0[1-3]\*\*' .planning/REQUIREMENTS.md)" -ge 2 ] && [ "$(grep -cE '^\| QUAL-0[1-3] \| Phase 4 \| (Complete|Partial) \|' .planning/REQUIREMENTS.md)" -eq 3 ] && [ "$(grep -cE '^\| QUAL-0[1-3] \| Phase 4 \| Pending \|' .planning/REQUIREMENTS.md)" -eq 0 ] && grep -qE 'Last updated: 20[0-9]{2}-[01][0-9]-[0-3][0-9].*Phase 4' .planning/REQUIREMENTS.md && [ "$(grep -c '^- \[ \] .*QUAL-02' .planning/PROJECT.md)" -eq 0 ] && [ "$(grep -c '^- \[x\] Voice preserved across all files.*Validated in Phase 4' .planning/PROJECT.md)" -ge 1 ] && grep -q 'Phase 4' .planning/PROJECT.md && [ "$(grep -c '^- \[x\] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md)" -eq 6 ] && [ "$(grep -c '^- \[ \] \*\*SPOKE-' .planning/REQUIREMENTS.md)" -eq 5 ] && [ "$(grep -c '^- \[ \] \*\*FIX-0[12]\*\*' .planning/REQUIREMENTS.md)" -eq 2 ]</automated>
  </verify>
  <acceptance_criteria>
    - All three QUAL checklist items are now either `[x]` or `[x]+Partial-suffix`: `grep -cE '^- \[(x| )\] \*\*QUAL-0[1-3]\*\*' .planning/REQUIREMENTS.md` returns exactly 3 (three lines present) AND `grep -c '^- \[x\] \*\*QUAL-0[1-3]\*\*' .planning/REQUIREMENTS.md` returns ≥ 2 (at least 2 of 3 Complete; 3 of 3 in default case)
    - All three QUAL Traceability rows show `Complete` or `Partial`, not `Pending`: `grep -cE '^\| QUAL-0[1-3] \| Phase 4 \| (Complete|Partial) \|' .planning/REQUIREMENTS.md` returns exactly 3 AND `grep -cE '^\| QUAL-0[1-3] \| Phase 4 \| Pending \|' .planning/REQUIREMENTS.md` returns exactly 0
    - Last-updated footer bumped to today and names Phase 4: `grep -qE 'Last updated: 20[0-9]{2}-[01][0-9]-[0-3][0-9].*Phase 4' .planning/REQUIREMENTS.md` returns success
    - PROJECT.md Active list no longer contains the QUAL-02 voice-preservation line AND the line moved to Validated (not deleted): `grep -c '^- \[ \] .*QUAL-02' .planning/PROJECT.md` returns 0 AND `grep -c '^- \[x\] Voice preserved across all files.*Validated in Phase 4' .planning/PROJECT.md` returns ≥ 1 (positive assertion guards against accidental deletion of the line during the Active→Validated move)
    - PROJECT.md contains a Phase 4 reference (Validated annotation + Key Decisions row): `grep -c 'Phase 4' .planning/PROJECT.md` returns ≥ 2 (one in Validated, one in Key Decisions)
    - No other requirements are touched: `grep -c '^- \[x\] \*\*HUB-0[1-6]\*\*' .planning/REQUIREMENTS.md` returns exactly 6 (HUB-01..HUB-06 still Complete from Phase 3), `grep -c '^- \[ \] \*\*SPOKE-' .planning/REQUIREMENTS.md` returns exactly 5 (SPOKE-01..05 still Pending, not Phase 4 territory), `grep -c '^- \[ \] \*\*FIX-0[12]\*\*' .planning/REQUIREMENTS.md` returns exactly 2 (FIX-01, FIX-02 still Pending — Phase 1 separate concern)
    - No source files (README.md, work.md, style.md, playbooks.md) are modified in this task: `git status --porcelain README.md work.md style.md playbooks.md` returns empty (no pending changes), and `git log -1 --name-only --format=` returns only `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md`
    - Atomic commit landed with the expected message: `git log -1 --format=%s` matches `docs(04): mark QUAL-01..QUAL-03 complete in REQUIREMENTS.md`
    - Hub size budget still preserved (sanity check — this plan shouldn't touch hub but verifying invariant): `wc -w < README.md` ≤ 380 AND `wc -l < README.md` ≤ 55
    - Link integrity still passes (sanity check — invariant across phase): `git ls-files | grep -cx 'README.md'` returns 1 AND `grep -nE '^### Dev Expectations\b' work.md` returns 1
  </acceptance_criteria>
  <done>
    `.planning/REQUIREMENTS.md` has QUAL-01..QUAL-03 marked `[x]` (or `[x]` + `Partial` annotation per Bdon's hub-standalone disposition), Traceability rows updated, Last-updated footer bumped to today with Phase 4 annotation. `.planning/PROJECT.md` shows QUAL-02 moved from Active to Validated and a new Key Decisions row records the Phase 4 ship. No source file (README.md, work.md, style.md, playbooks.md) is modified. Atomic commit `docs(04): mark QUAL-01..QUAL-03 complete in REQUIREMENTS.md` records the patch. No other requirements (SPOKE-*, FIX-*, HUB-*) are touched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 04-AUDIT.md (source of truth for dispositions) → REQUIREMENTS.md / PROJECT.md (downstream readers) | Bdon's per-row dispositions from 04-01-PLAN.md Task 2 drive the QUAL flip decision. A drift between 04-AUDIT.md and the requirements doc would mislead the next milestone planner. |
| State commit → next-phase planner | The next planner runs `gsd-sdk query state.load` and reads STATE.md + REQUIREMENTS.md to determine readiness. Incorrect QUAL state = misinformed planning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | QUAL status accuracy in REQUIREMENTS.md (e.g., marking QUAL-03 Complete when Bdon shipped it Partial) | mitigate | Step 1 explicitly reads `04-01-SUMMARY.md` to determine the disposition per QUAL — the SUMMARY is the canonical record of Bdon's Task 2 + Task 3 decisions. Acceptance criteria allow either `Complete` or `Partial` so the verifier doesn't enforce a default that would mask a Partial. |
| T-04-10 | Information Disclosure | Untouched requirement IDs accidentally toggled (SPOKE-* / FIX-* / HUB-*) | mitigate | Acceptance criteria pin the exact counts: HUB-01..06 still Complete (6), SPOKE-01..05 still Pending (5), FIX-01..02 still Pending (2). Any toggle outside the QUAL range trips the verify command. |
| T-04-11 | Tampering | Source files (README.md, spokes) accidentally edited in a doc-only patch | mitigate | Acceptance criterion `git log -1 --name-only --format=` returns ONLY `.planning/REQUIREMENTS.md` and `.planning/PROJECT.md` — confirms the commit's scope. Frontmatter `files_modified` excludes source files; executor reading the plan sees the scope upfront. |
| T-04-12 | Repudiation | Phase 4 ship history (which dispositions Bdon chose) | mitigate | PROJECT.md Key Decisions row records Phase 4 ship date, fix-commit count, QUAL outcome, and the deferred-items list. Git commit message names the audit trail (`04-AUDIT.md`, `Phase 2 02-01 and Phase 3 03-02 Task 2 patch patterns`). Together they form a durable record of why each QUAL flipped where it did. |
| T-04-13 | Denial of Service | Stale `Last updated` footer in REQUIREMENTS.md misleads downstream readers | mitigate | Step 2c mandates updating the footer with today's date and a Phase 4 annotation. Acceptance criterion enforces both via a single regex grep. |
</threat_model>

<verification>
After Task 1 completes:

- REQUIREMENTS.md QUAL-01..QUAL-03 reflect the Phase 4 outcome — all Complete by default, with Partial allowed for any check Bdon explicitly deferred.
- REQUIREMENTS.md HUB-01..HUB-06 unchanged (Complete from Phase 3); SPOKE-01..05 unchanged (Pending); FIX-01, FIX-02 unchanged (Pending — Phase 1 separate); FIX-03 unchanged (Withdrawn).
- REQUIREMENTS.md Last-updated footer reflects today with Phase 4 annotation.
- PROJECT.md Active list has QUAL-02 voice line moved to Validated with `*Validated in Phase 4*` annotation.
- PROJECT.md Key Decisions table has a new row for the Phase 4 ship with date, fix-commit count, QUAL outcome, and deferred items.
- No source file edited in this plan.
- Single atomic commit recorded.
</verification>

<success_criteria>
- REQUIREMENTS.md hub-of-truth is in sync with the Phase 4 audit outcome: every Quality requirement has an explicit state (`[x]` Complete or `[x]` + Partial annotation), and Traceability rows match.
- PROJECT.md Active list is accurate: QUAL-02 no longer Active; only the post-Phase-4 manual content task (`playbooks.md` copy drafted) remains Active.
- PROJECT.md Key Decisions table records the Phase 4 ship with enough detail that a future planner can reconstruct what was done without re-reading 04-AUDIT.md.
- Atomic commit discipline preserved: one commit lands the doc-only patch, mirroring Phase 2 02-01 and Phase 3 03-02 Task 2.
- Phase 4 close-out invariants intact: hub size budget unchanged, link integrity unchanged, no source files touched.
</success_criteria>

<output>
After completion, create `.planning/phases/04-voice-and-link-audit/04-02-SUMMARY.md` capturing:
- Final QUAL state per requirement (Complete / Partial), with rationale per Partial if any.
- The single commit SHA that landed the REQUIREMENTS.md + PROJECT.md patch.
- Confirmation that no source file (README.md, work.md, style.md, playbooks.md) was edited in this plan.
- Final phase-level grep snapshot (mirrors 04-01-SUMMARY.md's snapshot for continuity).
- Pointer to 04-AUDIT.md for the full per-row disposition log.
- Pointer to STATE.md update task (which happens via `/gsd-state` after this plan, not in this plan): Phase 4 marked complete; milestone v1.0 advances to "audit shipped".
- Forward note for v2: list of deferred-to-v2 items rolled up from 04-AUDIT.md (Hub TL;DR if QUAL-03 Partial; power-asymmetry one-liner if C-11 deferred; British spelling if A6 deferred; etc.) — surfaces the v2 backlog at one well-known location.
</output>
</content>
</invoke>